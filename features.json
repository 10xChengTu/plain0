{
	"schemaVersion": 3,
	"updatedAt": "2026-07-29",
	"currentPhase": 5,
	"wipLimit": 1,
	"features": [
		{
			"id": "F001",
			"name": "Research, architecture and harness baseline",
			"phase": 0,
			"status": "complete",
			"acceptance": [
				"GitHub solution and license research is recorded",
				"Architecture and explicit product exclusions are recorded",
				"Progress, feature tracking and test strategy are resumable",
				"No temporary research clone is committed"
			],
			"evidence": {
				"commands": [
					"features.json parse and invariant check",
					"internal documentation reference check",
					"git diff --check",
					"git status --short"
				],
				"results": [
					"Passed on 2026-07-18",
					"Independent harness review blockers resolved"
				],
				"nativeScenarios": [],
				"platformGaps": [
					"No native application exists in F001; native acceptance starts with F010"
				],
				"acceptanceResults": [
					{
						"acceptance": "GitHub solution and license research is recorded",
						"result": "docs/research records the evaluated GitHub implementations, upstream licenses and rejected integration boundaries."
					},
					{
						"acceptance": "Architecture and explicit product exclusions are recorded",
						"result": "Product scope, architecture and accepted ADRs define the Tauri boundary and excluded AI, account, sync and general extension surfaces."
					},
					{
						"acceptance": "Progress, feature tracking and test strategy are resumable",
						"result": "progress.md, features.json and docs/testing.md provide the current WIP, phase gates and validation routes."
					},
					{
						"acceptance": "No temporary research clone is committed",
						"result": "The F001 completion audit and clean tracked tree confirmed that research clones and temporary artifacts were not committed."
					}
				]
			}
		},
		{
			"id": "F010",
			"name": "Tauri application and IPC foundation",
			"phase": 1,
			"status": "complete",
			"acceptance": [
				"Tauri window renders the allowlisted modular Workbench without Electron",
				"Typed command and event bridge has stable error DTOs",
				"Capabilities and CSP expose minimum permissions",
				"Architecture checks reject AI, Auth, Sync, Gallery and Extension Host entrypoints or enablement",
				"Feature completion evidence shape is validated automatically",
				"One command runs TypeScript and Rust checks"
			],
			"evidence": {
				"commands": [
					"pnpm check",
					"pnpm test:e2e:browser",
					"pnpm tauri build --debug --bundles app"
				],
				"results": [
					"pnpm check passed on 2026-07-18: formatting, TypeScript, lint, feature schema, 20 unit tests, frontend build, architecture and bundle checks, Rust fmt/clippy and 2 Rust tests",
					"Browser E2E passed 1/1 and directly audited the registered command, view-container and view IDs against the excluded surface policy",
					"macOS debug Plain.app bundle built successfully with the minimum Tauri capability and explicit CSP",
					"The bundle gate records 203 transitional source-map debt files while rejecting reachable excluded commands, hosts and workers"
				],
				"nativeScenarios": [
					"Computer Use opened Plain.app at tauri://localhost and observed the Plain window, Workbench editor group, Open File and Open Recent actions without Accounts",
					"The native command palette returned 0 Results for Chat and Extensions; Plain then quit and reported isRunning=false"
				],
				"platformGaps": [
					"F010 native rendering was accepted on macOS; Windows and Linux packaging remains F120",
					"The full filesystem, search, theme, PTY, Git and DAP native matrix remains F130",
					"The inert monaco-vscode-api missing-services debt must be physically retired before F110 can complete"
				],
				"acceptanceResults": [
					{
						"acceptance": "Tauri window renders the allowlisted modular Workbench without Electron",
						"result": "The macOS Plain.app bundle and Browser E2E rendered the allowlisted Workbench in Tauri/WKWebView with no Electron runtime."
					},
					{
						"acceptance": "Typed command and event bridge has stable error DTOs",
						"result": "TypeScript and Rust contract tests validated versioned command/event DTOs and sanitized stable error fields."
					},
					{
						"acceptance": "Capabilities and CSP expose minimum permissions",
						"result": "Architecture checks and the debug bundle audited the explicit CSP and minimum Tauri capability file."
					},
					{
						"acceptance": "Architecture checks reject AI, Auth, Sync, Gallery and Extension Host entrypoints or enablement",
						"result": "Source and final-bundle guards rejected excluded imports, commands, views, hosts and workers while recording transitional source-map debt."
					},
					{
						"acceptance": "Feature completion evidence shape is validated automatically",
						"result": "pnpm check:features validates the versioned feature contract and complete-feature evidence in CI and local checks."
					},
					{
						"acceptance": "One command runs TypeScript and Rust checks",
						"result": "pnpm check ran formatting, types, lint, feature/unit/build/architecture/bundle gates and Rust fmt, clippy and tests."
					}
				]
			}
		},
		{
			"id": "F020",
			"name": "Workspace path policy and file tree",
			"phase": 2,
			"status": "complete",
			"acceptance": [
				"Open folder and file tree CRUD work in a scoped workspace",
				"A workspace can hold multiple independently authorized canonical roots",
				"Symlink and traversal escapes are rejected",
				"External changes update the tree with rescan fallback"
			],
			"evidence": {
				"commands": [
					"pnpm check",
					"pnpm test:e2e:browser",
					"pnpm tauri:build:e2e"
				],
				"results": [
					"pnpm check passed in full on 2026-07-22: 30 frontend test files, 607 cases, 2277 frontend modules, 2112 bundle sources, 203 recorded migration-debt files and Rust 255/255.",
					"All Browser E2E scenarios passed 14/14.",
					"The move+delete incomplete-outcome matrix passed 10/10 across 5 repeated runs.",
					"An independent adversarial review of the delete incomplete-outcome matrix hardened the Harness boundary contracts to 132/132 passing hostile-mutation unit tests."
				],
				"nativeScenarios": [
					"Computer Use launched the freshly built absolute Plain.app bundle and opened a temporary single-root workspace through the native macOS folder picker.",
					"The native Explorer created an empty file, saved an exact 31-byte edit, created a nested directory, renamed within the same root and copied a file, with every result verified on disk.",
					"External file additions and deletions converged in the Explorer on the first FSEvents poll with no manual Refresh click.",
					"The Workbench DOM confirmation dialog was cancelled via both the Cancel button and Escape, leaving the target file and its disk bytes and SHA-256 unchanged.",
					"After explicit user confirmation, clicking the irreversible permanent-delete action removed the target from the Explorer, the preview editor and disk, leaving the workspace directory empty."
				],
				"platformGaps": [
					"The real multi-root desktop matrix (dual-root projection, cross-root disk write chain, per-root FSEvents convergence and root add/remove lifecycle) is registered as E2E-001 in docs/e2e-handover.md and handed over to Codex for native execution.",
					"The multi-root permanent-delete desktop variant is registered as E2E-002 in docs/e2e-handover.md as a lower-priority handover to Codex.",
					"Windows and Linux native packaging remains F120.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110."
				],
				"acceptanceResults": [
					{
						"acceptance": "Open folder and file tree CRUD work in a scoped workspace",
						"result": "Browser E2E and the real single-root desktop matrix both verified open/create/rename/copy/save/delete CRUD, with disk bytes and SHA-256 checked after every native step."
					},
					{
						"acceptance": "A workspace can hold multiple independently authorized canonical roots",
						"result": "Rust capability/watcher tests plus Browser multi-root projection, cross-root write-chain, removal and incomplete-outcome fixtures passed; the real native multi-root desktop matrix is tracked as E2E-001 in docs/e2e-handover.md."
					},
					{
						"acceptance": "Symlink and traversal escapes are rejected",
						"result": "The Rust capability-relative test suite covers nofollow resolution, TOCTOU races and mid-operation symlink swaps for every mutating command."
					},
					{
						"acceptance": "External changes update the tree with rescan fallback",
						"result": "The real single-root desktop session converged instantly through FSEvents, and the Browser fixture verified lost-wake timer convergence and independent per-root convergence across two roots."
					}
				]
			}
		},
		{
			"id": "F030",
			"name": "Editing, preview and hot-exit recovery",
			"phase": 2,
			"status": "complete",
			"acceptance": [
				"Open, preview, pin, split, edit and save files",
				"External save conflicts never overwrite silently",
				"Dirty content survives window restart",
				"Text, image and Markdown preview have safe fallbacks"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm test:e2e:browser"],
				"results": [
					"pnpm check passed in full on 2026-07-23: 33 frontend test files, 650 cases, 2283 frontend modules, 2118 bundle sources, 203 recorded migration-debt files and Rust 293/293.",
					"All Browser E2E scenarios passed 22/22 (19 carried over from S1-S5 plus 3 new S7 scenarios: editor-group split, PNG binary-fallback placeholder, Markdown plain-text fallback).",
					"Each S1-S5 slice additionally passed a focused repeat (--repeat-each=3, --retries=0) of its own new scenario(s) before the full-suite run; S7's three new scenarios likewise passed --repeat-each=3 --retries=0 (9/9) before the full-suite run."
				],
				"nativeScenarios": [
					"F030 registered no new real-desktop scenarios of its own in this slice; the real Tauri/WKWebView edit-and-save chain (open via the system folder picker, create/edit/save an exact-byte file, verify on disk) was already established during the F020 single-root desktop acceptance and is the native evidence this feature continues to rely on.",
					"F030's own desktop matrix (kill-9 restart recovery, real CloseRequested close handshake, on-disk backup directory audit) is registered as E2E-003 in docs/e2e-handover.md and handed over to Codex for native execution; it has not yet been executed."
				],
				"platformGaps": [
					"E2E-003 in docs/e2e-handover.md (real hot-exit restart recovery, external-conflict Cmd+S save, and orphan-decoration desktop matrix) is handed over to Codex and not yet executed.",
					"The real Rust WindowEvent::CloseRequested close handshake (veto until the last throttled backup is confirmed flushed, then allow close) is not implemented; PlainWorkingCopyBackupTracker.onFinalBeforeShutdown always returns false, so a normal window close may lose the most recent throttled backup write. This is declared as a known-current-state, not-a-regression condition in E2E-003 and tracked as a follow-up item in docs/e2e-handover.md.",
					"S2's externally-deleted-open-file DELETED detection uses a bounded per-root LRU (256 entries) of resources seen through plainReadFile as an approximation of \"currently open files\", not a precise open-editor/working-copy reference count; root revocation does not proactively clear this LRU, it only shrinks lazily as new reads evict old entries (harmless because the revoked root stops receiving wakes), so the approximation can lazily retain entries past root removal.",
					"The two new S5 frontend files (plain-workspace-backup-service.ts, plain-workspace-backup-tracker.ts) reuse and minimally extend existing Harness closed-set checks (MIDDLE_SERVICE_DESCRIPTORS, ALLOWED_MONACO_APP_IMPORTS, isAllowedBridgeIdentifier) rather than adding a dedicated AST hostile-mutation suite of the density used by earlier F020 slices; only the real source passing the existing checks was used as positive evidence.",
					"Windows and Linux native packaging remains F120.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110."
				],
				"acceptanceResults": [
					{
						"acceptance": "Open, preview, pin, split, edit and save files",
						"result": "Open/edit/save are covered by the existing F020 versioned read/write chain and the S3/S5 Browser E2E for dirty tracking and save; preview and pin are covered by the S1 Browser E2E (single-click preview replaces the previous preview tab, double-click or edited content promotes it to pinned). Splitting an editor into multiple groups is now covered by the S7 Browser E2E ('splits an editor into two groups that stay in sync while editing...'): the real Ctrl/Cmd+\\ `workbench.action.splitEditor` keybinding produces a second `.editor-group-container` (the real `active`/`inactive` DOM markers `EditorGroupView#setActive` toggles), both groups show a tab for the same resource, editing either side's own Monaco widget updates the other instantly and dirties both tabs together (same shared working copy), a single Ctrl/Cmd+S clears both, and closing the split side's sole tab auto-removes the now-empty group (default closeEmptyGroups) back to one group with the saved content intact. This is still unmodified, inherited default Workbench behavior that F030 did not patch or instrument, but it is no longer an unverified gap."
					},
					{
						"acceptance": "External save conflicts never overwrite silently",
						"result": "The S3 Browser E2E ('shows a Reload/Save As/Details save-conflict notification...') verifies that an external write racing a save surfaces the Plain FILE_MODIFIED_SINCE conflict notification with only Reload/Save As/Details actions (no Retry/Overwrite), leaves the model dirty and the on-disk file untouched, and restores clean state on Reload."
					},
					{
						"acceptance": "Dirty content survives window restart",
						"result": "The S5 Browser E2E ('restores an unsaved edit as a dirty editor after a simulated hot-exit reload...') verifies an unsaved edit is backed up, survives a simulated page reload against the persisted browser-mock backup store, and is restored as a dirty editor with the unsaved content on reopening the same workspace, then stops being restored once saved. This is a simulated reload of the mock persistence layer, not a real OS process kill/relaunch against the Rust backup store; the real kill-9-and-relaunch desktop variant is registered as E2E-003 and not yet executed."
					},
					{
						"acceptance": "Text, image and Markdown preview have safe fallbacks",
						"result": "Text editing/preview is exercised throughout every F020/F030 Browser E2E via Monaco. Image and Markdown safe fallbacks are now covered by two new S7 Browser E2E scenarios against a real binary fixture (a genuine 68-byte, CRC-verified PNG whose actual 0x00 bytes trip the real detectEncodingFromBuffer zero-byte scan, not a name/extension special case) and a real plain-text Markdown fixture: opening the PNG shows the real, unmodified upstream BinaryFileEditor placeholder (.monaco-editor-pane-placeholder, the exact upstream 'The file is not displayed in the text editor because it is either binary or uses an unsupported text encoding.' label and its 'Open Anyway' action) with no crash and no .monaco-editor underneath it; opening the Markdown file opens a normal Monaco text editor with a real absence check for any .webview surface and for a 'Markdown: Open Preview' palette command - genuine because Plain bundles no markdown-language-features/media-preview extension and no webview-service-override at all (confirmed against package.json/pnpm-lock.yaml) - and edits save normally through the same write path as every other file. Both paths remain unmodified, inherited default Workbench behavior that F030 did not patch or instrument, but they are no longer an unverified gap."
					}
				]
			}
		},
		{
			"id": "F040",
			"name": "Quick Open, workspace search and replace",
			"phase": 2,
			"status": "complete",
			"acceptance": [
				"File and content search honor ignore and include/exclude rules",
				"Results stream and cancel without blocking the UI",
				"Replace plan validates file versions before writes"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm test:e2e:browser"],
				"results": [
					"pnpm check passed in full on 2026-07-23: 38 frontend test files, 725 cases, 2300 frontend modules, 2134 bundle sources, 203 recorded migration-debt files and Rust 345/345.",
					"All Browser E2E scenarios passed 32/32 (31 carried over from S1-S4 plus 1 new F040 S5 scenario proving the search.exclude default excludes a node_modules directory from both Quick Open file search and Search view text search).",
					"The new S5 scenario additionally passed a focused repeat (--repeat-each=3, --retries=0) before the full-suite run, matching every earlier slice's own acceptance discipline."
				],
				"nativeScenarios": [
					"F040 introduces no changes to the native file open/read/save IPC surface: Quick Open's \"open match\" and every replace path's per-resource save both route through the exact same native workspace read and versioned-write (wv1/PLR1/PLW1) chain already validated at native desktop level during F020/F030's own acceptance (system folder picker, exact-byte CRUD, versioned save conflict handling). No native-desktop scenario was executed specifically for F040's own additions in this feature -- the bounded Rust file/text-search traversal, gitignore/exclude matching, streaming wake/poll/cancel protocol, and replace coordinator are all new Rust/TypeScript surface with no prior native evidence to inherit.",
					"F040's own desktop matrix -- real large-directory search performance and cancellation responsiveness, real multi-layer .gitignore/search.exclude effectiveness at real disk scale, Cmd+P open-from-large-tree, and a real version-conflict replace path with on-disk verification -- is registered as E2E-004 in docs/e2e-handover.md and handed over to Codex for native execution; it has not yet been executed."
				],
				"platformGaps": [
					"E2E-004 in docs/e2e-handover.md (real large-directory search performance/cancellation, real multi-layer .gitignore/search.exclude effectiveness, Cmd+P open-from-large-tree, and a real version-conflict replace path with on-disk verification) is handed over to Codex and not yet executed.",
					"Rust's regex engine (the `regex` crate) is linear-time only: no PCRE2, lookaround, or backreference support in search patterns, narrower than upstream's optional PCRE2 mode; an invalid or unsupported pattern surfaces as a Warning message rather than being silently accepted or crashing.",
					"Replace substitutes text literally and does not support `$1`-style capture-group backreferences in the replacement text -- Rust does not return capture groups to the frontend (a deliberate narrowing, stated alongside the regex limitation above, not hidden).",
					"Replacing across multiple files produces one independent undo entry per file rather than one shared cross-file undo group -- a deliberate isolation trade-off (F040 S4) so one file's save failure never blocks or rolls back another file's successful replace.",
					"`search.useIgnoreFiles`/`search.useGlobalIgnoreFiles`/`search.useParentIgnoreFiles` have no registered configuration schema; Rust's search domain hardcodes \"always honor .gitignore, never global/parent ignore files\" and reads no such flag from any request DTO, so registering a schema for settings neither side consumes was judged actively misleading rather than treated as a completeness gap (F040 S5 decision).",
					"Symlink exclusion from search results is proven by 3 Rust unit tests (F040 S2) and one browser-mock-level unit test (tests/unit/workspace-search-bridge.test.ts, \"never reports a symlink as a match and never traverses through one\"), not by a live Browser/Playwright scenario -- the shared installNativeIpcMock fixture helper used by every Browser E2E test does not expose symlink-node injection through its extraFiles parameter, and adding that plumbing was judged to risk the ~30 other scenarios sharing it for marginal signal beyond the existing unit-level proof against the identical mock code.",
					"\"Replace All in File\" (per-file granularity) shares its code path with \"Replace\" and \"Replace All\", both of which have dedicated Browser E2E coverage, but it has no dedicated Browser E2E scenario of its own (F040 S4 decision; unit tests and code review cover its shared logic).",
					"An over-8-MiB unopened file's replace failure has no dedicated Browser E2E of its own; the underlying 8 MiB read limit itself is already covered by earlier workspace read slices (F020).",
					"Root-relative search results are not paired with a rootId in the wire response (F040 S2 documented assumption): Plain currently only ever authorizes a single workspace root, so this has not caused an observable issue, but a future multi-root search would need this addressed first.",
					"Windows and Linux native packaging remains F120.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110."
				],
				"acceptanceResults": [
					{
						"acceptance": "File and content search honor ignore and include/exclude rules",
						"result": "F040 S2's Rust file search honors .gitignore with per-directory layering (deepest layer wins; negation and nested overrides both tested) and applies search.exclude/files.exclude via excludeGlobs, proven end-to-end by the S2 Browser E2E (a .gitignore-ignored sibling file never appears in Quick Open, its non-ignored sibling does and opens on Enter) and by S3 reusing the identical gitignore/exclude plumbing for text search. F040 S5 additionally: (a) registered a minimal, real search.exclude configuration schema default (`**/node_modules`, `**/bower_components`, `**/*.code-search`, matching upstream exactly) -- previously absent because Plain deliberately never imports the ~30-key search.contribution.js (F040 S1 decision, to avoid its Chat-context side effect), so every search had silently included node_modules unless a workspace's own .gitignore happened to cover it; (b) discovered and fixed that PlainSearchView's hand-built text-search query never applied any exclude pattern at all -- only Quick Open's file search did, via the pre-existing files.exclude default and upstream QueryBuilder plumbing. Text search now merges files.exclude/search.exclude the same way Quick Open does; both paths are proven together by the new Browser E2E (\"excludes a node_modules directory from both Quick Open file search and Search view text search by default\"). The `filePattern` include is a Rust-side case-insensitive subsequence prefilter, not upstream's includePattern glob syntax. Rust hardcodes \"always honor .gitignore\" and intentionally exposes no search.useIgnoreFiles/useGlobalIgnoreFiles/useParentIgnoreFiles schema, since neither side consumes such a flag (see platformGaps). Symlinks are never followed or reported as search results, proven by 3 Rust unit tests (S2) plus one browser-mock-level unit test; see platformGaps for why this specific behavior is not additionally exercised as a live Browser/Playwright scenario."
					},
					{
						"acceptance": "Results stream and cancel without blocking the UI",
						"result": "F040 S3 implemented the streaming text-search protocol (workspace_search_text_start/poll/cancel: an explicit wake signal plus front-end pull, with bounded queue backpressure) and proved it with Browser E2E covering grouped multi-file streaming render, click-to-editor navigation, a new query cancelling an in-flight one (asserted via exactly one cancel call and two start calls against the native IPC call log), a truncation banner past search.maxResults, and an invalid-regex Warning message -- none of it blocks the UI: typing, cancelling and re-querying all stay responsive even against the mock's artificial per-poll delay. File search (Quick Open, S2) is a bounded single request-response with a client-side 512-result cap and stale-response discarding while the user keeps typing. What Browser-mock evidence cannot prove is real elapsed-time search performance over a large real directory tree, or cancellation responsiveness against genuine Rust thread scheduling rather than a scripted mock delay; that real-disk-scale evidence is registered as E2E-004 in docs/e2e-handover.md (step 1) and has not yet been executed."
					},
					{
						"acceptance": "Replace plan validates file versions before writes",
						"result": "F040 S4's replace coordinator calls the exact same versioned wv1/PLR1/PLW1 save chain every manual edit already uses (ITextFileService.files.get(resource)?.save()), so a version conflict encountered during replace surfaces through the identical FILE_MODIFIED_SINCE path as any other save -- proven by the Browser E2E (\"keeps a version-conflicted file's replace visibly failed while a sibling file's replace still succeeds\"): the conflicted file's replace fails visibly (inline error, its match retained) while an unrelated sibling file's replace succeeds and is verified on disk. Per-match Replace, per-file Replace All in File, and global Replace All all route through the same coordinator; Replace and Replace All each have dedicated Browser E2E coverage (including one already-open editor kept in sync and one long-line absolute-column case), while Replace All in File shares that same code path without a dedicated scenario of its own (see platformGaps). Known, stated narrowing rather than a hidden gap: replace text is substituted literally with no $1-style capture-group backreferences; replacing across multiple files produces one independent undo entry per file rather than one shared cross-file undo group (a deliberate isolation trade-off so one file's failure never blocks or rolls back another); an over-8-MiB unopened file's replace fails visibly through the existing bulk-edit/working-copy read-limit path but has no dedicated Browser E2E of its own."
					}
				]
			}
		},
		{
			"id": "F050",
			"name": "VS Code color theme compatibility",
			"phase": 2,
			"status": "complete",
			"acceptance": [
				"Import local VSIX and unpacked theme directory",
				"Apply workbench colors and TextMate token colors",
				"JSONC, include and tmTheme fixtures pass",
				"Extension code is never executed"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm exec playwright test --retries=0"],
				"results": [
					"pnpm check passed in full on 2026-07-24: 43 frontend test files, 804 cases, 2305 frontend modules, 2139 bundle sources, 203 recorded migration-debt files (unchanged categories/SHA-256 since S3) and Rust 477/477 (up from 454 at the end of S3; +23 new: theme::selection's own read/write/round-trip/corruption/staging tests, ThemeLibrary::get_selection/set_selection, ThemeService::get_selection/set_selection, the two new dto types, and the one new THEME_SELECTION_INVALID closed-set entry).",
					"All Browser E2E scenarios passed 42/42 (39 carried over from S0-S3 plus 3 new S4 scenarios: booting directly on a persisted non-default theme, persisting the accepted theme's settingsId via theme_set_selection, and falling back to Dark Modern with zero crash -- plus the persisted-selection-clearing assertion added to the existing S3 removal scenario -- when the stored id matches no known theme). The 3 new S4 scenarios additionally passed a focused repeat (--repeat-each=3, --retries=0, 12/12) before the full-suite run, matching every earlier slice's own acceptance discipline.",
					"cargo fmt --check and cargo clippy --all-targets -- -D warnings both passed with zero diffs/warnings."
				],
				"nativeScenarios": [
					"F050 introduces no changes to the native workspace file open/read/save IPC surface, so no native-desktop scenario was executed specifically for it -- the Rust theme domain (src-tauri/src/theme/) is entirely new, self-contained surface (VSIX/directory unpack, manifest/theme-document validation, the import/list/read-resource/remove/get-selection/set-selection commands) with no prior native evidence to inherit.",
					"F050's own desktop matrix -- real system file-picker VSIX import, a real theme package fixture, cross-restart persistence of both the imported package library and the selected theme id, two hostile VSIX fixtures (zip-slip, no theme contributions) rejected with a desensitized toast and zero on-disk half-finished state, and removing the active imported theme falling back and clearing the persisted selection across a real restart -- is registered as E2E-005 in docs/e2e-handover.md and handed over to Codex for native execution; it has not yet been executed."
				],
				"platformGaps": [
					"E2E-005 in docs/e2e-handover.md (real system file-picker VSIX import, cross-restart persistence of the imported library and the selected theme id, hostile VSIX rejection with on-disk verification, and remove-then-restart selection clearing) is handed over to Codex and not yet executed.",
					"Imported theme packages never carry a contributes.themes[].id (S2's own manifest scope never captures a theme-level id at all): the registry and the persisted selection both use the resolved label (or a path-basename fallback) as the settingsId, matching upstream's own `settingsId = theme.id || label` fallback rule bit-for-bit rather than treating the missing field as a gap.",
					"Icon themes and product icon themes (contributes.iconThemes/productIconThemes) are parsed and retained verbatim in the stored package record but never validated, resolved, or applied to any UI surface -- deliberately deferred whole to F060, now in_progress.",
					"Plain does not follow the OS light/dark setting; the active theme only ever changes via explicit user selection (the Color Theme quick pick) or the one-time persisted-selection restore at boot -- a deliberate narrowing stated in the frozen research plan's exclusions, not an oversight.",
					"There is no Settings UI of any kind; the persisted selection is exclusively read/written through the theme_get_selection/theme_set_selection command pair, never through a workbench.colorTheme configuration key (see docs/research/2026-07-24-theme-compatibility.md's decision 3 for why: the in-memory configuration service would still read a stale workbench.colorTheme value with no writer keeping it in sync, so no such key is ever registered).",
					"installMultiRootNativeIpcMock (the separate multi-root Browser mock in tests/browser/workspace.spec.ts, independent of the installNativeIpcMock every theme scenario in this feature uses) still has no theme_list/theme_get_selection stubs -- a pre-existing S3 gap this slice did not need to close. Every multi-root scenario now additionally prints one theme_get_selection console.warn alongside S3's own theme_list one during bootstrap (both caught by the coordinator/picker's own best-effort fallback paths and asserted nowhere), rather than the assertions of any of those scenarios changing.",
					"A persisted selection.plain.json file is a plain, non-package sibling directly under the theme library root, so once a selection has ever been written, Rust's own theme_list enumeration counts it toward its existing skipped tally (the same tolerance record::list_theme_packages already extends to a leftover staging directory) -- a benign accounting fact, covered by a dedicated Rust test, not a functional gap.",
					"Windows and Linux native packaging remains F120.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110."
				],
				"acceptanceResults": [
					{
						"acceptance": "Import local VSIX and unpacked theme directory",
						"result": "F050 S1 built a bounded, capability-relative VSIX (zip) unpacker (zip crate, enclosed_name/is_symlink entry checks backstopped by Plain's own RelativePath::parse_wire since enclosed_name only catches `../` depth underflow and not absolute-path/drive-letter prefixes -- see the implementation-deviation record) with entry-count/per-entry/aggregate byte caps enforced against actually-read bytes, proven by 17 Rust tests covering zip-slip, symlink entries, oversized entries, oversized packages, zip bombs, corrupt/empty archives and clean round-trips; directory import (S1) shares the identical staging/limit machinery via an explicit non-recursive stack walk, covered by 4 more Rust tests. S2 layered manifest/theme-document validation on top (JSONC with exactly comments+trailing-commas, contributes.themes[] path/uiTheme validation, include-chain cycle/depth/file-count budgets, .tmTheme structural checks) with 19 further error-code tests. S3 exposed both paths as theme_import_vsix/theme_import_directory Tauri commands behind a real native file/directory picker, consumed end-to-end by the frontend (registerExtension/registerFileUrl against the imported package's own resource whitelist) and proven live by Browser E2E: importing a VSIX via the Command Palette shows a success toast and the new theme appears in the picker; a package already present from a prior session (via theme_list) reappears and applies for real at startup with its own distinctive editor.background. Real system file-picker exercise of both commands is registered as E2E-005, not yet executed."
					},
					{
						"acceptance": "Apply workbench colors and TextMate token colors",
						"result": "Plain never registers an extension-point handler at all (the upstream one is structurally unreachable under NullExtensionService, the root cause S0's own probe discovered and fixed): PlainThemeRegistry/PlainThemePicker construct ColorThemeData instances directly from each manifest's contributes.themes[] entry (built-in via getBuiltinExtensions(), imported via the S3 coordinator's local registerExtension/registerFileUrl call) and hand them straight to IWorkbenchThemeService.setColorTheme()'s own instanceof ColorThemeData fallback branch -- proven live by the S0 Browser E2E (`.monaco-workbench` carries real vs-dark/theme classes and a real `--vscode-editor-background` CSS variable instead of the previously-undiscovered unthemed placeholder) and by every subsequent slice's own scenarios reusing the identical apply path for imported themes. Applying colors requires a real IExtensionResourceLoaderService, which no existing override package provided (S0's own PlainExtensionResourceLoaderService closes that gap); token colors flow through the same ColorThemeData#ensureLoaded resource read. `.tmTheme` files are only structurally validated (S2), never parsed into token rules by this Rust domain -- token color application through .tmTheme (a tokenColors string reference) is exercised end-to-end by the S3 whitelist/registerFileUrl path but the actual TextMate scope-to-color resolution is entirely upstream Monaco/TextMate machinery this feature does not modify."
					},
					{
						"acceptance": "JSONC, include and tmTheme fixtures pass",
						"result": "S2's manifest.rs parses package.json and every theme document with jsonc-parser configured for exactly `{allow_comments: true, allow_trailing_commas: true}` and every other option explicitly false (compile-time const-asserted, not just tested) -- deliberately narrower than upstream's looser JSON5-adjacent dialect. include chains are walked with an explicit visited-set (cycle rejection) and a depth cap of 32, plus a whole-package file-count budget of 64 shared across every contributes.themes[] entry's own chain -- both caps independent by design (see the frozen research doc's decision 2). `.tmTheme` files get a structural-only check (`<?xml` header + `<plist` + `<dict` presence) rather than a full plist parse, a deliberate scope line (the bytes are never executed or otherwise consumed by this Rust domain -- the real XML-to-token-rule parsing is Monaco/TextMate's own job downstream of the whitelist read). All of this is proven by 20 Rust theme_json tests plus 19 manifest tests covering the exact error-code closed set (23 codes through S3, 24 after S4's own THEME_SELECTION_INVALID addition) -- zip-slip, symlink entries, oversized/mis-shaped documents, include cycles/depth/file-count overruns, and out-of-manifest path references are all covered by dedicated, independently-constructed fixtures (no binary zip/plist files committed to the repo; every fixture is built in-process)."
					},
					{
						"acceptance": "Extension code is never executed",
						"result": "The theme domain's manifest parser (manifest.rs) only ever checks whether main/browser/activationEvents are present (recording a single containsCode boolean) and never reads, evaluates, or otherwise consumes their string values; no Extension Host of any kind (local, worker, WASM, remote or sidecar) exists anywhere in Plain, and registerExtension is called with no second argument (the official, purely declarative form) both for the S0 built-in enumeration path and the S3 imported-package path -- confirmed by real-source inspection (not assumption) that its own 'add this extension to IExtensionService' branch is an unconditional no-op under NullExtensionService.canAddExtension(), so an imported manifest never becomes reachable through any extension-point/activation surface even in principle. contributes.iconThemes/productIconThemes are parsed and retained verbatim but never resolved into any UI-consuming code path (F060's own scope). Confirmed by the full acceptance suite's own zero-Extension-Host architecture guard (scripts/plain/check-boundaries.mjs's fixed monaco-vscode-extensions-service-override import ban) staying green across every S0-S4 slice."
					}
				]
			}
		},
		{
			"id": "F060",
			"name": "VS Code file and product icon themes",
			"phase": 2,
			"status": "complete",
			"acceptance": [
				"File, folder and expanded icon precedence matches fixtures",
				"Font and image resources stay inside the imported theme",
				"Unsafe SVG or missing icon IDs fall back safely"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm exec playwright test --retries=0"],
				"results": [
					"pnpm check passed in full on 2026-07-24: 43 frontend test files, 886 cases (up from 804 at the end of S1/before S2's own +35, then +47 across this S3 slice: theme-codec's new selection-request/response and icon-contribution-array coverage, plain-theme-picker's new persist/apply-persisted describe blocks for both icon axes, plain-theme-import-coordinator's new store-axis and removal-fallback coverage), 2305 frontend modules, 2139 bundle sources, 203 recorded migration-debt files (unchanged categories/SHA-256 since S2) and Rust 547/547 (up from 538 at the end of S1; +9 new: selection.rs's per-field/multi-axis/backward-compatibility tests, library.rs's file-icon/product-icon axis coverage, dto/tests.rs's icon-contribution-summary and three-axis selection tests).",
					"All Browser E2E scenarios passed 50/50 (45 carried over from S1/S2 plus 5 new S3 scenarios: discovering and applying an imported package's file icon theme and product icon theme in their quick picks, booting directly on persisted color/file-icon/product-icon selections including one resolved against an imported package, booting with the None/Default sentinels honored (not silently falling back to vs-minimal), persisting the accepted file icon and product icon theme selections via theme_set_selection including the None/Default sentinels, and falling back both icon axes to their own defaults with zero crash when their persisted ids match nothing). The 5 new scenarios additionally passed a focused repeat (--repeat-each=3, --retries=0, 15/15) before the full-suite run, matching every earlier slice's own acceptance discipline.",
					"cargo fmt --check and cargo clippy --all-targets -- -D warnings both passed with zero diffs/warnings."
				],
				"nativeScenarios": [
					"F060 introduces no changes to the native workspace file open/read/save IPC surface; the icon-theme validation pipeline (src-tauri/src/theme/{icon_theme_json,product_icon_theme_json,svg_sanitize,font_check}.rs, S1) and the selection persistence extension (src-tauri/src/theme/selection.rs's three-axis, per-field update, S3) are new/extended surface within the same self-contained theme domain F050 already established, with no native-desktop evidence of their own to inherit.",
					"F060's own desktop matrix -- a real VSIX carrying a color theme, a file icon theme and a product icon theme together imported through the real system file picker, all three axes rendering for real in a real WKWebView (SVG background images, a real font glyph, a real editor background color), cross-restart persistence of all three selections, and a malicious-SVG package rejected with a desensitized toast and zero on-disk half-finished state -- is registered as E2E-006 in docs/e2e-handover.md and handed over to Codex for native execution; it has not yet been executed."
				],
				"platformGaps": [
					"E2E-006 in docs/e2e-handover.md (real VSIX with all three theme axes imported via the real system file picker, real SVG/font rendering in a real WKWebView, cross-restart persistence of all three selections, and a malicious-SVG package rejected) is handed over to Codex and not yet executed.",
					"embedded-opentype (EOT) fonts are unconditionally rejected: the format has no fixed magic-byte signature the closed font_check.rs validator can recognize, so any font declaring it fails THEME_FONT_INVALID regardless of the actual bytes -- a known, stated narrowing (the format is effectively extinct outside legacy IE), not a hidden gap.",
					"data: URIs are rejected outright in every SVG/font resource path with no MIME-subtype allowlist (e.g. a small inline data:image/png;base64 fallback some real-world extensions use would be rejected) -- judged more auditable than adding a MIME allowlist exception, and no F060 fixture needs it.",
					"None (file icon theme) and Default (product icon theme) are persisted as two reserved, non-empty sentinel strings (plain:no-file-icon-theme / plain:default-product-icon-theme) rather than the literal empty-string settingsId upstream's own singletons use, because theme::selection::validate_theme_selection_id rejects an empty id outright on every axis; this is an internal persistence-encoding detail invisible to the user (the real upstream singleton instance is still what gets applied) and is not exercised by any on-disk-content assertion in the Browser E2E suite, only by its real applied effect.",
					"installMultiRootNativeIpcMock (the separate multi-root Browser mock in tests/browser/workspace.spec.ts) still has no theme_list/theme_get_selection stubs, a pre-existing gap from F050 S3 this feature did not need to close; every multi-root scenario now prints one additional theme_get_selection console.warn per icon axis alongside F050's own existing one during bootstrap (three total instead of one), all caught by the coordinator/picker's own best-effort fallback paths and asserted nowhere.",
					"theme_set_selection's per-field update request type (Option<Option<String>> per axis, the 'double option' pattern) has no dedicated Rust fuzz/property test beyond the hostile-mutation-style unit tests already covering every leave/clear/set/no-op/multi-axis/rejection combination by hand; judged sufficient given the closed, small (3-axis) shape.",
					"Plain does not follow the OS light/dark setting on any of the three axes; the active selection only ever changes via explicit user action or the one-time persisted-selection restore at boot, matching F050's own stated narrowing.",
					"Windows and Linux native packaging remains F120.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110."
				],
				"acceptanceResults": [
					{
						"acceptance": "File, folder and expanded icon precedence matches fixtures",
						"result": "F060 S1's icon_theme_json.rs parses iconDefinitions/fonts/file/folder/folderExpanded/rootFolder*/fileExtensions/fileNames/folderNames/languageIds/light/highContrast/hidesExplorerArrows with the same upstream-matching leniency contributes.themes[] documents get (a malformed individual definition or font entry is skipped, never fails the whole document), proven by 14 Rust tests. S2 activated the built-in vs-minimal contribution end to end: a real browser probe confirmed README.md/src/the workspace root row each resolve to a real, non-empty background-image (file/folder/rootFolder precedence all exercised), and the File Icon Theme quick pick lists exactly None and Minimal (Visual Studio Code) with None disabling icons and switching back re-enabling them. S3 closed the S2-documented DTO projection gap (ThemePackageSummary now carries iconThemes/productIconThemes, see dto.rs's IconThemeContributionSummary) and proved an imported package's own file icon theme resolves and renders through the identical real-DOM background-image path, plus persists and reapplies across a simulated restart -- Browser E2E now covers both the built-in and the imported-package cases together."
					},
					{
						"acceptance": "Font and image resources stay inside the imported theme",
						"result": "S1's shared, package-wide resources whitelist (the same BTreeSet<String> accumulator contributes.themes[] already uses) is extended to every iconPath and fonts[].src[].path an icon/product icon document references; only a whitelisted relative path is ever registered as a blob URL or served by theme_read_resource, proven by 4 Rust import.rs integration tests (icon-only package, product-icon-only package, and two all-or-nothing rejection cases) plus the icon_theme_json/product_icon_theme_json unit tests' own resource-collection assertions. S3's Browser E2E fixture carries a real SVG and a real (fake-content but correctly-shaped) WOFF font registered this way, and confirms both actually render (a non-none background-image; the product icon theme document loads without upstream rejecting it) -- proving the whitelist-then-registerFileUrl path is exercised live, not just at the Rust layer. MAX_ICON_ASSOCIATIONS=4096 bounds the total icon/association count per document independently of the shared resources budget."
					},
					{
						"acceptance": "Unsafe SVG or missing icon IDs fall back safely",
						"result": "svg_sanitize.rs rejects <script>, on*= event attributes, <foreignObject>, external/javascript:/data: URLs and DOCTYPE/ENTITY declarations in a single lower-cased scan (13 Rust tests), applied to every iconPath and any fonts[].src[] declaring format:\"svg\"; font_check.rs checks the remaining five formats against closed magic-byte signatures (8 Rust tests) and one Rust import.rs test proves a malicious SVG makes the whole package import fail atomically with zero residue, mirroring the identical zip-slip/oversized-package precedent. A file icon theme's own iconDefinitions structurally cannot reference an association Rust rejected (the association simply never enters the validated document), so 'missing icon ID' safety is a structural guarantee from S1's own validation rather than a runtime fallback branch; S3 additionally proved the two icon selection axes fall back safely and with zero crash when a persisted id matches nothing (own-axis warn, clear, and the already-applied vs-minimal/Default bootstrap default stands), independently of the color axis and of each other."
					}
				]
			}
		},
		{
			"id": "F070",
			"name": "Rust PTY terminal",
			"phase": 3,
			"status": "complete",
			"acceptance": [
				"Interactive shell supports input, resize, search and exit",
				"Multiple tabs and splits clean up on window close",
				"Backpressure and output fragmentation tests pass"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm test:e2e:browser"],
				"results": [
					"pnpm check passed in full on 2026-07-25: cargo fmt --check and cargo clippy --all-targets -- -D warnings both zero-diff/zero-warning; cargo test 637/637 (up from 634 at the end of the multi-tab/split/scrollback slice -- +3 new: vt/tests.rs's escape-sequence-split-across-two-reads-at-every-one-of-17-boundaries test, its one-byte-at-a-time complement, and a multi-byte UTF-8 character split across two feed() calls, closing this slice's own fragmentation-evidence gap); pnpm exec vitest run 50 test files, 1024 cases (unchanged -- this slice touched no app/** source or test file); pnpm exec vite build 2150 bundle sources; node scripts/plain/check-boundaries.mjs 44 app sources / 89 Rust sources / 15 pinned runtime dependencies (all unchanged); node scripts/plain/check-bundle.mjs 2150 sources, 203 tracked transitional debt sources (categories and debtSourceSha256 unchanged since the previous slice).",
					"The 3 new Rust tests were additionally run standalone (cargo test terminal::vt::, 22/22 in that module) before being folded into the full suite above, and cargo fmt --check / cargo clippy --all-targets -- -D warnings were re-run scoped to the terminal domain first and passed with zero diffs/warnings.",
					"The one new Browser E2E scenario (a burst of 500 terminal output lines pushed with no yield in between, mirroring a real pty reader flooding output at OS speed) passed a focused repeat (--repeat-each=3, --retries=0, 3/3) before the full-suite run; the full pnpm exec playwright test --retries=0 suite then passed 63/63 (up from 62/62 at the end of the previous slice, the +1 being this new scenario), zero regressions."
				],
				"nativeScenarios": [
					"F070's own real-desktop matrix -- a real interactive shell (input/output), resize against the WKWebView's actual layout pipeline, multiple tabs and a split each with an independently-alive real process, an untrusted workspace's real trust confirm/decline copy, and high-throughput commands (yes / cat over a large file) observed not to stall the UI or leave orphaned processes after window close -- is registered as E2E-007 in docs/e2e-handover.md and handed to Codex for native execution; it has not yet been executed.",
					"Every other acceptance-relevant mechanism this feature needs -- the byte-level PTY<->VT flow-control gate's real pause/resume under load, the VT<->frontend single-frame emission credit, and VT-level parsing correctness across an escape sequence or a multi-byte UTF-8 character split at an arbitrary feed() boundary -- is proven directly against real subprocesses and the real libghostty-vt parser in src-tauri/src/terminal/{flow.rs,vt/tests.rs,service/tests.rs}, not only through Browser mock or native-desktop evidence. The Browser E2E suite additionally proves the frontend's own ack-driven backpressure path end to end (a 500-line burst mock, pushed with no yield in between) coalesces into a small, bounded number of real frame/ack round trips without dropping content or leaving the page unresponsive to further keyboard input -- but this remains a Chromium/mock-transport proof, not a WKWebView/real-process one, which is exactly what E2E-007 exists to close."
				],
				"platformGaps": [
					"E2E-007 in docs/e2e-handover.md (the real desktop terminal matrix: real shell input/output, resize, multi-tab/split process lifecycle, untrusted-workspace trust copy, and high-throughput commands under real OS scheduling) is handed to Codex and not yet executed.",
					"In-terminal find (half of the first acceptance's 'search') does not exist on either platform in the current implementation: plain-terminal-input.ts's own key encoder forwards Ctrl-modified keys (including Ctrl+F) as real terminal control sequences on Windows/Linux, and WKWebView provides no built-in in-page find the way a browser tab does on macOS -- discovered and recorded during the multi-tab/split/scrollback slice; only 'terminal text can be natively selected' is proven (Browser E2E), not 'terminal text can be found'.",
					"Scrollback history rows carry no per-cell color -- the wire protocol's ScrollbackCell only ever carries bold/italic/underline-style booleans, never a resolved or palette-indexed color -- so history text always renders in the pane's current default foreground/background rather than its original color; a fix needs vt.rs to expose resolved colors off GridRef the way CellIteration already does for the live viewport, judged out of this feature's scope.",
					"Splits are capped at 2 panes per tab and are not recursively subdividable; a pane viewing scrollback history does not automatically jump back to live on new output (only Shift+End or scrolling to the bottom does); both are deliberate, recorded MVP narrowings, not defects.",
					"No shell integration (OSC 133/633) and no session persistence/reconnect across a restart; Windows/Linux native packaging remains F120 -- all stated up front in docs/research/2026-07-24-libghostty-terminal.md and unchanged by this closing slice.",
					"libghostty-vt and its Rust binding are both pre-1.0 (breaking upstream API changes are possible) and the build depends on Zig 0.15.x plus a pinned, gitignored local Ghostty source checkout (.ghostty-vendor/) for offline reproducibility -- a real, disclosed supply-chain narrowing, not a hidden one.",
					"Rendering stays DOM (the wterm-style MVP): the WebGL/beamterm upgrade path docs/research/2026-07-24-libghostty-terminal.md left open was evaluated against this slice's own real throughput evidence (see acceptanceResults[2]'s result) and judged unnecessary for now -- the single-frame emission credit already bounds the DOM renderer's paint rate to the display's own refresh rate regardless of how fast the pty itself produces bytes, and rows are painted as contiguous same-style <span> runs rather than one element per cell, bounding real DOM node count well below cols x rows. There is no real performance deficit driving an upgrade today; DOM MVP is judged sufficient and the upgrade is deferred rather than pursued speculatively.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110."
				],
				"acceptanceResults": [
					{
						"acceptance": "Interactive shell supports input, resize, search and exit",
						"result": "Input: terminal_input_text (IME commit/paste) and terminal_input_key (structured key events encoded through libghostty-vt's own encoder) both write real bytes to the pty, proven against a real cat/cat -v subprocess (service/tests.rs's echo_round_trip_through_cat, input_key_writes_the_libghostty_encoded_bytes_to_the_pty) and the IME-specific Browser E2E 'sends only the final IME-committed text, never intermediate composition state'. Resize: terminal_resize propagates to the real pty (resize_takes_effect_and_is_observed_by_stty_size feeds a live stty size report back through the VT mirror) and to the frontend's own real layoutBody-driven measurement (Browser E2E 'resizes the terminal grid to match the panel's real size'). Exit: both a normal exit code and a killed/signaled exit are captured (a_normal_exit_code_is_captured, kill_immediate_tears_down_synchronously_and_captures_a_signal_exit) and reachable from the view via 'Plain: Kill Terminal' (Browser E2E). Search: a code-level re-audit during the multi-tab/split/scrollback slice found that neither platform's real product target supports in-terminal find under the current implementation (Ctrl+F is intercepted and forwarded as a real terminal control sequence on Windows/Linux; WKWebView has no built-in in-page find on macOS) -- only the other half of this acceptance's intent, that terminal text is discoverable/selectable, is proven (Browser E2E 'terminal output is real, selectable DOM text'); recorded as a known, deliberate gap (see platformGaps) rather than silently claimed as met."
					},
					{
						"acceptance": "Multiple tabs and splits clean up on window close",
						"result": "TerminalService has tracked sessions per window_label in a HashMap<String, HashMap<TerminalSessionId, Arc<TerminalSession>>> since the S1 slice, and close_window_kills_and_removes_every_session_for_that_window (service/tests.rs) proves close_window concurrently kills and joins every thread of every session for a window, synchronously, with no session left behind (a killed session's terminal_ack subsequently reports TERMINAL_SESSION_NOT_FOUND). The frontend's own multi-tab/split lifecycle (TerminalTabsModel, TerminalPaneController) is proven end to end by 4 Browser E2E scenarios: two tabs with independently-alive sessions where closing one leaves the other running, 'Plain: Kill Terminal' closing only the active tab, a split producing two independently-sized-and-sessioned panes, and a reload while multiple tabs (including a split) are open tearing down with zero pageerror. The real desktop process-tree confirmation of 'clean up on window close' -- shell-level ps verification that no orphaned child process survives a real Cmd+Q -- is registered as E2E-007 step 6 and not yet executed."
					},
					{
						"acceptance": "Backpressure and output fragmentation tests pass",
						"result": "Byte-level PTY->VT backpressure: flow.rs's own unit tests cover the high/low water mark hysteresis in isolation (stays_clear_below_the_high_water_mark, reaching_the_high_water_mark_pauses_until_the_low_water_mark, wait_until_clear_to_read_blocks_while_paused_and_unblocks_on_ack, cancel_wakes_a_paused_waiter_and_reports_cancelled, an_over_generous_ack_saturates_at_zero_rather_than_underflowing); service/tests.rs's output_well_beyond_the_high_water_mark_still_arrives_completely_and_in_order re-proves the same gate against a real subprocess generating ~112 KiB (2,200 lines) -- well beyond the 100,000-byte high water mark -- while continuously acking as a real frontend would, observes the reader was in fact paused, and asserts the final viewport content is byte-perfect with every frame's sequence number strictly consecutive (no gaps, no reordering, no silent drops). VT->frontend frame-level backpressure: FrameEmitGate's single-outstanding-frame credit is proven in pure unit tests (frame_emit_gate_single_credit_and_tolerant_ack_semantics, frame_emit_gate_does_not_spend_credit_or_a_sequence_number_on_a_clean_snapshot) and against a real session (frame_emission_is_gated_until_the_previous_frame_is_acked: a second frame does not appear until the first is acked, and the coalesced content fed in the meantime is exactly what the freed frame reports). Output fragmentation (this slice's own gap-fill): vt/tests.rs's feeding_an_escape_sequence_split_across_two_reads_parses_identically_regardless_of_the_split_point feeds the same SGR-plus-newline byte stream split into exactly two feed() calls at every one of its 17 possible byte offsets and asserts a byte-for-byte-identical DirtyFrame to a single whole feed() at every one; feeding_the_same_bytes_one_byte_at_a_time_produces_an_identical_dirty_frame_to_one_call repeats this at the worst-case one-byte-per-call granularity; feeding_a_multi_byte_utf8_character_split_across_two_feed_calls_still_decodes_correctly proves a 2-byte UTF-8 character (é) split exactly between its two bytes across two feed() calls still decodes to the complete character rather than mojibake or a dropped cell. A Browser E2E frontend-level complement ('a high-throughput burst of output is coalesced by the single-frame credit gate instead of stalling the UI or dropping content') pushes 500 lines with no yield in between through the same mocked single-credit gate the Rust FrameEmitGate mirrors, and proves the burst's last line still reaches the screen, the number of real terminal_ack round trips stays under 10 regardless of the 500 lines pushed (proving genuine coalescing, not just speed), and the page accepts further keyboard input immediately afterward. WebGL/beamterm upgrade judgment (the deferred decision point docs/research/2026-07-24-libghostty-terminal.md left open): not upgraded. The architecture's own single-frame emission credit already caps the DOM renderer's paint rate at whatever the display's refresh rate allows, independent of how fast the pty itself produces bytes -- a slow consumer only delays the next ack, which throttles frame production upstream rather than ever asking the DOM to paint faster than it can; the renderer additionally groups each row's cells into contiguous same-style <span> runs rather than one element per cell, bounding real DOM node count well below cols x rows. Combined with the real ~112 KiB/2,200-line throughput test and the 500-line Browser burst test above showing no stall and no dropped content under load, there is no real performance deficit driving a WebGL/beamterm upgrade today; DOM MVP is judged sufficient and the upgrade is deferred rather than pursued speculatively."
					}
				]
			}
		},
		{
			"id": "F080",
			"name": "Core Git workflow",
			"phase": 3,
			"status": "complete",
			"acceptance": [
				"Status, diff, hunk stage, commit, fetch, pull and push work",
				"Git output parsing is independent of locale",
				"Untrusted workspaces never start Git or configured helper processes",
				"Background reads cannot execute hooks, fsmonitor, external diff, textconv or credential helpers",
				"Destructive actions preview impact and require confirmation"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm test:e2e:browser"],
				"results": [
					"pnpm check passed in full on 2026-07-26 (exit code 0, captured with an explicit `echo EXIT=$?` rather than through a pipe -- an earlier run in this session reported a false success because `pnpm check | tail` yields tail's exit code, which masked a real format:check failure): cargo fmt --check and cargo clippy --all-targets -- -D warnings both zero-diff/zero-warning; cargo test 783/783 (up from 715 at the end of S2: +41 in S3's stage/commit/discard domain, +27 in S4's network domain and its askpass/locale fixtures); pnpm exec vitest run 57 test files, 1217 cases (up from 50/1024 at the end of F070); node scripts/plain/check-boundaries.mjs 54 app sources / 112 Rust sources / 16 pinned runtime dependencies; node scripts/plain/check-bundle.mjs 2162 sources, 203 tracked transitional debt sources.",
					"Debt-surface zero drift held across all five F080 slices: debtSourceCount 203, debtSourceSha256 03208818d455dabc60ddc6d2185e54bb8427303f7bd0ac111ac4d29d43c02b8b and categoryCounts {chatAgent:121, mcp:16, authAccount:11, syncEditSessions:8, extensionRuntime:47} are byte-identical to the F070 baseline; only sourceCount moved (2150 -> 2162) as this feature's own self-built SCM/network modules were added. Verified personally after rebuilding dist -- check:bundle analyses the built bundle, so running it against a cleaned tree reports every debt category as 0 and the empty-string SHA e3b0c442..., which is an artifact of the missing build and not a real drift.",
					"pnpm test:e2e:browser 80/80 passed in 2.6m (exit code 0), up from 63/63 at the end of F070. An earlier run of the same suite aborted after 51 passes with the remainder unrun; the cause was mine, not the code's -- prettier --write rewrote tests/browser/workspace.spec.ts while that run was still in flight. The re-run on a stable tree was clean.",
					"The two safety contrasts at the core of this feature were each run standalone rather than only inside the full gate, and each has a control case proving its fixture is real: cargo test hook 7/7 (recounted after the post-review fixes below; the earlier-recorded 6/6 predated F080 S4's own pre-push hook test being added to this same name filter and was never updated) -- of which these 5 are the git ones (the other two are unrelated workspace::directory_copy hook tests that share the name filter): background_read_disables_a_malicious_hooks_path_hook vs write_mode_allows_the_repositorys_own_hooks_path_hook_to_fire on one shared core.hooksPath fixture, commit_runs_the_repositorys_own_pre_commit_hook and a_failing_pre_commit_hook_blocks_the_commit, plus network_mode_allows_the_repositorys_own_pre_push_hook_to_fire (S4's own hooks-still-fire-for-Network-mode contrast, previously omitted from this count); cargo test askpass 3/3 (control_hostile_core_askpass_fires_without_a_git_askpass_override proves a malicious repository's core.askPass genuinely runs when GIT_ASKPASS is unset, then network_mode_git_askpass_blocks_a_hostile_core_askpass_when_credential_helper_is_incomplete proves pinning it restores fail-closed, and network_mode_never_invokes_askpass_when_credential_helper_fully_supplies_credentials proves that pinning does not defeat a working credential helper). No test was skipped for want of a git binary."
				],
				"nativeScenarios": [
					"All fetch/pull/push coverage runs against a real local `git init --bare` directory standing in for the remote, so the porcelain is exercised end to end with zero real network access: push_uploads_local_commits_to_a_local_bare_remote, pull_fast_forwards_the_local_branch, fetch_updates_the_remote_tracking_branch_from_a_local_bare_remote, plain_push_is_rejected_when_the_remote_has_diverged, force_push_with_a_valid_lease_rewrites_the_remote and stale_force_push_is_still_rejected_by_force_with_lease -- the last of these is what proves --force-with-lease is actually protecting commits someone else pushed, rather than being a cosmetic flag.",
					"Real desktop scenarios that cannot be reached from either the Browser suite or a local bare remote -- Keychain-backed credentials, a real ssh-agent handshake, a real authenticated push to a real remote, and the wording shown when authentication fails -- are registered as E2E-008 in docs/e2e-handover.md for native acceptance."
				],
				"platformGaps": [
					"This feature deliberately ships no credential-entry UI of its own. A missing or incomplete credential fails cleanly and quickly (GIT_TERMINAL_PROMPT=0 with GIT_ASKPASS pinned to a rejecting program) instead of hanging a child process forever on a TTY that a GUI application does not have. Repositories whose credentials are already resolvable through a configured credential helper or ssh-agent work normally.",
					"fetch/pull/push run the plain, unscoped git command (no remote/refspec arguments); @{upstream} is used only as a precondition check (pull/push refuse before spawning when the current branch has no upstream at all, matching what a bare command would refuse anyway) and by pull's own implicit merge step, not as a guarantee on what fetch/push themselves touch. A plain fetch updates every remote-tracking ref the remote's own configured refspec covers, not only the current branch's upstream (verified empirically against a multi-branch remote); a plain push only happens to touch just the current branch because of today's default push.default=simple, not because this code enforces that scope (verified empirically that push.default=matching uploads every matching local branch instead). There is no UI for choosing a different remote/branch/push.default than the repository's own configuration, or for setting an upstream, consistent with the research document's \"never a general-purpose git_run\" constraint.",
					"The ahead/behind counts shown in the confirmation preview reflect the last known remote-tracking state and can be stale before a fetch. This is inherent to --force-with-lease semantics rather than a defect.",
					"Hunk-level staging is reachable only through the `Plain: Stage First Change in Active File (Hunk)` command and only for hunk index 0; there is no gutter or diff-editor \"Stage Selected Ranges\" affordance yet.",
					"The locale-independence acceptance could not be proven the obvious way: this host's Apple Git 2.50.1 is not compiled with gettext (verified via --build-options and by forcing LANG=fr_FR.UTF-8), so asserting that real command output stays English would pass vacuously and prove nothing. It is instead proven structurally -- LANG/LC_ALL are unconditionally overridden for all three GitExecModes even when the test process itself is given hostile LANG/LC_ALL values, with a real subprocess echoing its own $LANG/$LC_ALL back through a hooks fixture to confirm the override reaches the child. A host with a gettext-enabled git would allow the stronger direct assertion."
				],
				"acceptanceResults": [
					{
						"acceptance": "Status, diff, hunk stage, commit, fetch, pull and push work",
						"result": "covered by the Rust domain tests above plus Browser scenarios for staging/unstaging (single and bulk), commit with Amend, discard, the four network operations and hunk staging through Monaco's own diff engine."
					},
					{
						"acceptance": "Git output parsing is independent of locale",
						"result": "two independent NUL/porcelain-v2 tokenizers parse byte records rather than localized text, and the LANG/LC_ALL override is proven unconditional as described under platformGaps."
					},
					{
						"acceptance": "Untrusted workspaces never start Git or configured helper processes",
						"result": "structurally guaranteed rather than directly observed by these two tests: every command in the domain resolves its repository through repo::resolve_repo_toplevel, which calls TrustService::require_trusted before ever returning a repo directory to spawn against, so no run_git call site in an untrusted window is reachable at all. preview_rejects_when_workspace_is_not_trusted and fetch_rejects_when_workspace_is_not_trusted assert the specific, narrower thing they actually observe: the call returns the WORKSPACE_NOT_TRUSTED error code and not some other outcome -- neither test asserts anything about whether a process was or was not spawned."
					},
					{
						"acceptance": "Background reads cannot execute hooks, fsmonitor, external diff, textconv or credential helpers",
						"result": "hostile fixtures with control cases cover core.hooksPath, core.fsmonitor, diff.external and textconv, and the askpass trio above closes the credential-helper dimension. GitExecMode::Write and GitExecMode::Network deliberately do respect the repository's own hooks -- a user-initiated write or push should honour repo configuration per ADR 0003 -- and the paired fixtures assert both directions on one shared fixture so the distinction cannot silently collapse."
					},
					{
						"acceptance": "Destructive actions preview impact and require confirmation",
						"result": "gitDiscardPaths and gitFetch/gitPull/gitPush each have exactly one production call site whose method body is locked by an AST contract (validateGitDiscardConfirmationBoundary, validateGitNetworkConfirmationBoundary) to await the confirmation resolver and return unless the decision is exactly \"confirmed\"; the resolver modules are DOM-free, have no skip-the-dialog branch, and cannot call a bridge themselves. Force push is a distinct confirmation kind with its own wording and button rather than a boolean on the push path, and a separate contract fails the build on a bare --force literal anywhere in the git Rust domain (every .rs file under src-tauri/src/git/, not only network.rs -- broadened post-review from an earlier, narrower network.rs-only scan; confirmed empirically that no other file in the domain legitimately needs the literal quoted string \"--force\", so the wider scan costs nothing). The two contracts carry 26 unit tests (13 each), of which 22 feed deliberately hostile sources -- a second call site in a new file, a bypass method inside the same view class, a skip-the-dialog branch, a resolver that calls a bridge itself -- and assert the contract actually rejects them."
					}
				]
			}
		},
		{
			"id": "F090",
			"name": "Native Git history and blame tools",
			"phase": 3,
			"status": "complete",
			"acceptance": [
				"Inline and file blame, hover and age heatmap work",
				"File and line history, revision navigation and compare work",
				"Graph, refs, stash and worktree workflows pass fixtures",
				"No GitLens Plus, account, cloud, brand or AI code is present"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm test:e2e:browser"],
				"results": [
					"pnpm check passed in full on 2026-07-28 (exit code 0, captured via redirection plus an explicit `echo EXIT=$?` rather than through a pipe, per this session's own F080-era lesson): cargo fmt --check and cargo clippy --all-targets -- -D warnings both zero-diff/zero-warning; cargo test 988/988 (up from 783 at the end of F080: +205 across this feature's six slices -- blame, file/line history, show-commit, graph+refs, stash, and worktree, each with real git fixtures, not mocked plumbing); pnpm exec vitest run 67 test files, 1421 cases (up from 57/1217 at the end of F080); node scripts/plain/check-boundaries.mjs 69 app sources / 124 Rust sources / 17 pinned runtime dependencies; node scripts/plain/check-bundle.mjs 2191 sources, 203 tracked transitional debt sources.",
					"Debt-surface zero drift held across every one of this feature's slices, S6 included: debtSourceCount 203, debtSourceSha256 03208818d455dabc60ddc6d2185e54bb8427303f7bd0ac111ac4d29d43c02b8b and categoryCounts {chatAgent:121, mcp:16, authAccount:11, syncEditSessions:8, extensionRuntime:47} are byte-identical to the F070/F080 baseline; only sourceCount moved (2162 -> 2191), and only from this feature's own new self-built modules plus the one new pinned dependency (multi-diff-editor-service-override, audited clean of chat/AI coupling in the frozen research doc) -- S6 itself added zero new app sources (it only edited four existing files), so 2191/203/69-app-sources are unchanged from the S5 baseline.",
					"pnpm test:e2e:browser 86/86 passed in 3.0m (exit code 0), up from 80/80 at the end of F080 -- the +6 is this closing slice's own new coverage (see below); the pre-existing 80 all continued to pass with zero regressions. The one pre-registered flaky case unrelated to this feature (`shows missing-parent create failures for both workspace roots`, an F030-era Explorer race, spawned off as an independent follow-up during F090 S3) did not reproduce during this session's runs.",
					"S6's own central finding, stated plainly rather than folded into routine housekeeping: this feature's five preceding slices (S0-S5) each individually and explicitly deferred Browser E2E coverage for its own new view, reasoning (accurately, in isolation) that Rust fixtures plus frontend unit tests were sufficient and that `workspace.spec.ts`'s existing size made a new scenario disproportionate. The cumulative effect, undetected until this slice actually opened each of the seven new views (inline blame + hover, file/line history, commit-detail multi-diff, graph, refs, stash, worktree) through Playwright for the first time, was that two of them had never actually worked in a real Workbench at all -- not 'untested', but genuinely broken, silently, since the slice that introduced them.",
					"Bug 1 (discovered writing the History-view test, `plain-git-history-view.ts`): `PlainGitHistoryView`, added in S1, never had a single DI decorator declared on it -- not `PlainGitStashView`'s S4-era 'declared some indices but not all' near-miss, but zero declarations at all. Because `IInstantiationService`'s dependency lookup falls back to whatever an undeclared subclass inherits through the prototype chain, the base nine `ViewPane` services still resolved correctly (so this did not, unlike the S4 stash bug, take down sibling views in the same container), but this class's own two added services (`workspaceContextService`, `editorService`) were silently `undefined` on every real construction. `Show File History`/`Show Line History` threw `Cannot read properties of undefined (reading 'activeEditor')` the instant either button was clicked -- and S2's entire commit-detail/multi-diff feature, whose only production entry point is this same view's 'View Changed Files' button, was therefore equally unreachable since S2 shipped. Fixed by declaring all 11 indices, mirroring `PlainGitStashView`/`PlainGitWorktreeView`/`PlainScmView`'s already-correct pattern; verified by the new Browser tests for both the History view and the commit-detail multi-diff editor, both of which failed before the fix and pass after it.",
					"Bug 2 (discovered writing the blame test, `plain-git-blame.ts`): `buildBlameDecorations`'s inline-blame decoration is an intentional zero-width range (`Position(line, MAX_SAFE_INTEGER)`, per this feature's own design doc). `ITextModel.deltaDecorations` accepted it and returned a well-formed decoration id with no error of any kind -- but the real view's injected-text query (`getInjectedTextInInterval`, consulted on every render) unconditionally drops any decoration whose range is empty unless `showIfCollapsed: true` is set, which this decoration never set. The result: inline blame text never appeared in a real editor's DOM, from S0 onward, with nothing -- no exception, no console warning, no failed IPC call -- to signal it. The hover half of the same feature worked the entire time regardless, because `PlainGitBlameHoverProvider` looks up its data by line number independent of whether any decoration actually rendered, which is precisely why this had gone unnoticed: the feature looked half-alive under manual inspection of the hover alone. Root-caused by directly reading `@codingame/monaco-vscode-api`'s own `textModel.js`/`textModelEvents.js` (not guessed), fixed with one line (`showIfCollapsed: true`), and confirmed via the new Browser test's own DOM assertion on the decoration's rendered text.",
					"Both fixes are two-line diffs; neither touched any Rust code, wire contract, or existing AST boundary contract. No new AST contract was added to guard against either recurring, on the judgment that (1) the DI-decorator class of bug already has a documented, repeatable fix pattern from S4 that a future author can follow, and (2) a structural lint for 'every zero-width after/before decoration must set showIfCollapsed' would be a narrow, single-call-site rule with low reuse value elsewhere in this codebase; the real, durable defense against both is the Browser coverage this slice added, which now exercises every constructor and every render path a unit test cannot reach."
				],
				"nativeScenarios": [
					"S3's own real (not merely referenced) large-repository benchmark, run against four synthetic repositories built with `git fast-import` in `/private/tmp` and deleted immediately after measurement (never committed): 50,000 commits/0 refs and 50,000/1,200 and 50,000/12,001 refs, plus 500,000 commits/0 and /1,201 refs. The central finding: `log_graph`'s wall time is dominated by `--topo-order`'s own upfront full-graph walk, not by `maxCount` or ref count -- 500,000 commits with a single ref took 1474ms with `--topo-order` versus 13-14ms without it, regardless of whether `--branches --tags --remotes` was also requested; ref count has a real but much smaller additive effect (50,000 commits: 134ms at 0 refs, 905ms at 12,001 refs). All measured numbers stay 10-15x inside the existing 30-second `GIT_EXEC_TIMEOUT` budget even at 500,000 commits; a linear extrapolation to Linux-kernel scale (~1.3M commits) lands around 5-6 seconds, disclosed explicitly as extrapolation, not a measurement. `refs_list` (`for-each-ref`, no topological sort) stayed fast throughout (39-905ms, scaling with ref count only, never with commit count).",
					"E2E-009 (docs/e2e-handover.md) is newly registered for this feature's own native-only remainder: whether the S3 numbers above actually *feel* fast on a real large repository in a real desktop session (a synthetic-repository millisecond count is not the same claim as subjective responsiveness); the real macOS native folder picker and any first-access sandbox authorization dialog for `worktree add`, which the Browser suite cannot render at all; a real `git stash pop` three-way-merge conflict (the Browser mock's conflict outcome is scripted, not a real merge); and the multi-diff editor rendering a real multi-file historical commit's actual diff content end to end. E2E-009 also flags, as a required pre-check before any of its other steps, that a real desktop session must confirm inline blame decorations actually render and that every History-view button works without throwing -- the exact two defects this slice found and fixed in the Browser suite -- since WKWebView's rendering/DI-resolution path is not guaranteed identical to the Chromium path this session's own Playwright run exercised."
				],
				"platformGaps": [
					"Inline blame is always whole-file (no `-L` viewport-scoped incremental fetch wired to scroll position), a disclosed S0 scope cut; the Rust-side `-L` support this would need is implemented and tested, just not yet consumed by the frontend contribution.",
					"Line history's start/end range is a plain two-number input form, not derived from the active editor's live selection (S1 scope cut); file-history's 'View Changed Files' entry point only appears once a row is expanded, and commit-detail's `MultiDiffEditorItem.goToFileUri` is left `undefined` everywhere (S2 scope cut) -- the diff panes are fully functional, there is simply no 'reveal in the current working copy' affordance yet.",
					"The commit-graph swimlane-collapse mitigation the frozen research doc's own risk list anticipated (folding lanes past some threshold) was deliberately not built: S3's real benchmark found the actual bottleneck is git's own `--topo-order` walk, not frontend SVG rendering of many lanes, so a lane-collapse UI would not address the real cost.",
					"Stash exposes only `push -m <message> [--include-untracked]`; `--keep-index` and other non-default `git stash` variants are not surfaced (S4 scope cut). A conflicting `apply`/`pop` is reported with the conflicted-path list but the stash panel does not yet wire that into the multi-diff-editor machinery S2 already built for commit detail -- it renders as a plain text list (S4 scope cut, though `gitShowCommitBlob` is already generic enough for a future slice to reuse for this).",
					"`worktree add` never exposes `-b`/`-B` to explicitly name a new branch (S5's deliberate minimal parameter surface, matching the frozen plan's literal command shape); the destination path is always a single new subfolder of a user-picked parent directory (the sandbox-compatible authorization model the lead session ruled on for this feature, documented in the frozen research doc's own 'risks and unknowns' section), never an arbitrary path.",
					"This slice (S6) did not add a structural AST contract against either of the two DI/decoration bugs it found and fixed (see results above for why); a future self-built `ViewPane` subclass or zero-width decoration remains exposed to the same class of defect unless a human remembers the pattern from this report."
				],
				"acceptanceResults": [
					{
						"acceptance": "Inline and file blame, hover and age heatmap work",
						"result": "Now covered end to end by a real Browser test: opening a file renders `.plain-git-blame-inline` decorations whose text is built from the fixture's own author/relative-time/summary (not a placeholder), tagged with the correct age-heatmap bucket class, and hovering the annotated line fetches and displays the full commit message body via a genuine `git_blame_commit_messages` round trip. This is also where S6 found and fixed the `showIfCollapsed` defect described above -- before the fix, this same test failed with zero decorations ever appearing in the DOM despite the bridge call and decode both succeeding silently. Rust-side coverage (S0, unchanged) remains the `--line-porcelain`/`-L` parser, the C-style path unescaping (including a real literal-LF filename fixture), and the batch `git log --no-walk` hover-message fetch."
					},
					{
						"acceptance": "File and line history, revision navigation and compare work",
						"result": "Now covered end to end by two real Browser tests: the History view's file-history list expands to show a commit's full message, its line-history list drills into one specific revision's own diff hunk (verified against exact fixture text, not merely 'something rendered'), and a separate test opens a historical commit's changed-file list as a real multi-diff editor (three distinct file panes titled with each file's own real basename, mapped correctly from added/deleted/modified `kind`s) via `PlainGitCommitMultiDiffSourceResolver`. This is also where S6 found and fixed the DI-decorator defect described above -- both of these tests failed before the fix (the first with a thrown exception, the second because its own entry point, the History view, was unreachable) and pass after it. Rust-side coverage (S1/S2, unchanged) remains `--follow`/`-L` history parsing, the `--skip`/`--max-count` drill-down with stale-index detection, and `show_commit`'s two-explicit-diff-invocation construction (never `git show`) including the merge/root-commit edge cases documented in that slice's own report."
					},
					{
						"acceptance": "Graph, refs, stash and worktree workflows pass fixtures",
						"result": "Now covered end to end by three real Browser tests, one of the few areas where the pre-S6 Rust-fixture-plus-unit-test coverage genuinely was already correct (no defect found here): the Graph/Refs view test asserts both the correct swimlane count (via distinct `cx` values across four SVG-rendered nodes forming a merge/two-branch/base DAG -- a real assertion on `computeGraphLayout`'s own algorithm output, not merely 'an SVG exists') and the branches/remote-branches/tags lists correctly grouped, sorted, and marked with the current HEAD and its upstream; the Stash test lists a real fixture entry, shows its real changed-file summary, and pops it only after a confirmation dialog naming that specific entry, ending in a real `git_stash_pop` call and an empty list; the Worktree test lists real entries, adds one (no confirmation, per this feature's own 'low-risk' design), and force-removes a dirty one only after an unforced attempt reports `needsForce` and a confirmation dialog is answered. Rust-side coverage (S3/S4/S5, unchanged) remains the swimlane/refs field-safety AST contracts, the real multi-branch DAG and non-ASCII ref-name fixtures, the `GIT_LITERAL_PATHSPECS=1` interaction with `stash push --include-untracked` (see below), and the two-phase unforced-then-forced `worktree remove` sequence."
					},
					{
						"acceptance": "No GitLens Plus, account, cloud, brand or AI code is present",
						"result": "Unchanged from each slice's own original finding, re-verified during S6's own reading of every file touched: `PlainScmProvider.historyProvider` remains `constObservable(undefined)` across all seven new views; none of them consume any vendor SCM history/graph/timeline/multi-diff `ScmMultiDiffSourceResolverContribution` contribution; the swimlane layout algorithm (`plain-git-graph-layout.ts`) was designed from the publicly-documented technique alone, never by reading `git log --graph` output or any third-party tool's source; the one new pinned dependency (`multi-diff-editor-service-override`) was audited clean of chat/AI coupling before being adopted (frozen research doc). The existing `no AI/Chat-related SCM command exists in the command palette` Browser test (unchanged, still passing) continues to cover this for the whole SCM surface including the seven new views."
					}
				]
			}
		},
		{
			"id": "F100",
			"name": "Generic Debug Adapter Protocol client",
			"phase": 4,
			"status": "complete",
			"acceptance": [
				"Stdio and TCP transports pass fragmented frame fixtures",
				"Breakpoints, stepping, stack, variables and evaluate work",
				"runInTerminal uses the integrated terminal",
				"Missing or untrusted adapters fail with actionable confirmation"
			],
			"evidence": {
				"commands": ["pnpm check", "pnpm test:e2e:browser"],
				"results": [
					"pnpm check passed in full on 2026-07-28 (exit code 0, captured via redirection plus an explicit `echo EXIT=$?` rather than through a pipe, per this session's own F080-era lesson): cargo fmt --check and cargo clippy --all-targets -- -D warnings both zero-diff/zero-warning; cargo test 1177/1177 (up from 988 at the end of F090: +189 across this feature's six slices S0-S5 -- framing, exec/trust, confirm/confirm_store, tcp, protocol, session, service, output_gate -- every slice exercised against real subprocesses and/or real TCP sockets, not only in-memory mocks); pnpm exec vitest run 74 test files, 1580 cases (up from 67/1428 at the end of the pre-F100 ViewPane-decorator harness hardening slice); node scripts/plain/check-boundaries.mjs 86 app sources / 145 Rust sources / 17 pinned runtime dependencies; node scripts/plain/check-bundle.mjs 2208 sources, 203 tracked transitional debt sources.",
					"Debt-surface zero drift held across every one of this feature's six slices: debtSourceCount 203, debtSourceSha256 03208818d455dabc60ddc6d2185e54bb8427303f7bd0ac111ac4d29d43c02b8b and categoryCounts {chatAgent:121, mcp:16, authAccount:11, syncEditSessions:8, extensionRuntime:47} are byte-identical to the F070/F080/F090 baseline; only sourceCount moved (2191 -> 2208), driven entirely by this feature's own new app/features/debug source files. Verified personally: `git log --oneline -- package.json` shows no commit since F090 S6 (`a43a836c`), and `grep -n monaco-vscode package.json` still lists exactly the same 15 `@codingame/monaco-vscode-*` entries F090 left behind, with no `monaco-vscode-debug-service-override` line anywhere in package.json or pnpm-lock.yaml -- this feature added zero new npm dependencies of any kind, pinned or otherwise, unlike F090 which added multi-diff-editor-service-override. This is a direct, checked confirmation of the frozen research document's own prediction, not an assumption carried over from it.",
					"pnpm test:e2e:browser 97/97 passed in about 3.5 minutes (exit code 0), up from 86/86 at the end of F090 -- the +11 is this feature's own new coverage across S3 (breakpoints/call-stack/variables/watch, +5), S4 (step controls/Debug Console/runInTerminal, +4) and S5 (session-ended alert + Debug Console ack/elision, +2); every pre-existing scenario continued to pass with zero regressions. The one pre-registered flaky case unrelated to this feature (`shows missing-parent create failures for both workspace roots`, an F030-era Explorer race) did not reproduce during this feature's runs.",
					"Verified by direct inspection, not by trusting each slice's own self-report: `grep -c 'pub(crate) async fn debug_' src-tauri/src/debug/commands.rs` and a count of `name: \"debug_` entries inside `DEBUG_COMMAND_CONTRACTS` in scripts/plain/boundary-contracts.mjs both independently return 17 (not 16 as an earlier verbal summary of this feature stated) -- all 17 real `debug_*` Tauri commands (3 confirmation commands from S1, 3 session commands from S2, 5 breakpoint/stack/variable/evaluate commands from S3, 5 step-control commands plus 1 output-ack command from S4/S5) are locked by an exact parameters/returnType/body AST contract, confirmed by grep against the real committed source rather than assumed from the slice reports.",
					"S3's own closing report claimed `DEBUG_COMMAND_CONTRACTS` had been extended with exact contracts for its 5 new commands (`debug_set_breakpoints`/`debug_stack_trace`/`debug_scopes`/`debug_variables`/`debug_evaluate`); this was false -- when S4 began extending the same array for its own new commands, a repository-wide grep showed those 5 command names had never appeared in any scripts/ or tests/ file, so `validateDebugCommandRegistration` was silently only covering the 8 commands from S1/S2 for an entire slice. S4 discovered this while working on something else and backfilled all 5 missing contracts (bringing the array to 15 entries at that point, now 17 after S4's own 5 step commands and S5's 1 output-ack command), rather than treating it as a pre-existing acceptable state. This is recorded here exactly as it happened -- a completion claim that was not true until a later slice's incidental work caught and fixed it -- not smoothed over.",
					"Two more places where this feature's own task instructions or the frozen research document did not match protocol reality, both corrected rather than silently worked around: (1) the task instructions for S4 required gating stepping controls (`continue`/`next`/`stepIn`/`stepOut`/`pause`) on `Capabilities` fields named `supportsContinue`/`supportsNext`/`supportsStepIn`/`supportsStepOut`/`supportsPause` -- verified against this feature's own real `lldb-dap`/`debugpy` capability captures (docs/research/2026-07-28-generic-dap.md) that no such fields exist in the DAP specification at all: these five base requests are mandatory baseline requests every adapter must implement: the real, narrower negotiation points are `supportsStepInTargetsRequest` and `supportsSteppingGranularity`, both explicitly out of this feature's v1 scope (same tier as the excluded Disassembly view). The implemented gating instead uses real session thread state (`stoppedThreadId`/`lastKnownThreadId` in `app/features/debug/plain-debug-call-stack-view.ts`), confirmed by direct source inspection during this closing pass. (2) The frozen research document's own slice split put Debug Console/REPL's `debug_evaluate` in S4, but this feature's task instructions explicitly required Watch's `debug_evaluate` (context:\"watch\") in S3 -- implemented per the explicit task instruction, recorded as a slice-scope ruling rather than an unapproved deviation from the frozen doc.",
					"Two real product defects were caught by this feature's own mandatory real-Browser-E2E discipline (assert real DOM/request bodies, not just element presence), both fixed within the same slice that introduced them, both recorded rather than treated as routine: S3's most serious finding was that `onMouseDown` toggled a breakpoint on any mouse button before `onContextMenu` fired, so a right-click to edit a conditional breakpoint/logpoint deleted the breakpoint first and left the popup with nothing to edit -- meaning conditional-breakpoint/logpoint editing had never been reachable since this feature's own first implementation of it; fixed by gating the toggle on `!event.event.leftButton`. S3 also found and fixed a mouse-event interface field misdeclared at the wrong nesting level, and a redundant double round-trip when both condition and logMessage were saved together. S4's finding was that the Debug Console view container and view were registered but no open command existed for either -- the panel could never actually be shown -- discovered only while writing this feature's own Playwright coverage and fixed by adding the `Plain: Debug Console` command.",
					"Real, machine-measured benchmark numbers (`--nocapture`, not third-party citations or an 'untested' placeholder), from S5 against a real spawned Python mock adapter subprocess: a 2000-frame `stackTrace` round trip took 12.607125ms for a ~239KB response body; a 50,000-element `variables` round trip took 146.759709ms for a ~3.23MB response body -- both comfortably inside the 64 MiB `MAX_DAP_MESSAGE_BYTES` ceiling (the 50,000-element case uses about 1/20th of it), confirming the constant is generously sized for real large-object scenarios rather than merely asserted to be. A 6,000-event (~1.2 MiB) `output` flood took 71.826417ms end to end (real subprocess, real pipe, real reader thread, real gate); only 64 of the 6,000 events were actually delivered to the frontend sink before ack (exactly the high-water-mark constant), the other 5,936 were merged/discarded, and acking released one `plain/outputElided` notification reporting exactly 138624 elided bytes."
				],
				"nativeScenarios": [
					"No Computer-Use-driven real desktop Tauri scenario was executed for F100 at all -- unlike every other completed feature in this project, F100 has zero native desktop evidence of its own; the entire real-adapter, real-desktop matrix is newly registered as E2E-010 in docs/e2e-handover.md and none of it has been executed. This is stated plainly rather than substituted with the research-phase evidence below, which is real but is not a desktop-application scenario.",
					"The closest thing to native evidence this feature has is from its own frozen research phase (docs/research/2026-07-28-generic-dap.md), which is real protocol-level evidence but was captured with a hand-written zero-dependency Python framing client against two real local DAP adapters, not against a running Plain.app: a real `lldb-dap` (Apple's own native lldb DAP wrapper) `initialize`/Capabilities handshake over stdio succeeded, but actually launching a debuggee under it hung completely (120s timeout) in that sandboxed research environment, suspected to be a ptrace/task_for_pid restriction rather than a protocol or design defect -- see platformGaps for why this also surfaces a real, independent macOS code-signing risk this feature does not resolve. A real `debugpy` session was run to full completion over stdio (initialize through disconnect, including a real breakpoint hit, stack/scopes/variables/evaluate, and continue-to-exit) and a real TCP session with `initialize` deliberately fragmented into 74 3-byte chunks against a real listening `debugpy.adapter` was also verified.",
					"Every one of this feature's own S0-S5 slices additionally proved its Rust-side mechanism against a real spawned subprocess and/or a real TCP socket, not only in-memory mocks: a real Python mock adapter subprocess for the full handshake/session lifecycle (S2), the step-control commands (S4), runInTerminal spawning a real TerminalService session (S4), per-request timeout classification (S5) and the output-backpressure flood (S5); a real local TCP socket for the S1 fragmented-frame test and the S2 close_window peer-observes-disconnect test. This is real subprocess/socket-level evidence, proven at the Rust integration-test layer -- it is not a substitute for a real Plain.app desktop session driven by Computer Use, which is exactly the gap E2E-010 exists to close."
				],
				"platformGaps": [
					"E2E-010 in docs/e2e-handover.md (real lldb-dap native debugging pending F120 code-signing entitlements, real debugpy end-to-end in a real Plain.app, real runInTerminal triggered by a real adapter, and the subjective feel of deep call stacks/large variables on a real large program) is handed over to Codex and not yet executed -- this is the single largest disclosed gap in this feature: F100 has no native-desktop acceptance evidence of its own at all.",
					"A real macOS application-signing risk was discovered during this feature's research phase, not resolved by it: a signed macOS application whose own spawned child (a native debugger such as lldb-dap) needs to call task_for_pid on a further child (the debuggee) typically requires the com.apple.security.cs.debugger entitlement on the application's own code signature, or the OS will deny it -- independent of whether Plain's own trust/confirmation logic is correct. This is a packaging-layer prerequisite that belongs to F120 (Branding, packaging, notices and release checks), not something this feature resolves; if F120 has not granted this entitlement, native-language breakpoints/stepping may not work at all in a signed release build even though they work in this feature's own dev-mode acceptance. E2E-010 states this dependency explicitly so the executing party knows this scenario may not be runnable until F120 lands the entitlement.",
					"The S1-identified TCP 'spawn-then-connect' composition (an adapter Plain itself spawns, which then opens a TCP listener Plain connects to, as opposed to connecting to an adapter already running externally) is not wired into any production code path. S5 built and tested an isolated primitive for it (`exec::spawn_adapter_as_tcp_companion`, confirmed via the `Tcp` variant rather than `spawn_adapter`'s hardcoded `Stdio` variant, with bidirectional isolation tests proving a subject confirmed only for one transport variant cannot be borrowed by the other spawn path) but deliberately did not connect it to `DebugSessionService::start_session`'s TCP branch, which still only ever calls `tcp::connect_adapter` (connect-only). Making it production-usable still needs a bounded retry-connect loop (a freshly spawned listener's first connect attempt will typically ECONNREFUSED) and a new configuration-surface field (a `spawnBeforeConnect`-style flag threaded through the frontend config parser and `DebugSessionStartRequest`) -- both judged out of scope for a 'robustness' slice and explicitly left as a documented follow-up in `src-tauri/src/debug/mod.rs`'s module doc, not silently dropped.",
					"runInTerminal was never actually triggered end to end by either of this feature's two real test adapters during development (debugpy defaults to `console:\"internalConsole\"`, and no launch configuration was constructed that would make lldb-dap request it); its real, spawn-a-real-TerminalService-session implementation and Rust integration test (S4) were both exercised against a synthetic, purpose-built mock adapter that emits a `runInTerminal` reverse request on demand, not against genuine lldb-dap/debugpy behavior. E2E-010 registers a real-adapter-triggered `runInTerminal` scenario to close this gap.",
					"`runInTerminal`'s `kind` field (`integrated`/`external`) is deliberately never inspected -- Plain has exactly one terminal facility (the integrated terminal) and building a second, higher-risk spawn surface to shell out to an OS-native terminal application for a branch that has never been exercised was judged not worth the new attack surface; every `runInTerminal` request is routed through the same integrated-terminal path regardless of what `kind` an adapter declares, proven by a real three-way (`integrated`/`external`/absent) Rust test asserting identical behavior.",
					"The Disassembly view (`supportsDisassembleRequest`, real and present on lldb-dap's own capability set) is explicitly excluded from v1, matching the frozen research document's own scope line; DAP's own in-flight request cancellation is absent because the protocol itself defines no such mechanism, not because of an implementation gap; `Plain: Start Debugging` always uses the first configuration in launch.json with no QuickPick for multiple configurations; the Watch view does not support nested `variablesReference` expansion of a compound evaluate result (only its top-level `result` string is shown); hit-count conditional breakpoints (`hitCondition`) are not implemented, matching the frozen research document's own S3 scope line.",
					"`output_gate`'s discard policy is a per-category oldest-bytes-first budget (`DEBUG_OUTPUT_MERGE_CAP_BYTES` = 1 MiB per category, `DEBUG_OUTPUT_MAX_CATEGORIES` = 16), not a single global memory budget -- worst case, a malicious/malformed adapter that concentrates output into a small number of categories and drives each to its cap can hold up to 16 MiB rather than one bounded global figure; judged acceptable (still a finite bound, and 16 already generously exceeds the ~5 categories the real DAP specification defines) and not tightened further. No test drives a single DAP message to the real 64 MiB `MAX_DAP_MESSAGE_BYTES` boundary itself -- this feature's own real large-object benchmarks (2000-frame stack, 50,000-element array) prove real large-object scenarios sit far below this ceiling, not how the framing layer behaves exactly at or beyond it, which was not required by this slice's own acceptance scope.",
					"Zeroing the monaco-vscode-api bundle migration debt remains F110; Windows and Linux native packaging, and the macOS code-signing entitlement this feature's own research surfaced as a prerequisite for native-debugger acceptance, remain F120.",
					"This feature adds no new @codingame/monaco-vscode-* dependency at all -- the pinned runtime dependency count is unchanged at 17, and the 15 codingame packages in package.json are exactly those F010-F090 already pinned. That is the opposite of F090, which did adopt multi-diff-editor-service-override, and it is not a stylistic preference: the debug-service-override is structurally unusable here rather than merely Chat-coupled. contributes.debuggers and contributes.breakpoints are ExtensionsRegistry extension points, and with no extensions and a NullExtensionService that activates nothing, ConfigurationManager.getDebugger(type) is permanently empty, so the type-resolution path is dead even with Chat and Task stripped out. DebugService additionally constructs AdapterManager and ConfigurationManager directly rather than taking them injected, so neither can be replaced. The debug UI, session state machine, framing and transports are therefore all self-built."
				],
				"acceptanceResults": [
					{
						"acceptance": "Stdio and TCP transports pass fragmented frame fixtures",
						"result": "S0's `framing::FrameDecoder` (built and fixture-tested against synthetic byte streams before any real adapter existed) and S1's `tcp.rs` reuse the identical decoder unchanged against a real local TCP socket -- the fixture set (header/body split across reads, one read containing multiple complete messages, a Content-Length mismatching the real body length, missing/unparseable Content-Length, unknown extra headers, and a MAX_DAP_MESSAGE_BYTES/MAX_DAP_HEADER_BYTES ceiling) was frozen directly from this feature's own real-byte research capture of lldb-dap's and debugpy's actual initialize handshakes, not invented from scratch. S1's own TCP test fragments a real initialize request into 74 3-byte chunks with a 3ms gap between them against a real listening debugpy.adapter and confirms correct reassembly; the S0 stdio fixtures and the S1 TCP fixtures both pass. Real desktop-scale fragmentation against a genuinely slow/adversarial network path is not exercised beyond this and is part of E2E-010's remaining scope."
					},
					{
						"acceptance": "Breakpoints, stepping, stack, variables and evaluate work",
						"result": "S3 delivered Monaco glyph-margin breakpoints (three-state verified/unverified/rejected, condition/logMessage editing gated by real Capabilities negotiation, not an assumed-always-on feature), a call-stack ViewPane driving frame selection, and a variables view with real lazy variablesReference expansion plus start/count pagination (proven against a real 5,000-element page in Rust and a real paginated 150-element sequence in Browser E2E) and a Watch view reusing debug_evaluate(context:\"watch\"). S4 added continue/next/stepIn/stepOut/pause, gated on real session thread state after this feature discovered the task instruction's assumed Capabilities fields (supportsContinue etc.) do not exist in the DAP specification -- see results for the correction. All of it is proven against real spawned Python mock adapter subprocesses in Rust (including a benchmark against 2000 stack frames and a 50,000-element array) and by 11 new Browser E2E scenarios across S3/S4; S3's own mandatory real-DOM-assertion Browser E2E discipline caught and fixed a defect that had made conditional-breakpoint/logpoint editing unreachable since this feature's own first implementation of it (right-click deleted the breakpoint before the edit popup could use it) -- see results for detail. No real native adapter (lldb-dap) has driven any of this end to end on a real desktop; that remains E2E-010."
					},
					{
						"acceptance": "runInTerminal uses the integrated terminal",
						"result": "S4 replaced S2's placeholder (forward-a-diagnostic-event-and-auto-reject) with a real implementation: an AppHandle-free core function (`handle_run_in_terminal_reverse_request`) parses RunInTerminalRequestArguments and calls a new internal `TerminalService::start_program` API (not the `terminal_start` Tauri command) to spawn a real, visible terminal tab titled `Debug: <title>`, closable by the user through the terminal domain's own ordinary kill path, with no hidden second spawn surface (locked by the new `validateDebugRunInTerminalBoundary` AST contract). Proven end to end against a real spawned Python mock adapter that itself issues a real runInTerminal reverse request: the adapter's own received reply carries a real positive processId, a real terminal session is created and produces real PTY output, and the frontend notification's session id matches. As disclosed in platformGaps, this was never actually triggered by either of this feature's two real reference adapters (debugpy defaults to internalConsole; no lldb-dap launch configuration was constructed that requests it) -- only by a synthetic mock adapter built for this purpose -- and a real-adapter-triggered desktop exercise of this path is registered as part of E2E-010, not yet executed."
					},
					{
						"acceptance": "Missing or untrusted adapters fail with actionable confirmation",
						"result": "Every adapter spawn/connect path is structurally gated trust-then-confirmation-then-action, locked by AST contracts (validateDebugAdapterSpawnBoundary, validateDebugAdapterConnectBoundary, validateDebugAdapterConfirmationBoundary, validateDebugTcpCompanionSpawnBoundary): trust.require_trusted must run first, then confirmation.require_confirmed keyed on the exact (command, args, transport) triple (not the workspace or the launch.json `type` string, so a silently swapped binary always re-prompts), with real positive/negative canary tests (a file-creation canary for stdio, a non-blocking TCP-accept-backlog canary for TCP) proving the gate is not merely cosmetic. Missing/malformed adapter configuration (`.plain/debug-adapters.json`/`.vscode/launch.json`) resolves deterministically to an actionable `adapter-not-found` error rather than silently doing nothing, covered by 20+ adversarial parser fixtures. S3 added a real trust-declined/empty-workspace UX (`plain-debug-trust.ts`, mirroring the terminal domain's own four-state flow) that this feature had not originally planned but discovered was a real UX gap during implementation -- without it, an untrusted workspace's `Plain: Start Debugging` only threw a raw WORKSPACE_NOT_TRUSTED error with no way to grant trust from that entry point."
					}
				]
			}
		},
		{
			"id": "F110",
			"name": "Legacy subsystem retirement",
			"phase": 5,
			"status": "complete",
			"acceptance": [
				"Electron and Node native runtime paths are removed",
				"AI, Agent, Chat, MCP and Copilot paths are removed",
				"Authentication, accounts and sync paths are removed",
				"General Extension Host and non-theme extensions are removed",
				"Language environments, remote, notebook, tasks and testing are removed"
			],
			"evidence": {
				"commands": [
					"pnpm check",
					"pnpm test:e2e:browser",
					"node scripts/plain/check-bundle.mjs --print"
				],
				"results": [
					"pnpm check passed in full on 2026-07-29 (exit code 0, captured via redirection plus an explicit `echo EXIT=$?` rather than through a pipe): cargo fmt --check and cargo clippy --all-targets -- -D warnings both zero-diff/zero-warning; cargo test 1177/1177 (unchanged since F100 -- this feature's seven implementation slices plus this closeout touched zero Rust source); pnpm exec vitest run 77 test files, 1654 cases; node scripts/plain/check-boundaries.mjs 88 app sources / 145 Rust sources / 17 pinned runtime dependencies; node scripts/plain/check-bundle.mjs 1985 sources, 53 tracked transitional debt sources.",
					"This feature's own tracked-debt trajectory, verified slice by slice against a real bundle rather than assumed: the F010-F100 baseline this feature inherited was 203 sources across five categories (chatAgent 121, mcp 16, authAccount 11, syncEditSessions 8, extensionRuntime 47), but acceptance 5 ('Language environments, remote, notebook, tasks and testing are removed') had zero coverage in that baseline. S0 (this feature's own frozen research document, finding 6, and the main session's own ruling point 4) found 57 more sources that had been in the bundle since F010 and in no baseline at all: one chatAgent file misclassified by a remote-sounding filename (remoteCodingAgentsService.js, 121 -> 122) and seven newly-tracked categories (notebook 23, tasks 1, testing 9, remote 11, languagePacks 2, languageDetection 2, treeSitter 8). Rather than reduce 203 while those 57 sat uncounted -- which would have flattered the metric without moving it -- S0 raised the honest baseline to 260 first, verified by a real pnpm build:frontend and check-bundle.mjs --print matching commit f87cc64e's own real numbers exactly. From that honest 260: S2 removed the mcp/syncEditSessions/authAccount (minus globalCompositeBar.js) stubs, 260 -> 231; S3 removed the chatAgent stubs (122 -> 9, the largest single removal), 231 -> 117; S4 migrated globalCompositeBar.js into app/ and closed authAccount's remaining tail, 117 -> 113; S5 severed extensionRuntime's only bundle-reachability path, 113 -> 84; S6 closed the seven newly-tracked categories from S0, 84 -> 53. sourceCount moved in step across the same slices: 2208 -> 2179 -> 2057 -> 2053 -> 2016 -> 1985, unchanged since. Every one of these numbers is a real, independently reproduced check-bundle.mjs count taken at the time of its own slice, not a retrospective estimate.",
					"The final, honestly measured floor per category (docs/bundle-baseline.json's categoryNotes carries the full per-file dependency-graph audit; this is the summary): mcp 0, syncEditSessions 0, tasks 0, testing 0 -- all four real, verified zeros. authAccount 1 (authentication.service.js, kept alive by chatEntitlementService.js -- itself one of chatAgent's own 9 floor files -- injecting IAuthenticationService into a class this feature's own real dependency audit confirmed is never actually DI-constructed anywhere in the running app). languagePacks 1 (localizedStrings.js, kept alive by VS Code's own built-in browser-automation driver.js, unrelated to this project's own Tauri/Playwright harness). remote 4 (remoteAgentService.service.js pinned by IPathService/ILabelService, two foundational always-constructed services; remoteHosts.js pinned by four ordinary URI-parsing consumers unrelated to remote development; remoteAuthorityResolver.js/.service.js pinned by WebviewElement's own non-optional constructor parameter, reinstated after a real before/after rebuild proved removing it bought zero file-count benefit while adding real risk). chatAgent 9 (kept alive by the Command Palette, Go to Anything, Go to Symbol and Format Document -- ordinary, always-running Workbench features, not chat functionality). notebook 9 (kept alive by a customEditorInputFactory.js type-check import chain that never actually constructs a NotebookEditorInput, plus INotebookDocumentService's real, load-bearing use inside every file/Explorer label widget). extensionRuntime 19 (every one of the 11 extensions-service-override package files is gone entirely; the 19 that remain back Format Document, Format Document With..., Change Language Mode and the declarative ExtensionsRegistry configuration/theme/snippet contribution mechanism AGENTS.md itself sanctions). languageDetection 2 and treeSitter 8 (both deliberately untouched -- see the S6 zero-benefit findings below).",
					"S5 is this project's fourth documented case of pnpm check passing completely while a real user-facing path was broken, and the first of the four with a genuinely new failure shape. The first version of the patch correctly deleted missing-services.js's global registerSingleton(IExtensionService, NullExtensionService, InstantiationType.Eager) and correctly added app/services.ts's own binding of IExtensionService to PlainNullExtensionService inside initialize()'s services map -- types checked, the AST contract passed, all 17 of PlainNullExtensionService's own unit tests passed, cargo test and the bundle ratchet were untouched and green. Full pnpm test:e2e:browser then failed 97 of 98 scenarios, all with the identical <body data-plain-ready='error'> bootstrap failure. Running a single scenario directly surfaced the real error: '[createInstance] hoverService depends on extensionService which is NOT registered.' The root cause was not a missing registration but the same token bound through two non-equivalent mechanisms: consumers resolved through the global singleton registry (which the patch had just emptied of this token) are constructed before initialize()'s services map ever takes effect, so binding IExtensionService only in the services map left every early global-registry consumer -- hoverService among them -- looking at an empty slot. The fix was for app/services.ts to also call registerSingleton(IExtensionService, PlainNullExtensionService, InstantiationType.Eager) directly, restoring the mechanism the patch had removed rather than replacing it with a different one. None of pnpm check's own steps could have caught this: the SyncDescriptor's type was correct, the AST contract checks shape rather than timing, and the unit tests construct an isolated object rather than a real container -- none of those three layers ever runs a real bootstrap. This is the same failure class as three earlier incidents in this project (F090 S0's zero-width blame decorations that never rendered, F090 S1's history view with zero DI decorators that threw on click, F100 S3's onMouseDown deleting a breakpoint before its own context menu could fire) -- every one only visible at real runtime/DOM timing, which is exactly why this feature's own discipline treats a full pnpm test:e2e:browser pass, not a green pnpm check, as the actual delivery bar.",
					"S6 made two deliberate zero-touch decisions after real dependency-graph audits, not oversights. languageDetection's ILanguageDetectionService is resolved unconditionally at the top of editorStatus.js's real, always-registered Change Language Mode command (Ctrl+K Ctrl+M, f1: true) before any other logic runs, the same load-bearing pattern S5 already found for IExtensionGalleryService -- removing it would reproduce S5's own bootstrap-death class on a real, everyday command instead of at boot. treeSitter's ITreeSitterLibraryService has a second, entirely independent registerSingleton inside standaloneServices.js (unconditionally loaded by base-service-override) with the identical always-false supportsLanguage() behavior as the missing-services.js stub this slice would have removed -- deleting the missing-services.js copy would have been genuinely zero-benefit, confirmed by real content inspection that both registrations behave identically. Both categories were left at their S0-frozen values (2 and 8) by deliberate choice, documented before any patch edit was attempted. S6 also removed and then reverted IRemoteAuthorityResolverService after a real before/after rebuild showed removing it changed the remote category's file count not at all (webviewElement.js's own non-optional constructor dependency on it, independent of missing-services.js, kept the file reachable regardless) while adding real risk with no equivalent fallback registration to lean on -- reverted via git stash rather than shipped as a speculative improvement. Separately, S6 flagged, and this closeout repeats rather than resolves, a genuine unexplained anomaly: workbench/api/common/extHostTypes/notebooks.js was already reachable in the bundle before this feature ever touched anything, and an exhaustive grep across the entire installed dependency tree (every @codingame/* package, every nested .pnpm store copy, app/ itself) found zero real importers of it or of the file that re-exports from it. Its presence is inert regardless of the mechanism keeping it in the source map, but why it is reachable at all remains genuinely unknown and is recorded as an open question for a future slice, not smoothed over as explained.",
					"The runtime excluded-surface guard (app/excluded-surface-policy.ts's findExcludedWorkbenchSurfaces, enforced by app/excluded-surfaces.ts's enforceExcludedWorkbenchSurfaces at bootstrap) changes role with this feature's completion, and this closeout verified rather than assumed the change is safe by reading the guard's own implementation: it is a pure blacklist that captures whatever command/view/contribution ids are actually registered at the time it runs and matches them against six category regexes, throwing only if the intersection is non-empty -- it asserts nothing about any id's existence and cannot fail merely because a target disappeared. Before this feature, it was the only line of defense proving the excluded chat/agent/mcp/auth/sync/notebook/task/testing/remote surfaces were unreachable, because the underlying bundle debt (203, later found to be 260) still physically existed. After this feature's real removals, for the four categories that reached a true zero (mcp, syncEditSessions, tasks, testing) there is no longer any registerable id these regexes could even match in the first place -- the guard becomes redundant for those categories rather than load-bearing, which is already demonstrated empirically rather than only by code reading: PLAIN_EXCLUDED_SURFACE_GUARD_V1 was never once thrown across any of this feature's Browser E2E runs. For the categories that remain at a non-zero floor (authAccount, chatAgent, extensionRuntime, notebook, remote, languagePacks, languageDetection, treeSitter), the guard keeps its original, load-bearing role, since those floor files are real, reachable vendor code and the guard is what proves none of it registers a user-facing id a user could actually reach. The guard was deliberately kept for the zeroed categories too, as defense in depth against a future regression reintroducing a matching id through a path this feature's own bundle analysis does not cover, at zero known cost.",
					"This closeout also found and fixed a stale, self-contradictory guard in scripts/plain/check-bundle.mjs, discovered while preparing to flip F110 to complete. A check written on this project's very first day (2026-07-18, commit 3fe6c358f, long before F110 was researched) asserted that once features.json marked F110 complete, no bundle source path could end with /missing-services.js. A real pnpm build:frontend run during this closeout confirmed missing-services.js is still present in the real dist source map today, and necessarily remains present under the architecture the main session actually ruled on (decision point 1: patch surgery, not a services.js facade rewrite) -- that file still legitimately registers roughly 80 non-debt service stubs (IHoverService, IUndoRedoService, IUriIdentityService, IKeyboardLayoutService among them) this feature was never in scope to remove. The stale check would have made marking F110 complete fail pnpm check permanently, for a file whose continued presence is the intended, accepted final state rather than a residual. It has been removed, with an explanatory comment recording why and what it assumed, in favor of the check that was already running unconditionally on every pnpm check and is the strictly correct proof: the token-level content scan (forbiddenDebtTokenStrings) that greps the entire built dist/**/*.js for every specific token string this feature's own patches removed from missing-services.js/services.js, regardless of feature status. This is disclosed here in full rather than folded silently into the closeout: it is a change to guard/script code (scripts/plain/check-bundle.mjs, not app/ or src-tauri/ product code), narrow, precedented by every one of S0-S6's own routine guard/contract-script edits, and it does not weaken any actual security guarantee since the more precise token-level scan it defers to was already in force.",
					"Honest characterization of how completely acceptance points 2, 3 and 5 were achieved, in the terms this closeout's own task framing required: not 'fully removed', but 'reduced from 260 tracked sources to 53, of which AI/Chat-related debt (chatAgent) fell from 122 to 9, mcp and settings-sync fell to 0, authentication/accounts fell to 1, and every remaining item is justified file-by-file and proven inert or zero-benefit to remove further.' Full pnpm exec playwright test 98/98 passed twice independently during this closeout (about 3.2 minutes each run, exit code 0 both times, node_modules/.vite cleared before each run); the one pre-registered flaky case unrelated to this feature (shows missing-parent create failures for both workspace roots, an F030-era Explorer race) did not reproduce in either run. Electron/Node legacy source-tree removal (S1, acceptance 1) deleted 16,103 tracked files and 5,128,731 lines (src/, extensions/, build/, test/, cli/, remote/web plus four top-level tool files), verified against a clean pnpm check and zero-drift bundle counts at the time, and documented file-by-file in docs/f110-s1-legacy-source-removal-manifest.md as a factual removal manifest for F120's own third-party-notices rewrite to consume."
				],
				"nativeScenarios": [
					"No native desktop (Computer Use) evidence exists for F110 of its own -- consistent with this feature's own division of labor (docs/e2e-handover.md, in force since 2026-07-22), every one of this feature's seven slices produced only Browser mock E2E and Rust/unit evidence; the real-desktop dimension is registered as E2E-011 in docs/e2e-handover.md and has not been executed.",
					"The closest indirect evidence is that PLAIN_EXCLUDED_SURFACE_GUARD_V1 (the runtime excluded-surface guard) was never once thrown across any of this feature's own Browser E2E runs, across all seven slices and this closeout's own two independent 98/98 reruns -- real evidence the guard's blacklist regexes never matched a real registered id in a real (mock-IPC) bootstrap, but this is the same category of evidence F100 already flagged as real-but-not-a-desktop-application-scenario: Playwright's mock-IPC bootstrap is not a real WKWebView, real command palette, or real menu bar, which is precisely the gap E2E-011 exists to close (a full manual command-palette/menu/settings/Activity-Bar audit, plus a real-desktop confirmation of the theme import/replace/remove flow through the post-S5 PlainNullExtensionService.deltaExtensions path)."
				],
				"platformGaps": [
					"E2E-011 in docs/e2e-handover.md (real-desktop exhaustive command-palette/menu/settings/Activity-Bar audit for AI/Chat/Account/Sync/Extension-Host reachability, real confirmation of the Manage gear and its compact title-bar variant, and a real theme import/replace/remove regression pass through the post-S5 PlainNullExtensionService.deltaExtensions path) is registered and not yet executed -- this is the single largest disclosed gap for this feature, matching the same disclosed-gap pattern F100 used for E2E-010.",
					"authAccount (1), chatAgent (9), extensionRuntime (19), notebook (9) and remote (4) do not reach zero, languagePacks (1) does not reach zero, and languageDetection (2) and treeSitter (8) were deliberately left untouched at their S0-frozen values -- every one of these eight numbers is a real, dependency-graph-audited floor with a file-by-file reason in docs/bundle-baseline.json's categoryNotes (a non-optional constructor dependency of an always-running Workbench feature, or a proven zero-benefit-to-remove case), not an unexplained residual, but acceptance points 2, 3, 4 and 5 are honestly not fully zero and this feature does not claim otherwise.",
					"workbench/api/common/extHostTypes/notebooks.js (one of the notebook category's 9 floor files) is reachable in the real bundle for a reason this feature's own exhaustive dependency-tree grep could not determine -- flagged as a genuine open question in docs/bundle-baseline.json's categoryNotes.notebook and repeated here rather than resolved, since resolving it would require investigation this closeout was not scoped to perform.",
					"cgmanifest.json, cglicenses.json and ThirdPartyNotices.txt were deliberately left unmodified by this feature per the main session's own ruling (decision point 6) -- docs/f110-s1-legacy-source-removal-manifest.md records the factual removal (which third-party components' original license files left the tree, and which of the top-level cgmanifest.json's 14 registrations correspond only to the deleted trees) as F120's own input, but their rewrite is F120's own responsibility.",
					"Top-level resources/ (legacy VS Code Electron packaging assets: platform icons, Inno Setup material, server manifests) and a second batch of orphaned legacy tooling config (.eslint-ignore, .eslint-allowed-javascript-files, .eslint-plugin-local, eslint.config.js, tsfmt.json, .lsifrc.json, .mention-bot, CodeQL.yml) were confirmed during S1 to be equally unreferenced by app/ or src-tauri/, but were judged outside this feature's authorized deletion scope (neither the frozen research document nor the task instructions named them) and were left in place as a disclosed observation rather than deleted opportunistically.",
					"scripts/plain/check-boundaries.mjs's requiredPatches marker for @codingame/monaco-vscode-api@35.0.1 still reads its original string ('Plain intentionally has no accounts surface') rather than a newer marker asserting the debt registrations were removed, as the frozen research document's own technical-proposal section suggested as a nice-to-have; this closeout confirmed the existing marker is still genuinely present and enforced and left it unchanged, judging the suggested marker refresh out of this closeout's authorized scope rather than a defect requiring a fix.",
					"Windows and Linux native packaging, code-signing entitlements, and the branding/bundle-id/SBOM work this feature's own bundle cleanup is an input to all remain F120, unchanged from F100's own disclosure of the same boundary."
				],
				"acceptanceResults": [
					{
						"acceptance": "Electron and Node native runtime paths are removed",
						"result": "S1 (docs/f110-s1-legacy-source-removal-manifest.md) deleted 16,103 tracked files / 5,128,731 lines: src/ (8,957 files, the entire Electron main/renderer process tree and every built-in workbench contrib's original implementation), extensions/ (6,477 files across 96 built-in extension directories including copilot/github/github-authentication/microsoft-authentication, plus 10 top-level tooling files), build/ (381 files, the gulp/Electron packaging pipeline), test/ (196 files), cli/ (83 files, an independent legacy Rust CLI unrelated to src-tauri/'s own Cargo workspace) and remote/web (6 files), plus four orphaned top-level tool files (.vscode-test.js, gulpfile.mjs, scripts/generate-definitelytyped.sh and one other). Verified rather than assumed dead: tsconfig.json's own exclude list already named every one of these directories before deletion, vite.config.ts's root is app/ and never touches them, and a real post-deletion pnpm check plus full pnpm test:e2e:browser passed with zero drift in bundle sourceCount/categoryCounts -- direct proof none of the 16,103 deleted files had ever participated in a real build or bundle output. Every one of these directories is now retrievable only via git show f87cc64e:<path> (the last commit before deletion), never as working-tree state."
					},
					{
						"acceptance": "AI, Agent, Chat, MCP and Copilot paths are removed",
						"result": "mcp reached a real, verified 0 (S2 removed 15 of 16 tokens; the 16th, mcpPlatformTypes.js, fell out as a side effect of S3 removing its only real consumer inside the chatAgent tree). chatAgent went from the honest S0 baseline of 122 down to a floor of 9 (S3), each of the 9 held alive by a real, non-optional constructor dependency of an ordinary, always-running Plain feature -- the Command Palette, Go to Anything, Go to Symbol in File/Workspace and Format Document -- not by any chat/agent/copilot functionality itself; every one of the 9 is enumerated with its exact real consumer in docs/bundle-baseline.json's categoryNotes.chatAgent, including two real AI-agent-plugin/voice-agent trees (platform/agentPlugins/**, workbench/contrib/agentsVoice/**) this feature's own audit found were never covered by any prior category matcher at all and removed outright rather than left uncounted. Not literally zero, but every remaining file is a real, load-bearing piece of ordinary Workbench infrastructure with no chat/agent/copilot behavior of its own, verified by a real dependency-graph audit against the actual bundled corpus rather than assumed from package names."
					},
					{
						"acceptance": "Authentication, accounts and sync paths are removed",
						"result": "syncEditSessions reached a real, verified 0 (S2, all 14 tokens removed with zero required consumers found anywhere in the installed dependency tree). authAccount went 11 -> 5 (S2, after two real rebuild-driven discoveries that missing-services.js alone was not the only reachability path) -> 1 (S4). S4's own contribution was migrating the real, working Activity Bar 'Manage' gear (app/features/workbench/plain-global-composite-bar.ts's PlainGlobalCompositeBar/PlainGlobalActivityActionViewItem) out of the vendor globalCompositeBar.js file rather than deleting it -- the gear is genuine, already-functioning UI whose account-specific behavior a pre-existing patch had already gutted; the classifier had only ever flagged it by filename. A second, previously-missed consumer (the view-title-bar-service-override package's own compact title-bar variant) required a second patch pass before authAccount actually moved. The single file that remains (authentication.service.js) is kept alive only by chatEntitlementService.js -- itself one of chatAgent's own accepted 9 floor files -- injecting IAuthenticationService into a class (ChatEntitlementRequests) this feature's own real content audit confirmed is never actually constructed by the running application, because IChatEntitlementService's own registerSingleton was already removed in S3. Not literally zero, but the one remaining file is provably inert and the honest floor is recorded rather than forced down by reopening a decision S3 already settled."
					},
					{
						"acceptance": "General Extension Host and non-theme extensions are removed",
						"result": "S5 severed the only reachability path bringing the entire @codingame/monaco-vscode-extensions-service-override package into the bundle at all (services.js's initialize() no longer imports or spreads that package's getServiceOverride() at all), and all 11 files of that package -- including the real ExtensionServiceOverride/ExtensionService classes and every WebWorker/Remote/local Extension Host mechanism -- are now entirely absent from the real bundle, not merely neutered. extensionRuntime fell 47 -> 19; IExtensionService is now bound to a Plain-owned app/services/plain-null-extension-service.ts::PlainNullExtensionService (17 unit tests assert field-by-field behavioral inertness: activateByEvent/activateById/startExtensionHosts are no-ops, canAddExtension/canRemoveExtension are permanently false, every event is Event.None). This same slice produced this project's fourth documented 'pnpm check green, real feature broken' incident: the registration was written correctly but bound only through initialize()'s services map rather than also through the global singleton registry the patch had just emptied, so early global-registry consumers like hoverService threw an unregistered-service error at real bootstrap -- caught only by a full pnpm test:e2e:browser run (97/98 failing) after pnpm check had passed completely, and fixed by also calling registerSingleton(IExtensionService, PlainNullExtensionService, InstantiationType.Eager) directly in app/services.ts. The 19 files that remain are enumerated individually in docs/bundle-baseline.json's categoryNotes.extensionRuntime, every one a real, non-optional dependency of ordinary Workbench features this product does ship (Format Document, Change Language Mode, the declarative ExtensionsRegistry contribution mechanism, NLS translation of imported theme-package manifests) -- not Extension-Host/RPC/worker/remote-host scaffolding, which is entirely gone."
					},
					{
						"acceptance": "Language environments, remote, notebook, tasks and testing are removed",
						"result": "This acceptance had zero baseline coverage before this feature: S0 found 57 sources (spanning notebook, tasks, testing, remote, languagePacks, languageDetection, treeSitter) had been in the bundle since F010 and in no baseline at all, and raised the honest debt count from 203 to 260 before removing anything, specifically to avoid reducing a number next to 57 uncounted sources. S6 then closed most of these seven categories: tasks reached a real 0 (1 token, zero other consumers found); testing reached a real 0 (9 tokens, zero other consumers found); notebook fell 23 -> 9 (the 9 held alive by a real customEditorInputFactory.js type-check import chain that never actually constructs a NotebookEditorInput, and by INotebookDocumentService's real, load-bearing use inside every file-label/Explorer-row widget in the product); remote fell 11 -> 4 (IRemoteAgentService kept deliberately -- a non-optional dependency of IPathService/ILabelService, both foundational, always-constructed services, removing it would reproduce the S5 bootstrap-death class; remoteHosts.js and remoteAuthorityResolver.js/.service.js each kept alive by real, independent, non-remote-development consumers); languagePacks fell 2 -> 1 (localizedStrings.js kept alive by VS Code's own built-in browser-automation driver, unrelated to i18n language-pack downloading or to Plain's own Tauri/Playwright harness). Two categories were deliberately left untouched at their S0-frozen values after real dependency audits found removal would be zero-benefit or unsafe: languageDetection (2, ILanguageDetectionService is resolved unconditionally at the top of the real, always-registered Change Language Mode command) and treeSitter (8, a second independent registerSingleton for the identical token with identical always-false behavior already exists in standaloneServices.js, so removing the missing-services.js copy would remove nothing). One file (notebooks.js, part of the notebook floor) is reachable for a reason this feature's own exhaustive dependency-tree grep could not determine and is recorded as an open question rather than resolved. Not literally zero across any of the five named surfaces, but every non-zero floor is either a real, load-bearing dependency of an ordinary Workbench feature or a proven zero-benefit-to-remove case, documented file-by-file in docs/bundle-baseline.json."
					}
				]
			}
		},
		{
			"id": "F120",
			"name": "Branding, packaging, notices and release checks",
			"phase": 5,
			"status": "complete",
			"acceptance": [
				"Bundle id, protocol, data directory and UI use Plain branding",
				"Third-party notices and SBOM match shipped code and assets",
				"macOS, Windows and Linux packages build in CI"
			],
			"evidence": {
				"commands": [
					"pnpm check",
					"pnpm test:e2e:browser",
					"pnpm tauri:build:e2e",
					"pnpm exec tauri build --bundles app",
					"node scripts/plain/generate-third-party-notices.mjs --check",
					"codesign -d --entitlements - <built .app>",
					"a real, bounded C-harness task_for_pid experiment (ad-hoc-signed with/without com.apple.security.cs.debugger)"
				],
				"results": [
					"pnpm check passed in full (EXIT=0, captured via redirection plus an explicit echo EXIT=$? rather than a pipe): cargo fmt --check and cargo clippy --all-targets -- -D warnings both zero-diff/zero-warning; cargo test 1177/1177 (unchanged -- this closeout touched zero Rust source); pnpm exec vitest run 79 test files / 1686 cases (up from 77/1666: +2 new files, forbidden-brand-strings.test.mjs and ci-packaging-contract.test.mjs, +20 new cases -- 8 in boundary-contracts.test.mjs for the new bundle/entitlements contracts, 6 in each new file); node scripts/plain/check-boundaries.mjs 88 app sources / 145 Rust sources / 17 pinned runtime dependencies (unchanged); node scripts/plain/check-bundle.mjs 1985 sources / 53 tracked transitional debt sources (unchanged -- the debt ratchet did not move, this closeout touched zero app/ or src-tauri/src files). Full pnpm exec playwright test passed 98/98 (about 3.2 minutes, exit 0) after clearing node_modules/.vite first; the one pre-registered flaky case unrelated to this feature (shows missing-parent create failures for both workspace roots) did not reproduce.",
					"S5 (entitlements): created src-tauri/Entitlements.plist containing only com.apple.security.cs.allow-jit=true. A real plutil -lint pass first showed the file well-formed, but a real ad-hoc-signed pnpm tauri:build:e2e (with APPLE_SIGNING_IDENTITY=- to force real, non-default codesigning) then failed with 'Failed to parse entitlements: AMFIUnserializeXML: syntax error near line 13' -- Apple's own entitlements parser (AMFI) is stricter than the general CoreFoundation plist parser plutil uses and rejects a literal '--' sequence inside an XML comment, a real XML-spec rule most tooling tolerates but AMFI does not; this project's own prose style uses '--' as an em dash throughout, so the file's original explanatory comment tripped it. Fixed by keeping Entitlements.plist itself comment-free and moving the rationale into scripts/plain/boundary-contracts.mjs's own EXPECTED_ENTITLEMENTS comment instead. After the fix, a real ad-hoc-signed build (APPLE_SIGNING_IDENTITY=-) produced a Plain.app with CodeDirectory flags=0x10002(adhoc,runtime) (hardened runtime genuinely on) and codesign -d --entitlements - showing com.apple.security.cs.allow-jit=true embedded; the raw signed binary was launched directly (not via a GUI Computer Use session) and stayed running for 4 seconds with no crash and no error output, the specific failure mode the research document's own '结论 4.3' warned hardened-runtime-without-JIT-entitlement causes. Separately discovered and disclosed rather than silently worked around: Tauri's bundler only invokes codesign (and therefore only ever applies bundle.macOS.entitlements) when some signingIdentity is actually configured -- the project's real default build path (no identity configured, matching the currently-accepted ad-hoc-only decision) does not touch codesign/entitlements at all and produces the same bare linker-applied ad-hoc signature as before this feature (confirmed: reverted the one-off APPLE_SIGNING_IDENTITY=- test after verifying it, and a fresh default pnpm tauri:build:e2e reproduced the pre-existing flags=0x20002(adhoc,linker-signed), zero-entitlements state exactly). signingIdentity was deliberately NOT added as a permanent tauri.conf.json default: doing so would have made every local build on this development machine specifically start failing at the DMG/entitlements-adjacent xattr step (see the CI note below) due to an unrelated, pre-existing Anaconda-installed xattr shadowing the real macOS /usr/bin/xattr on this machine's PATH -- a real, reproduced local-environment quirk, disclosed here rather than fixed by silently rewriting build scripts and their locked-string contracts for a machine-specific problem outside this task's scope.",
					"S5 (the com.apple.security.cs.debugger question): ran a real, bounded, reproducible experiment rather than deciding from documentation alone. A small C harness (posix_spawn a child, then task_for_pid(mach_task_self(), childPid, ...)) was compiled and ad-hoc codesigned two ways. Without any entitlement: task_for_pid fails fast and cleanly (KERN_FAILURE, no hang) -- reproduced twice. Re-signed with only com.apple.security.cs.debugger added: the identical call reliably blocks and had to be force-killed -- reproduced twice, with the process tree confirmed stuck via ps (not merely a timeout). Real Apple lldb itself (already signed by Apple with whatever entitlements it needs) was also run directly (lldb -o 'process launch' -o quit -- <target>) in this same environment and hung identically at process launch, reproducing F100's own original finding in a fresh session. DevToolsSecurity -status reports Developer Mode disabled on this machine with no passwordless path to enable it (enabling it would itself be a system security setting change, correctly out of bounds regardless of task convenience). Together: the entitlement measurably changes kernel-level behavior for the immediate caller regardless of what spawned it (consistent with the research document's 结论 4.4 caller-based correction -- Plain.app spawning an adapter is not the same as Plain.app being the caller of task_for_pid), but this specific sandboxed execution environment cannot complete real native process debugging at all, for reasons independent of any entitlement Plain.app itself carries. Per the main-session ruling's point 6, com.apple.security.cs.debugger was NOT added to Entitlements.plist -- the real-Mac, Developer-Mode-enabled verification the research document's 结论 4.4 and docs/e2e-handover.md's E2E-010 step 3 already call for still has not happened, and adding the entitlement now would be exactly the 'expand the attack surface on a guess' move the ruling forbids.",
					"S6 (CI): .github/workflows/plain-ci.yml gained a second job, build-macos (runs-on: macos-latest, gated on the existing check job passing), deliberately macOS-only per the ruling's point 7 -- no windows-latest or second ubuntu-latest packaging job was added. It installs Zig 0.15.2 via mlugg/setup-zig@v1 (matching this repository's own pinned Zig requirement for libghostty-vt-sys's build.rs, confirmed against this machine's own locally-installed zig version 0.15.2 and docs/research/2026-07-24-libghostty-terminal.md), runs pnpm ghostty:vendor:setup, then pnpm exec tauri build --bundles app. What was locally verified: the exact same command sequence (Node/pnpm install, ghostty:vendor:setup, a real tauri build) was run by hand on this machine and succeeded, producing a real, inspectable Plain.app (this is the same command class already used throughout this whole project's own local verification, not a new, untested sequence). What was NOT verified, and cannot be from here: the actual GitHub Actions runner environment -- whether mlugg/setup-zig@v1 behaves correctly on a real macos-latest runner, whether dtolnay/rust-toolchain@stable and Swatinem/rust-cache@v2 interact correctly with this repository's own Cargo workspace layout there, and general first-run CI timing/caching behavior. This matches the ruling's own explicit warning that a first real CI packaging run 'very likely surfaces unforeseen platform-specific problems' -- none of that can be pre-verified outside a real GitHub Actions run.",
					"S6 (the dmg target was deliberately dropped from the CI step, disclosed rather than silently scoped down): tauri.conf.json's bundle.targets is still [\"app\", \"dmg\"], unchanged, but a real local pnpm tauri:build (both targets) was attempted during this slice and the dmg step hung indefinitely -- confirmed via direct process inspection (ps showing bundle_dmg.sh's child osascript process alive with zero CPU progress after multiple checks), not merely a timeout. bundle_dmg.sh's own --sandbox-safe flag exists specifically to skip this Finder/AppleScript automation step, but Tauri's DmgConfig schema (read directly from @tauri-apps/cli's own config.schema.json) exposes no equivalent toggle, so there is no config-level fix available. This is judged very likely specific to this sandboxed verification environment lacking whatever interactive Automation/Apple-Events session a real desktop normally has (the same class of finding as the lldb/task_for_pid hangs above), not a genuine Tauri/GitHub-Actions defect -- DMG bundling via this same mechanism is a well-established, widely-used pattern for real Tauri projects on GitHub Actions macOS runners -- but this could not be confirmed with certainty from here. The CI job therefore runs `tauri build --bundles app` explicitly rather than the bare `pnpm tauri:build` script, so this feature's own CI evidence is grounded entirely in what was actually, repeatedly verified to work, and re-adding dmg is left as a disclosed follow-up for once a real CI run either confirms this is sandbox-specific or reveals a real fix is needed.",
					"S7 (release-check contracts, three of the four AST-contract items the research document proposed, item 3 already done in S5): (1) brand-string scan -- scripts/plain/forbidden-brand-strings.mjs, wired into check-bundle.mjs. A real pnpm build:frontend was run and every one of the research document's originally-proposed nine forbidden strings was measured as already present in the real, current dist/**/*.js (occurrence counts and exact source locations recorded in the module's own comment) via three distinct, already-accepted vendor sources: product.json.js's single always-loaded data blob (fields app/main.ts's productConfiguration override does not touch), the reverted dead ProductService class in missing-services.js (see the S0 incident below), and a newly-discovered third source -- the bundled vscode-codicons icon-name registry itself registers an icon literally named 'code-oss'. A zero-tolerance check against all nine as originally proposed would have failed on day one against the correct, already-accepted state of the bundle, exactly the 'green gate on day one is a broken gate' problem this project already fixed once before (F110 S7's removed missing-services.js-must-not-exist check). FORBIDDEN_BRAND_STRINGS is scoped to what is genuinely, currently absent and verified zero-tolerance today ('Visual Studio Code', 0 real occurrences); KNOWN_INERT_BRAND_STRINGS documents, with real evidence, every string this check does not gate and exactly why. A real, live reverse test was executed (not just a unit test): a forbidden string was appended to a real dist file, check-bundle.mjs was confirmed to fail with the exact expected message, the file was reverted, and check-bundle.mjs was confirmed clean again. (2) bundle-segment contract -- validateTauriConfiguration in scripts/plain/boundary-contracts.mjs was extended to lock config.identifier and the entire config.bundle shape (active/targets/category/copyright/icon array/macOS.minimumSystemVersion/macOS.entitlements path) via the same exact-match sameObject/sameArray style already used for the CSP contracts; previously this function checked nothing about bundle at all (confirmed by reading it before this slice). Reverse tests cover every field named in the research document's own 结论 6 (identifier reverted to a Code OSS-style value, bundle deleted entirely, each bundle field mutated one at a time, an extra undeclared bundle/macOS field added). (3) declaration freshness -- scripts/plain/generate-third-party-notices.mjs gained a --check mode (refactored main() to separate content generation from writing) that regenerates the real content in memory and diffs it against the committed ThirdPartyNotices.txt, exiting non-zero on drift; exposed as pnpm check:notices. Deliberately NOT wired into the default pnpm check chain, unlike every other check step: it requires cargo-about to be separately installed as a machine-local tool and re-resolves live pnpm/cargo dependency data on every run rather than being a fast, offline static check. A real, live reverse test was executed in both directions: run against the real, current repository state it reported 'fresh' (35 JS packages, 429 Rust crates, 111 notice blocks, matching S2+S3+S4's own numbers exactly); a stray line was then appended to the real committed ThirdPartyNotices.txt, the check was confirmed to report 'stale' and exit 1, and the file was restored (git status confirmed clean afterward).",
					"S0's reverted missing-services.js ProductService deletion (already recorded in progress.md, repeated here because it directly constrains what S7's brand-string scan could honestly claim): the dead second class ProductService (hardcoded nameShort=nameLong='Code - OSS Dev', applicationName='code-oss', dataFolderName='.vscode-oss') cannot currently be safely deleted -- a real attempt passed pnpm check in full and then broke 97 of 98 real Browser E2E scenarios with 'contextService depends on productService which is NOT registered', this project's fifth documented 'green gate, broken feature' incident. The delete was fully reverted (patch restored to its original sha256, missing-services-patch-contract.mjs's KEPT_TOKEN_REGISTRATIONS keeps IProductService, a matching reverse test exists) and re-verified with a full 98/98 Playwright rerun before this feature's own work began.",
					"Third-party declaration disclosure required by the task, repeated from S2+S3+S4's own evidence rather than silently dropped now that F120 is closing: three items remain explicitly flagged as needing human/legal confirmation, not resolved by this closeout -- jschardet's LGPL-2.1+ dynamic-linking exemption applicability in a bundled-WebView-JS context (a real, disputed grey area this project does not have standing to resolve); vscode-codicons' exact upstream commit/version correspondence to the font actually embedded in @codingame/monaco-vscode-api 35.0.1 (fontTools read the embedded font's own internal version string, 'Version 1.15 http://fontello.com', which is a different versioning scheme than cgmanifest.json's npm-style '0.0.46-0', so the two could not be cross-checked byte-for-byte); and libghostty-vt's vendored Ghostty C source Nerd Fonts sub-resource, confirmed only at the source/module-import-graph level (Zig's real build.zig import graph for the -Demit-lib-vt target never imports src/font/) rather than by a binary-level nm/otool inspection of the actual linked artifact."
				],
				"nativeScenarios": [
					"No native desktop (Computer Use) scenario was executed for S5-S7 of their own -- consistent with this project's established division of labor (docs/e2e-handover.md, in force since 2026-07-22): Claude implements and runs everything automatable from this environment, real interactive desktop verification is registered for Codex. What was done instead, and is real (not simulated) evidence even though it is not a GUI session: multiple real pnpm tauri:build:e2e / pnpm exec tauri build --bundles app / one-off APPLE_SIGNING_IDENTITY=- ad-hoc-signed builds were produced and inspected directly with codesign -dv, codesign -d --entitlements -, plutil -lint and spctl -a -vv (spctl correctly reports 'rejected', the expected, accepted outcome for an ad-hoc-only build with no Developer ID/notarization); the ad-hoc-signed, hardened-runtime, JIT-entitled binary was launched directly (bypassing Finder/LaunchServices, not through a Computer Use GUI session) and stayed running for 4 seconds with no crash.",
					"A separate, small, purpose-built C harness (not Plain's own code) was used to empirically test the com.apple.security.cs.debugger question against a real macOS kernel Mach API (task_for_pid), and real Apple lldb was run directly against a real compiled target -- both are real native-process-level evidence, gathered specifically because a real interactive Mac session with Developer Mode enabled was not available here; see the S5 entitlements results above for the exact findings and their limits."
				],
				"platformGaps": [
					"The icon is still the Tauri scaffold's default logo asset (src-tauri/icons/{32x32.png,128x128.png,128x128@2x.png,icon.icns,icon.ico}), confirmed via git log --diff-filter=A to have entered the tree in the initial feat: init commit, not designed for Plain. This feature does NOT claim 'VS Code de-branding complete' -- producing Plain's own icon is a design task requiring the product owner to provide or commission real artwork, not an engineering task this closeout could complete. bundle.icon is correctly wired to these files and the bundle contract locks that wiring, but the asset itself remains someone else's brand, not Plain's.",
					"Signing and notarization are explicitly on hold: the product owner decided not to invest in an Apple Developer Program account for this feature (main-session ruling, 'originally-pending decision point 7'). Only local ad-hoc signing was completed and verified (see S5 results). This directly and explicitly narrows F130: any acceptance scenario requiring 'install and run on a different machine' cannot be executed, macOS Gatekeeper's real notarization-gate behavior cannot be verified (only spctl's expected 'rejected' outcome for an unnotarized ad-hoc build was observed), and if real-Mac testing later proves native debugging genuinely depends on signing state, that acceptance is equally gated on this same deferred decision.",
					"Three third-party declaration items remain flagged as needing human/legal confirmation, unresolved by this closeout (repeated from S2+S3+S4, not newly discovered, not silently dropped): jschardet's LGPL-2.1+ dynamic-linking exemption applicability in this project's bundled-WebView-JS context; vscode-codicons' exact upstream commit/version correspondence to the embedded font (the embedded font's own internal version string uses a different scheme than cgmanifest.json's npm-style version, so they could not be cross-checked byte-for-byte); libghostty-vt's vendored Nerd Fonts sub-resource linkage, confirmed only at the source/import-graph level, not by a binary-level nm/otool inspection of the actual linked artifact.",
					"S0 discovered that missing-services.js's dead second ProductService class (hardcoded 'Code - OSS Dev'/'code-oss' values) cannot currently be safely deleted -- a real attempt passed pnpm check in full and then broke 97 of 98 real Playwright scenarios (this project's fifth documented 'green gate, broken feature' incident). The delete was fully reverted before this feature's later slices began; scripts/plain/forbidden-brand-strings.mjs's KNOWN_INERT_BRAND_STRINGS documents the specific strings this leaves in the real bundle and why the brand-string scan cannot gate them without forking a vendor package.",
					"CI packaging (S6) is verified only to the depth possible without a real GitHub Actions run: the exact command sequence was run by hand on this development machine and succeeded, but mlugg/setup-zig@v1's real behavior on a macos-latest runner, dtolnay/rust-toolchain@stable/Swatinem/rust-cache@v2's interaction with this repository's Cargo workspace there, and general first-run timing/caching are all unverified from here, exactly as the ruling anticipated ('very likely surfaces unforeseen platform-specific problems' on first run). The dmg bundle target was deliberately dropped from the CI step (kept in tauri.conf.json's own bundle.targets, just not requested by the CI build command) after a real local dmg build hung indefinitely on Finder/AppleScript automation with no config-level workaround available in Tauri's DmgConfig schema -- judged very likely specific to this sandboxed verification environment rather than a real platform defect, but not confirmed with certainty; re-adding it to CI is a disclosed follow-up.",
					"Windows and Linux packaging remain entirely out of scope for this feature, per the main-session ruling's point 7 ('add macOS to CI first, not all three platforms at once') -- tauri.conf.json's bundle.targets is still macOS-only (['app','dmg']), no windows-latest/second-ubuntu-latest CI job exists, and the third acceptance criterion's literal 'Windows and Linux' wording is only partially satisfied by design, disclosed rather than silently claimed complete.",
					"The reportIssueUrl/licenseUrl UI-reachability sweep the research document flagged as an incomplete verification attempt (Claude Browser tooling could not stably drive Quick Input for a command-palette audit) was not retried by this closeout; it remains an open item for a future real-desktop sweep, most naturally folded into a future E2E-011-style command-palette audit rather than a new, narrow item of its own.",
					"docs/e2e-handover.md gained a new registered entry (E2E-012) for the real-desktop dimensions this closeout could not execute from here: a real interactive GUI launch/visual confirmation of the ad-hoc hardened-runtime build (this closeout only confirmed the raw process survives 4 seconds, not that the WebView actually renders and is interactive), and a real CI first-run confirmation once a PR triggers the new build-macos job. It explicitly does not duplicate E2E-010 step 3 (the real-Mac com.apple.security.cs.debugger verification), which remains that entry's own responsibility."
				],
				"acceptanceResults": [
					{
						"acceptance": "Bundle id, protocol, data directory and UI use Plain branding",
						"result": "Already real and verified before this closeout (F120 S0/S1: identifier com.plain.editor, dataFolderName .plain, urlProtocol plain, document.title === \"Plain\"). This closeout's own contribution is making that state provably durable rather than just currently true: validateTauriConfiguration now locks config.identifier and the entire config.bundle shape (icon/copyright/category/targets/macOS fields) so a silent revert would fail pnpm check, and scripts/plain/forbidden-brand-strings.mjs's real-build-verified scan catches any newly-reintroduced Code OSS brand string in the shipped bundle (currently 'Visual Studio Code', with room to grow) while honestly documenting, rather than hiding, the vendor-baked Code OSS strings (product.json.js's data blob, the reverted dead ProductService class, a codicon icon name) this closeout confirmed cannot be removed without forking a vendor package. The icon itself is explicitly excluded from this acceptance's claim of completion -- it is still the Tauri scaffold's default asset, not Plain's own design (see platformGaps)."
					},
					{
						"acceptance": "Third-party notices and SBOM match shipped code and assets",
						"result": "S2+S3+S4 (already complete before this closeout) rewrote ThirdPartyNotices.txt/cgmanifest.json/cglicenses.json from the real, current dependency graph (35 JS packages, 429 Rust crates). This closeout's own contribution is the freshness guarantee that was previously entirely missing: scripts/plain/generate-third-party-notices.mjs's new --check mode (pnpm check:notices) regenerates the real content and diffs it against the committed file, verified live in both directions (fresh today; a real injected drift was caught and the file restored). Three items remain explicitly flagged as needing human/legal confirmation rather than resolved (jschardet LGPL dynamic-linking exemption scope, vscode-codicons exact version/commit correspondence, libghostty-vt's Nerd Fonts linkage confirmed only at source level) -- not silently dropped now that the feature is closing."
					},
					{
						"acceptance": "macOS, Windows and Linux packages build in CI",
						"result": "Only macOS, deliberately, per the main-session ruling's point 7: .github/workflows/plain-ci.yml gained a real build-macos job running pnpm exec tauri build --bundles app (dmg deliberately dropped from the CI command after a real local hang with no available config-level fix; Windows/Linux are entirely out of scope for this feature). The exact same command sequence was verified locally by hand (Zig 0.15.2 available, ghostty:vendor:setup, a real tauri build producing an inspectable Plain.app), but the actual GitHub Actions runner behavior -- the new job's first real run -- is unverified from this environment and could reveal problems this closeout did not anticipate, exactly as the ruling itself warned. scripts/plain/ci-packaging-contract.mjs locks that the workflow keeps a macos-* job with a real tauri build step, with reverse tests for a missing macOS job, a macOS job that never builds, and a real build step that only exists in an unrelated job."
					}
				]
			}
		},
		{
			"id": "F130",
			"name": "Browser and native end-to-end acceptance",
			"phase": 5,
			"status": "in_progress",
			"acceptance": [
				"Browser mock E2E covers required UI state machines",
				"Computer-driven Tauri E2E covers real fs, PTY, Git, theme and DAP",
				"No excluded product surface is reachable",
				"Startup, memory and large workspace baselines are recorded"
			]
		}
	]
}
