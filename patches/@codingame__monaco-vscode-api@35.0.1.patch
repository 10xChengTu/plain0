diff --git a/services.js b/services.js
index 018691b598a8aa41da826da3e6a1100fa4929b97..ac7d4eb9521ed4c2d7ce38cb13060d904a47b6d4 100644
--- a/services.js
+++ b/services.js
@@ -24,7 +24,6 @@ import './vscode/src/vs/workbench/contrib/inlayHints/browser/inlayHintsAccessibi
 import './vscode/src/vs/workbench/contrib/codeActions/browser/codeActions.contribution.js';
 import './vscode/src/vs/workbench/contrib/list/browser/list.contribution.js';
 import './vscode/src/vs/workbench/contrib/codeEditor/browser/editorFeatures.js';
-import './vscode/src/vs/workbench/browser/actions/developerActions.js';
 import './vscode/src/vs/workbench/contrib/dropOrPasteInto/browser/dropOrPasteInto.contribution.js';
 import './vscode/src/vs/workbench/contrib/meteredConnection/browser/meteredConnection.contribution.js';
 import './vscode/src/vs/workbench/contrib/inlineCompletions/browser/inlineCompletions.contribution.js';
diff --git a/vscode/src/vs/platform/files/common/files.d.ts b/vscode/src/vs/platform/files/common/files.d.ts
index 217e68b81ae5b921ecb15f866edd5f861f081f2e..43b7a7b7294045784b0e051fec711f9b3c49ea0e 100644
--- a/vscode/src/vs/platform/files/common/files.d.ts
+++ b/vscode/src/vs/platform/files/common/files.d.ts
@@ -775,6 +775,40 @@ export declare class FileOperationError extends Error {
     readonly options?: (IReadFileOptions | IWriteFileOptions | ICreateFileOptions) | undefined;
     constructor(message: string, fileOperationResult: FileOperationResult, options?: (IReadFileOptions | IWriteFileOptions | ICreateFileOptions) | undefined);
 }
+export type PlainWorkspaceWriteOutcome = Readonly<{
+    readonly status: "targetPublished";
+    readonly publicationEvidence: "targetObservedWritten";
+    readonly rename: "reportedSuccess";
+    readonly directorySync: "failed";
+    readonly target: "matchesWritten";
+}> | Readonly<{
+    readonly status: "targetPublished";
+    readonly publicationEvidence: "renameReportedSuccess";
+    readonly rename: "reportedSuccess";
+    readonly directorySync: "synced" | "failed";
+    readonly target: "changed" | "unverifiable";
+}> | Readonly<{
+    readonly status: "targetPublished";
+    readonly publicationEvidence: "targetObservedWritten";
+    readonly rename: "reportedFailure";
+    readonly directorySync: "synced" | "failed";
+    readonly target: "matchesWritten" | "changed" | "unverifiable";
+}> | Readonly<{
+    readonly status: "outcomeUnknown";
+    readonly observation: "native";
+    readonly rename: "reportedFailure";
+    readonly directorySync: "notAttempted";
+    readonly target: "ambiguous";
+}> | Readonly<{
+    readonly status: "outcomeUnknown";
+    readonly observation: "responseUnavailable";
+    readonly rename: "unobserved";
+    readonly directorySync: "unobserved";
+    readonly target: "ambiguous";
+}>;
+export declare function createPlainWorkspaceWriteOutcomeError(outcome: PlainWorkspaceWriteOutcome, options?: IWriteFileOptions): FileOperationError;
+export declare function isPlainWorkspaceWriteOutcomeError(error: unknown): error is FileOperationError;
+export declare function getPlainWorkspaceWriteOutcome(error: unknown): PlainWorkspaceWriteOutcome | undefined;
 export declare class TooLargeFileOperationError extends FileOperationError {
     readonly fileOperationResult: FileOperationResult.FILE_TOO_LARGE;
     readonly size: number;
diff --git a/vscode/src/vs/platform/files/common/files.js b/vscode/src/vs/platform/files/common/files.js
index 0ec5225b326fbfa5c0225f4d96fffc251ea6a301..053504dc5cd4b8fcb0c5717e0c74ab1b2a39ab99 100644
--- a/vscode/src/vs/platform/files/common/files.js
+++ b/vscode/src/vs/platform/files/common/files.js
@@ -346,6 +346,95 @@ var FileOperationResult;
     FileOperationResult[FileOperationResult["FILE_NOT_DIRECTORY"] = 9] = "FILE_NOT_DIRECTORY";
     FileOperationResult[FileOperationResult["FILE_OTHER_ERROR"] = 10] = "FILE_OTHER_ERROR";
 })(FileOperationResult || (FileOperationResult = {}));
+const plainWorkspaceWriteOutcomeErrors = ( new WeakMap());
+function plainWorkspaceWriteOutcomeSnapshot(value) {
+    try {
+        if (!value || typeof value !== "object" || Object.getPrototypeOf(value) !== Object.prototype || !Object.isFrozen(value)) {
+            throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: outcome must be one frozen plain object"));
+        }
+        const descriptors = Object.getOwnPropertyDescriptors(value);
+        const keys = Reflect.ownKeys(descriptors);
+        if (keys.some(key => typeof key !== "string")) {
+            throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: outcome keys must be strings"));
+        }
+        for (const key of keys) {
+            const descriptor = descriptors[key];
+            if (!descriptor || !("value" in descriptor) || descriptor.enumerable !== true) {
+                throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: outcome fields must be enumerable own data properties"));
+            }
+        }
+        structuredClone(value);
+        const status = descriptors.status?.value;
+        if (status === "targetPublished") {
+            const expectedKeys = ["status", "publicationEvidence", "rename", "directorySync", "target"];
+            if (keys.length !== expectedKeys.length || !expectedKeys.every(key => keys.includes(key))) {
+                throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: targetPublished fields differ from the closed contract"));
+            }
+            const publicationEvidence = descriptors.publicationEvidence.value;
+            const rename = descriptors.rename.value;
+            const directorySync = descriptors.directorySync.value;
+            const target = descriptors.target.value;
+            const reportedSuccessWithObservedTarget = rename === "reportedSuccess" && publicationEvidence === "targetObservedWritten" && directorySync === "failed" && target === "matchesWritten";
+            const reportedSuccessWithChangedTarget = rename === "reportedSuccess" && publicationEvidence === "renameReportedSuccess" && (directorySync === "synced" || directorySync === "failed") && (target === "changed" || target === "unverifiable");
+            const reportedFailureWithObservedTarget = rename === "reportedFailure" && publicationEvidence === "targetObservedWritten" && (directorySync === "synced" || directorySync === "failed") && (target === "matchesWritten" || target === "changed" || target === "unverifiable");
+            if (!reportedSuccessWithObservedTarget && !reportedSuccessWithChangedTarget && !reportedFailureWithObservedTarget) {
+                throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: targetPublished fields are not Rust-representable"));
+            }
+            return Object.freeze({
+                status,
+                publicationEvidence,
+                rename,
+                directorySync,
+                target
+            });
+        }
+        if (status === "outcomeUnknown") {
+            const expectedKeys = ["status", "observation", "rename", "directorySync", "target"];
+            if (keys.length !== expectedKeys.length || !expectedKeys.every(key => keys.includes(key))) {
+                throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: outcomeUnknown fields differ from the closed contract"));
+            }
+            const observation = descriptors.observation.value;
+            const rename = descriptors.rename.value;
+            const directorySync = descriptors.directorySync.value;
+            const target = descriptors.target.value;
+            const nativeUnknown = observation === "native" && rename === "reportedFailure" && directorySync === "notAttempted" && target === "ambiguous";
+            const responseUnavailable = observation === "responseUnavailable" && rename === "unobserved" && directorySync === "unobserved" && target === "ambiguous";
+            if (!nativeUnknown && !responseUnavailable) {
+                throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: outcomeUnknown fields are not Rust-representable"));
+            }
+            return Object.freeze({
+                status,
+                observation,
+                rename,
+                directorySync,
+                target
+            });
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: only incomplete or unknown outcomes can be branded"));
+    } catch {
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_OUTCOME: outcome validation failed"));
+    }
+}
+class PlainWorkspaceWriteOutcomeError extends FileOperationError {
+    constructor(outcome, options) {
+        const snapshot = plainWorkspaceWriteOutcomeSnapshot(outcome);
+        const message = snapshot.status === "targetPublished" ? "The workspace write may have changed the target file." : "The workspace write result could not be verified.";
+        super(message, FileOperationResult.FILE_OTHER_ERROR, options);
+        plainWorkspaceWriteOutcomeErrors.set(this, snapshot);
+    }
+}
+function createPlainWorkspaceWriteOutcomeError(outcome, options) {
+    return ( new PlainWorkspaceWriteOutcomeError(outcome, options));
+}
+function isPlainWorkspaceWriteOutcomeError(error) {
+    return error instanceof PlainWorkspaceWriteOutcomeError && plainWorkspaceWriteOutcomeErrors.has(error);
+}
+function getPlainWorkspaceWriteOutcome(error) {
+    if (!isPlainWorkspaceWriteOutcomeError(error)) {
+        return undefined;
+    }
+    return plainWorkspaceWriteOutcomeSnapshot(plainWorkspaceWriteOutcomeErrors.get(error));
+}
 const AutoSaveConfiguration = {
     OFF: "off",
     AFTER_DELAY: "afterDelay",
@@ -437,4 +526,4 @@ function getLargeFileConfirmationLimit(arg) {
     return 1024 * ByteSize.MB;
 }
 
-export { AutoSaveConfiguration, ByteSize, ETAG_DISABLED, FILES_ASSOCIATIONS_CONFIG, FILES_EXCLUDE_CONFIG, FILES_READONLY_EXCLUDE_CONFIG, FILES_READONLY_FROM_PERMISSIONS_CONFIG, FILES_READONLY_INCLUDE_CONFIG, FileChangeFilter, FileChangeType, FileChangesEvent, FileKind, FileOperation, FileOperationError, FileOperationEvent, FileOperationResult, FilePermission, FileSystemProviderCapabilities, FileSystemProviderError, FileSystemProviderErrorCode, FileType, HotExitConfiguration, NotModifiedSinceFileOperationError, TooLargeFileOperationError, createFileSystemProviderError, ensureFileSystemProviderError, etag, getLargeFileConfirmationLimit, hasFileAppendCapability, hasFileAtomicDeleteCapability, hasFileAtomicReadCapability, hasFileAtomicWriteCapability, hasFileCloneCapability, hasFileFolderCopyCapability, hasFileReadStreamCapability, hasFileRealpathCapability, hasOpenReadWriteCloseCapability, hasReadWriteCapability, hasReadonlyCapability, isFileOpenForWriteOptions, markAsFileSystemProviderError, toFileOperationResult, toFileSystemProviderErrorCode, whenProviderRegistered };
+export { AutoSaveConfiguration, ByteSize, ETAG_DISABLED, FILES_ASSOCIATIONS_CONFIG, FILES_EXCLUDE_CONFIG, FILES_READONLY_EXCLUDE_CONFIG, FILES_READONLY_FROM_PERMISSIONS_CONFIG, FILES_READONLY_INCLUDE_CONFIG, FileChangeFilter, FileChangeType, FileChangesEvent, FileKind, FileOperation, FileOperationError, FileOperationEvent, FileOperationResult, FilePermission, FileSystemProviderCapabilities, FileSystemProviderError, FileSystemProviderErrorCode, FileType, HotExitConfiguration, NotModifiedSinceFileOperationError, TooLargeFileOperationError, createFileSystemProviderError, createPlainWorkspaceWriteOutcomeError, ensureFileSystemProviderError, etag, getLargeFileConfirmationLimit, getPlainWorkspaceWriteOutcome, hasFileAppendCapability, hasFileAtomicDeleteCapability, hasFileAtomicReadCapability, hasFileAtomicWriteCapability, hasFileCloneCapability, hasFileFolderCopyCapability, hasFileReadStreamCapability, hasFileRealpathCapability, hasOpenReadWriteCloseCapability, hasReadWriteCapability, hasReadonlyCapability, isFileOpenForWriteOptions, isPlainWorkspaceWriteOutcomeError, markAsFileSystemProviderError, toFileOperationResult, toFileSystemProviderErrorCode, whenProviderRegistered };
diff --git a/vscode/src/vs/platform/files/common/plainWorkspaceDelete.d.ts b/vscode/src/vs/platform/files/common/plainWorkspaceDelete.d.ts
new file mode 100644
index 0000000000000000000000000000000000000000..299f37d1d978f3a0c8174cb544b687baec06f906
--- /dev/null
+++ b/vscode/src/vs/platform/files/common/plainWorkspaceDelete.d.ts
@@ -0,0 +1,70 @@
+import type { IDisposable } from "../../../base/common/lifecycle";
+import type { URI } from "../../../base/common/uri";
+import type { ResourceFileEdit } from "../../../editor/browser/services/bulkEditService";
+
+export declare const PLAIN_WORKSPACE_SCHEME = "plain-workspace";
+declare const plainWorkspaceDeleteAuthorizationBrand: unique symbol;
+export interface PlainWorkspaceDeleteAuthorization {
+    readonly [plainWorkspaceDeleteAuthorizationBrand]: true;
+}
+export type PlainWorkspaceDeleteKind = "file" | "directory" | "symlink";
+export type PlainWorkspaceDeleteReason = "entryChanged" | "entryUnverifiable" | "deleteFailed";
+export type PlainWorkspaceDeleteResult = Readonly<{ status: "deleted" }> | Readonly<{ status: "entryRetained"; reason: PlainWorkspaceDeleteReason }> | Readonly<{ status: "entryPartiallyDeleted"; reason: PlainWorkspaceDeleteReason; removedEntries: number }>;
+export type PlainWorkspaceDeleteState = Readonly<{ status: "pending" }> | Readonly<{ status: "inFlight" }> | Readonly<{ status: "ordinaryFailure" }> | Readonly<{ status: "outcomeUnknown" }> | PlainWorkspaceDeleteResult;
+export interface PlainWorkspaceDeleteAuthorizationSnapshot {
+    readonly confirmationId: string;
+    readonly entryId: string;
+    readonly rootId: string;
+    readonly relativePath: string;
+    readonly recursive: true;
+    readonly kind: PlainWorkspaceDeleteKind;
+    readonly permanent: true;
+}
+export interface PlainWorkspaceDeleteResourceSnapshot {
+    readonly scheme: typeof PLAIN_WORKSPACE_SCHEME;
+    readonly rootId: string;
+    readonly relativePath: string;
+}
+export type PlainWorkspaceDeleteResourceClassification = Readonly<{ kind: "other" }> | Readonly<{ kind: "plain"; scheme: typeof PLAIN_WORKSPACE_SCHEME; rootId: string; relativePath: string }>;
+export interface PlainWorkspaceDeleteElement {
+    readonly resource: URI;
+    readonly name: string;
+    readonly isDirectory: boolean;
+    readonly isSymbolicLink: boolean;
+}
+export interface PlainWorkspaceDeleteExplorerService {
+    applyBulkEdit(edits: readonly ResourceFileEdit[], options: Readonly<{ undoLabel: string; progressLabel: string }>): Promise<unknown>;
+}
+export interface PlainWorkspaceDeleteDialogService {
+    confirm(options: Readonly<{ type: "warning"; message: string; detail: string; primaryButton: string }>): Promise<Readonly<{ confirmed: boolean }>>;
+}
+export interface PlainWorkspaceDeleteWorkingCopyService {
+    getDirty(resource: URI): readonly unknown[];
+}
+export interface PlainWorkspaceDeleteFilesConfigurationService {
+    isReadonly(resource: URI): unknown;
+}
+export interface PlainWorkspaceDeleteCoordinatorContext {
+    readonly elements: readonly PlainWorkspaceDeleteElement[];
+    readonly explorerService: PlainWorkspaceDeleteExplorerService;
+    readonly dialogService: PlainWorkspaceDeleteDialogService;
+    readonly workingCopyFileService: PlainWorkspaceDeleteWorkingCopyService;
+    readonly filesConfigurationService: PlainWorkspaceDeleteFilesConfigurationService;
+}
+export type PlainWorkspaceDeleteCoordinator = (context: PlainWorkspaceDeleteCoordinatorContext) => Promise<void>;
+
+export declare function authorizePlainWorkspaceDeleteResourceEdit(options: object, resource: URI, value: PlainWorkspaceDeleteAuthorizationSnapshot): PlainWorkspaceDeleteAuthorization;
+export declare function validatePlainWorkspaceDeleteResourceEditBatch(edits: readonly ResourceFileEdit[]): boolean;
+export declare function movePlainWorkspaceDeleteResourceEditAuthorization(options: object, resource: URI, operation: object): boolean;
+export declare function movePlainWorkspaceDeleteWorkingCopyAuthorization(operation: object, resource: URI, fileOptions: object): boolean;
+export declare function movePlainWorkspaceDeleteFileServiceAuthorization(fileOptions: object, resource: URI, providerOptions: object): boolean;
+export declare function takePlainWorkspaceDeleteProviderAuthorization(providerOptions: object, resource: URI): PlainWorkspaceDeleteAuthorization | undefined;
+export declare function getPlainWorkspaceDeleteAuthorizationSnapshot(authorization: PlainWorkspaceDeleteAuthorization): PlainWorkspaceDeleteAuthorizationSnapshot;
+export declare function classifyPlainWorkspaceDeleteResource(resource: URI): PlainWorkspaceDeleteResourceClassification | undefined;
+export declare function beginPlainWorkspaceDeleteProviderDispatch(authorization: PlainWorkspaceDeleteAuthorization): void;
+export declare function completePlainWorkspaceDeleteProviderResult(authorization: PlainWorkspaceDeleteAuthorization, value: PlainWorkspaceDeleteResult): PlainWorkspaceDeleteResult;
+export declare function completePlainWorkspaceDeleteProviderFailure(authorization: PlainWorkspaceDeleteAuthorization, outcome: "ordinaryFailure" | "outcomeUnknown"): Readonly<{ status: "ordinaryFailure" | "outcomeUnknown" }>;
+export declare function getPlainWorkspaceDeleteState(authorization: PlainWorkspaceDeleteAuthorization): PlainWorkspaceDeleteState;
+export declare function snapshotPlainWorkspaceDeleteResource(resource: URI): PlainWorkspaceDeleteResourceSnapshot | undefined;
+export declare function registerPlainWorkspaceDeleteCoordinator(coordinator: PlainWorkspaceDeleteCoordinator): IDisposable;
+export declare function runPlainWorkspaceDeleteCoordinator(context: PlainWorkspaceDeleteCoordinatorContext): Promise<void>;
diff --git a/vscode/src/vs/platform/files/common/plainWorkspaceDelete.js b/vscode/src/vs/platform/files/common/plainWorkspaceDelete.js
new file mode 100644
index 0000000000000000000000000000000000000000..90ff02199b0822f4ee279609909b1b2f14a6be20
--- /dev/null
+++ b/vscode/src/vs/platform/files/common/plainWorkspaceDelete.js
@@ -0,0 +1,414 @@
+const PLAIN_WORKSPACE_SCHEME = "plain-workspace";
+const UUID_V4_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/u;
+const DELETE_KINDS = ( new Set(["file", "directory", "symlink"]));
+const DELETE_REASONS = ( new Set(["entryChanged", "entryUnverifiable", "deleteFailed"]));
+const MAX_RELATIVE_PATH_BYTES = 4096;
+const MAX_RELATIVE_PATH_SEGMENTS = 256;
+const utf8Encoder = ( new TextEncoder());
+const authorizationHandles = ( new WeakMap());
+const resourceEditAuthorizations = ( new WeakMap());
+const workingCopyAuthorizations = ( new WeakMap());
+const fileServiceAuthorizations = ( new WeakMap());
+const providerAuthorizations = ( new WeakMap());
+let registeredCoordinator;
+
+function ownDataSnapshot(value) {
+    if (!value || typeof value !== "object") {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: expected one plain object"));
+    }
+    const prototype = Object.getPrototypeOf(value);
+    if (prototype !== Object.prototype && prototype !== null) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: unexpected prototype"));
+    }
+    const descriptors = Object.getOwnPropertyDescriptors(value);
+    const keys = Reflect.ownKeys(descriptors);
+    const snapshot = Object.create(null);
+    for (const key of keys) {
+        if (typeof key !== "string") {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: fields must use string keys"));
+        }
+        const descriptor = descriptors[key];
+        if (!descriptor || !("value" in descriptor) || descriptor.enumerable !== true) {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: fields must be enumerable own data"));
+        }
+        snapshot[key] = descriptor.value;
+    }
+    return Object.freeze({ keys: Object.freeze(keys), snapshot: Object.freeze(snapshot), descriptors });
+}
+
+function exactOwnDataSnapshot(value, expectedKeys, requireOrdinary = false) {
+    const result = ownDataSnapshot(value);
+    if (result.keys.length !== expectedKeys.length || expectedKeys.some(key => !result.keys.includes(key))) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: unexpected fields"));
+    }
+    if (requireOrdinary && expectedKeys.some(key => result.descriptors[key]?.configurable !== true || result.descriptors[key]?.writable !== true)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: fields must be ordinary own data"));
+    }
+    return result.snapshot;
+}
+
+function isWellFormedUtf16(value) {
+    for (let index = 0; index < value.length; index++) {
+        const code = value.charCodeAt(index);
+        if (code >= 0xd800 && code <= 0xdbff) {
+            if (index + 1 >= value.length) {
+                return false;
+            }
+            const next = value.charCodeAt(index + 1);
+            if (next < 0xdc00 || next > 0xdfff) {
+                return false;
+            }
+            index++;
+        }
+        else if (code >= 0xdc00 && code <= 0xdfff) {
+            return false;
+        }
+    }
+    return true;
+}
+
+function isWindowsReservedSegment(value) {
+    const rawStem = value.split(".", 1)[0] ?? value;
+    const stem = rawStem.replace(/[a-z]/g, character => String.fromCharCode(character.charCodeAt(0) - 32));
+    return ["CON", "PRN", "AUX", "NUL", "CONIN$", "CONOUT$"].includes(stem) || /^(?:COM|LPT)(?:[1-9¹²³])$/u.test(stem);
+}
+
+function isPortableWorkspacePathSegment(value) {
+    if (value.length < 1 || !isWellFormedUtf16(value) || value === "." || value === ".." || value.endsWith(".") || value.endsWith(" ") || isWindowsReservedSegment(value)) {
+        return false;
+    }
+    for (const character of value) {
+        const code = character.charCodeAt(0);
+        if (code <= 0x1f || code === 0x7f || '/\\:<>"|?*'.includes(character)) {
+            return false;
+        }
+    }
+    return true;
+}
+
+function isValidRelativePath(value) {
+    if (typeof value !== "string" || value.length < 1 || value.length > MAX_RELATIVE_PATH_BYTES || !isWellFormedUtf16(value) || utf8Encoder.encode(value).byteLength > MAX_RELATIVE_PATH_BYTES) {
+        return false;
+    }
+    const segments = value.split("/");
+    return segments.length <= MAX_RELATIVE_PATH_SEGMENTS && segments.every(isPortableWorkspacePathSegment);
+}
+
+function deleteAuthorizationSnapshot(value) {
+    const snapshot = exactOwnDataSnapshot(value, [
+        "confirmationId",
+        "entryId",
+        "rootId",
+        "relativePath",
+        "recursive",
+        "kind",
+        "permanent"
+    ], true);
+    if (!UUID_V4_PATTERN.test(snapshot.confirmationId) || !UUID_V4_PATTERN.test(snapshot.entryId) || snapshot.confirmationId === snapshot.entryId || !UUID_V4_PATTERN.test(snapshot.rootId) || !isValidRelativePath(snapshot.relativePath) || snapshot.recursive !== true || !DELETE_KINDS.has(snapshot.kind) || snapshot.permanent !== true) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid authorization"));
+    }
+    return Object.freeze({
+        confirmationId: snapshot.confirmationId,
+        entryId: snapshot.entryId,
+        rootId: snapshot.rootId,
+        relativePath: snapshot.relativePath,
+        recursive: snapshot.recursive,
+        kind: snapshot.kind,
+        permanent: snapshot.permanent
+    });
+}
+
+function deleteResultSnapshot(value) {
+    const result = ownDataSnapshot(value);
+    const status = result.snapshot.status;
+    if (status === "deleted") {
+        if (result.keys.length !== 1 || result.keys[0] !== "status") {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid deleted result"));
+        }
+        return Object.freeze({ status });
+    }
+    if (status === "entryRetained") {
+        if (result.keys.length !== 2 || !result.keys.includes("status") || !result.keys.includes("reason") || !DELETE_REASONS.has(result.snapshot.reason)) {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid retained result"));
+        }
+        return Object.freeze({ status, reason: result.snapshot.reason });
+    }
+    if (status === "entryPartiallyDeleted") {
+        if (result.keys.length !== 3 || !result.keys.includes("status") || !result.keys.includes("reason") || !result.keys.includes("removedEntries") || !DELETE_REASONS.has(result.snapshot.reason) || !Number.isSafeInteger(result.snapshot.removedEntries) || result.snapshot.removedEntries < 1 || result.snapshot.removedEntries > 10000) {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid partial result"));
+        }
+        return Object.freeze({ status, reason: result.snapshot.reason, removedEntries: result.snapshot.removedEntries });
+    }
+    throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid delete result"));
+}
+
+function classifyPlainWorkspaceDeleteResource(resource) {
+    try {
+        const scheme = resource.scheme;
+        if (typeof scheme !== "string") {
+            return undefined;
+        }
+        if (scheme !== PLAIN_WORKSPACE_SCHEME) {
+            return Object.freeze({ kind: "other" });
+        }
+        const authority = resource.authority;
+        const path = resource.path;
+        const query = resource.query;
+        const fragment = resource.fragment;
+        if (typeof authority !== "string" || typeof path !== "string" || typeof query !== "string" || typeof fragment !== "string" || !UUID_V4_PATTERN.test(authority) || query !== "" || fragment !== "" || path.length <= 1 || !path.startsWith("/")) {
+            return undefined;
+        }
+        const relativePath = path.slice(1);
+        if (!isValidRelativePath(relativePath)) {
+            return undefined;
+        }
+        return Object.freeze({ kind: "plain", scheme, rootId: authority, relativePath });
+    }
+    catch {
+        return undefined;
+    }
+}
+
+function snapshotPlainWorkspaceDeleteResource(resource) {
+    const classification = classifyPlainWorkspaceDeleteResource(resource);
+    if (classification?.kind !== "plain") {
+        return undefined;
+    }
+    return Object.freeze({
+        scheme: classification.scheme,
+        rootId: classification.rootId,
+        relativePath: classification.relativePath
+    });
+}
+
+function assertPrepared(record) {
+    if (record.phase !== "prepared") {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: authorization is no longer pending"));
+    }
+}
+
+function assertResource(record, resource) {
+    const snapshot = snapshotPlainWorkspaceDeleteResource(resource);
+    if (snapshot === undefined || snapshot.rootId !== record.snapshot.rootId || snapshot.relativePath !== record.snapshot.relativePath) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: authorization resource mismatch"));
+    }
+}
+
+function assertResourceEditOptions(record, options) {
+    const snapshot = exactOwnDataSnapshot(options, ["recursive", "folder", "ignoreIfNotExists", "skipTrashBin"], true);
+    if (snapshot.recursive !== true || snapshot.folder !== (record.snapshot.kind === "directory") || snapshot.ignoreIfNotExists !== false || snapshot.skipTrashBin !== true) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid ResourceFileEdit options"));
+    }
+}
+
+function assertWorkingCopyOperation(record, operation, resource) {
+    const snapshot = exactOwnDataSnapshot(operation, ["resource", "recursive", "useTrash"], true);
+    if (snapshot.resource !== resource || snapshot.recursive !== true || snapshot.useTrash !== false) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid working-copy delete operation"));
+    }
+}
+
+function assertFileDeleteOptions(options) {
+    const snapshot = exactOwnDataSnapshot(options, ["recursive", "useTrash", "atomic"], true);
+    if (snapshot.recursive !== true || snapshot.useTrash !== false || snapshot.atomic !== false) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: delete must be recursive, permanent and non-atomic"));
+    }
+}
+
+function createAuthorizationRecord(value) {
+    const handle = Object.freeze(Object.create(null));
+    const record = {
+        snapshot: deleteAuthorizationSnapshot(value),
+        phase: "prepared",
+        terminal: undefined
+    };
+    authorizationHandles.set(handle, record);
+    return { handle, record };
+}
+
+function authorizePlainWorkspaceDeleteResourceEdit(options, resource, value) {
+    if (resourceEditAuthorizations.has(options)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: ResourceFileEdit options already authorized"));
+    }
+    const created = createAuthorizationRecord(value);
+    assertResource(created.record, resource);
+    assertResourceEditOptions(created.record, options);
+    resourceEditAuthorizations.set(options, created.record);
+    return created.handle;
+}
+
+function validatePlainWorkspaceDeleteResourceEditBatch(edits) {
+    if (!Array.isArray(edits)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid bulk edit collection"));
+    }
+    const records = edits.map(edit => resourceEditAuthorizations.get(edit?.options));
+    const authorizedCount = records.filter(record => record !== undefined).length;
+    if (authorizedCount === 0) {
+        return false;
+    }
+    if (authorizedCount !== edits.length) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: authorized delete batch must contain only authorized edits"));
+    }
+    let confirmationId;
+    const entryIds = ( new Set());
+    for (let index = 0; index < edits.length; index++) {
+        const edit = edits[index];
+        const record = records[index];
+        if (!record || !edit || edit.oldResource === undefined || edit.newResource !== undefined) {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: authorized batch must contain only deletes"));
+        }
+        assertPrepared(record);
+        assertResource(record, edit.oldResource);
+        assertResourceEditOptions(record, edit.options);
+        if (confirmationId === undefined) {
+            confirmationId = record.snapshot.confirmationId;
+        }
+        if (record.snapshot.confirmationId !== confirmationId || entryIds.has(record.snapshot.entryId)) {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: authorized batch identity mismatch"));
+        }
+        entryIds.add(record.snapshot.entryId);
+    }
+    return true;
+}
+
+function movePlainWorkspaceDeleteResourceEditAuthorization(options, resource, operation) {
+    const record = resourceEditAuthorizations.get(options);
+    if (record === undefined) {
+        return false;
+    }
+    resourceEditAuthorizations.delete(options);
+    assertPrepared(record);
+    assertResource(record, resource);
+    assertResourceEditOptions(record, options);
+    assertWorkingCopyOperation(record, operation, resource);
+    if (workingCopyAuthorizations.has(operation)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: working-copy operation already authorized"));
+    }
+    workingCopyAuthorizations.set(operation, record);
+    return true;
+}
+
+function movePlainWorkspaceDeleteWorkingCopyAuthorization(operation, resource, fileOptions) {
+    const record = workingCopyAuthorizations.get(operation);
+    if (record === undefined) {
+        return false;
+    }
+    workingCopyAuthorizations.delete(operation);
+    assertPrepared(record);
+    assertResource(record, resource);
+    assertWorkingCopyOperation(record, operation, resource);
+    assertFileDeleteOptions(fileOptions);
+    if (fileServiceAuthorizations.has(fileOptions)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: FileService options already authorized"));
+    }
+    fileServiceAuthorizations.set(fileOptions, record);
+    return true;
+}
+
+function movePlainWorkspaceDeleteFileServiceAuthorization(fileOptions, resource, providerOptions) {
+    const record = fileServiceAuthorizations.get(fileOptions);
+    if (record === undefined) {
+        return false;
+    }
+    fileServiceAuthorizations.delete(fileOptions);
+    assertPrepared(record);
+    assertResource(record, resource);
+    assertFileDeleteOptions(fileOptions);
+    assertFileDeleteOptions(providerOptions);
+    if (providerAuthorizations.has(providerOptions)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: provider options already authorized"));
+    }
+    providerAuthorizations.set(providerOptions, record);
+    return true;
+}
+
+function takePlainWorkspaceDeleteProviderAuthorization(providerOptions, resource) {
+    const record = providerAuthorizations.get(providerOptions);
+    if (record === undefined) {
+        return undefined;
+    }
+    providerAuthorizations.delete(providerOptions);
+    assertPrepared(record);
+    assertResource(record, resource);
+    assertFileDeleteOptions(providerOptions);
+    const handle = Object.freeze(Object.create(null));
+    authorizationHandles.set(handle, record);
+    return handle;
+}
+
+function getPlainWorkspaceDeleteAuthorizationSnapshot(authorization) {
+    const record = authorizationHandles.get(authorization);
+    if (!record) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: unknown authorization"));
+    }
+    return record.snapshot;
+}
+
+function beginPlainWorkspaceDeleteProviderDispatch(authorization) {
+    const record = authorizationHandles.get(authorization);
+    if (!record || record.phase !== "prepared") {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: provider dispatch rejected"));
+    }
+    record.phase = "inFlight";
+}
+
+function completePlainWorkspaceDeleteProviderResult(authorization, value) {
+    const record = authorizationHandles.get(authorization);
+    if (!record || record.phase !== "inFlight") {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: provider result rejected"));
+    }
+    const result = deleteResultSnapshot(value);
+    record.terminal = result;
+    record.phase = "terminal";
+    return result;
+}
+
+function completePlainWorkspaceDeleteProviderFailure(authorization, outcome) {
+    const record = authorizationHandles.get(authorization);
+    if (!record || record.phase !== "inFlight" || (outcome !== "ordinaryFailure" && outcome !== "outcomeUnknown")) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: provider failure rejected"));
+    }
+    record.terminal = Object.freeze({ status: outcome });
+    record.phase = "terminal";
+    return record.terminal;
+}
+
+function getPlainWorkspaceDeleteState(authorization) {
+    const record = authorizationHandles.get(authorization);
+    if (!record) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: unknown authorization"));
+    }
+    if (record.phase === "prepared") {
+        return Object.freeze({ status: "pending" });
+    }
+    if (record.phase === "inFlight") {
+        return Object.freeze({ status: "inFlight" });
+    }
+    return record.terminal;
+}
+
+function registerPlainWorkspaceDeleteCoordinator(coordinator) {
+    if (typeof coordinator !== "function" || registeredCoordinator !== undefined) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: coordinator registration rejected"));
+    }
+    registeredCoordinator = coordinator;
+    let active = true;
+    return Object.freeze({
+        dispose() {
+            if (active && registeredCoordinator === coordinator) {
+                active = false;
+                registeredCoordinator = undefined;
+            }
+        }
+    });
+}
+
+async function runPlainWorkspaceDeleteCoordinator(context) {
+    const coordinator = registeredCoordinator;
+    if (coordinator === undefined) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: coordinator is unavailable"));
+    }
+    return await coordinator(context);
+}
+
+export { PLAIN_WORKSPACE_SCHEME, authorizePlainWorkspaceDeleteResourceEdit, beginPlainWorkspaceDeleteProviderDispatch, classifyPlainWorkspaceDeleteResource, completePlainWorkspaceDeleteProviderFailure, completePlainWorkspaceDeleteProviderResult, getPlainWorkspaceDeleteAuthorizationSnapshot, getPlainWorkspaceDeleteState, movePlainWorkspaceDeleteFileServiceAuthorization, movePlainWorkspaceDeleteResourceEditAuthorization, movePlainWorkspaceDeleteWorkingCopyAuthorization, registerPlainWorkspaceDeleteCoordinator, runPlainWorkspaceDeleteCoordinator, snapshotPlainWorkspaceDeleteResource, takePlainWorkspaceDeleteProviderAuthorization, validatePlainWorkspaceDeleteResourceEditBatch };
diff --git a/vscode/src/vs/workbench/browser/parts/globalCompositeBar.js b/vscode/src/vs/workbench/browser/parts/globalCompositeBar.js
index dbf2cd589ee4f869b69ffa850d0b8a11f1e656b4..a94a5a349e647b6b900e055c930c88828a8c238b 100644
--- a/vscode/src/vs/workbench/browser/parts/globalCompositeBar.js
+++ b/vscode/src/vs/workbench/browser/parts/globalCompositeBar.js
@@ -117,15 +117,7 @@ let GlobalCompositeBar = class GlobalCompositeBar extends Disposable {
         this.registerListeners();
     }
     registerListeners() {
-        this.extensionService.whenInstalledExtensionsRegistered().then(() => {
-            if (!this._store.isDisposed) {
-                this._register(this.storageService.onDidChangeValue(
-                    StorageScope.PROFILE,
-                    AccountsActivityActionViewItem.ACCOUNTS_VISIBILITY_PREFERENCE_KEY,
-                    this._store
-                )(() => this.toggleAccountsActivity()));
-            }
-        });
+        // Plain intentionally has no accounts surface.
     }
     create(parent) {
         parent.appendChild(this.element);
@@ -137,30 +129,16 @@ let GlobalCompositeBar = class GlobalCompositeBar extends Disposable {
         return this.globalActivityActionBar.viewItems.length;
     }
     getContextMenuActions() {
-        return [toAction({
-            id: "toggleAccountsVisibility",
-            label: ( localize(3789, "Accounts")),
-            checked: this.accountsVisibilityPreference,
-            run: () => this.accountsVisibilityPreference = !this.accountsVisibilityPreference
-        })];
+        return [];
     }
     toggleAccountsActivity() {
-        if (this.globalActivityActionBar.length() === 2 && this.accountsVisibilityPreference) {
-            return;
-        }
-        if (this.globalActivityActionBar.length() === 2) {
-            this.globalActivityActionBar.pull(GlobalCompositeBar_1.ACCOUNTS_ACTION_INDEX);
-        } else {
-            this.globalActivityActionBar.push(this.accountAction, {
-                index: GlobalCompositeBar_1.ACCOUNTS_ACTION_INDEX
-            });
-        }
+        // Plain intentionally has no accounts surface.
     }
     get accountsVisibilityPreference() {
-        return isAccountsActionVisible(this.storageService);
+        return false;
     }
     set accountsVisibilityPreference(value) {
-        setAccountsActionVisible(this.storageService, value);
+        // Plain intentionally has no accounts surface.
     }
 };
 GlobalCompositeBar = GlobalCompositeBar_1 = ( __decorate([( __param(3, IConfigurationService)), ( __param(4, IInstantiationService)), ( __param(5, IStorageService)), ( __param(6, IExtensionService))], GlobalCompositeBar));
@@ -732,20 +710,7 @@ let SimpleGlobalActivityActionViewItem = class SimpleGlobalActivityActionViewIte
 };
 SimpleGlobalActivityActionViewItem = ( __decorate([( __param(2, IUserDataProfileService)), ( __param(3, IThemeService)), ( __param(4, IHoverService)), ( __param(5, IMenuService)), ( __param(6, IContextMenuService)), ( __param(7, IContextKeyService)), ( __param(8, IConfigurationService)), ( __param(9, IWorkbenchEnvironmentService)), ( __param(10, IKeybindingService)), ( __param(11, IInstantiationService)), ( __param(12, IActivityService)), ( __param(13, IStorageService))], SimpleGlobalActivityActionViewItem));
 function simpleActivityContextMenuActions(storageService, isAccount) {
-    const currentElementContextMenuActions = [];
-    if (isAccount) {
-        currentElementContextMenuActions.push(toAction({
-            id: "hideAccounts",
-            label: ( localize(3787, "Hide Accounts")),
-            run: () => setAccountsActionVisible(storageService, false)
-        }), ( new Separator()));
-    }
-    return [...currentElementContextMenuActions, toAction({
-        id: "toggle.hideAccounts",
-        label: ( localize(3789, "Accounts")),
-        checked: isAccountsActionVisible(storageService),
-        run: () => setAccountsActionVisible(storageService, !isAccountsActionVisible(storageService))
-    }), toAction({
+    return [toAction({
         id: "toggle.hideManage",
         label: ( localize(3788, "Manage")),
         checked: true,
@@ -756,19 +721,10 @@ function simpleActivityContextMenuActions(storageService, isAccount) {
     })];
 }
 function isAccountsActionVisible(storageService) {
-    return storageService.getBoolean(
-        AccountsActivityActionViewItem.ACCOUNTS_VISIBILITY_PREFERENCE_KEY,
-        StorageScope.PROFILE,
-        true
-    );
+    return false;
 }
 function setAccountsActionVisible(storageService, visible) {
-    storageService.store(
-        AccountsActivityActionViewItem.ACCOUNTS_VISIBILITY_PREFERENCE_KEY,
-        visible,
-        StorageScope.PROFILE,
-        StorageTarget.USER
-    );
+    // Plain intentionally has no accounts surface.
 }
 
 export { AccountsActivityActionViewItem, GlobalActivityActionViewItem, GlobalCompositeBar, SimpleAccountActivityActionViewItem, SimpleGlobalActivityActionViewItem, isAccountsActionVisible };
diff --git a/vscode/src/vs/workbench/contrib/files/browser/editors/textFileSaveErrorHandler.js b/vscode/src/vs/workbench/contrib/files/browser/editors/textFileSaveErrorHandler.js
index 145182985cf7b0cde4731c5176834c70f74b1f41..adb80a904b389beded5b669634749e6aa27c19fa 100644
--- a/vscode/src/vs/workbench/contrib/files/browser/editors/textFileSaveErrorHandler.js
+++ b/vscode/src/vs/workbench/contrib/files/browser/editors/textFileSaveErrorHandler.js
@@ -5,7 +5,7 @@ import { toErrorMessage } from '../../../../../base/common/errorMessage.js';
 import { isEqual, basename } from '../../../../../base/common/resources.js';
 import { Action } from '../../../../../base/common/actions.js';
 import { URI } from '../../../../../base/common/uri.js';
-import { FileOperationResult } from '../../../../../platform/files/common/files.js';
+import { FileOperationResult, getPlainWorkspaceWriteOutcome } from '../../../../../platform/files/common/files.js';
 import { ITextFileService } from '../../../../services/textfile/common/textfiles.service.js';
 import { IInstantiationService } from '../../../../../platform/instantiation/common/instantiation.js';
 import { Disposable, dispose } from '../../../../../base/common/lifecycle.js';
@@ -35,10 +35,55 @@ import Severity$1 from '../../../../../base/common/severity.js';
 const CONFLICT_RESOLUTION_CONTEXT = "saveConflictResolutionContext";
 const CONFLICT_RESOLUTION_SCHEME = "conflictResolution";
 const LEARN_MORE_DIRTY_WRITE_IGNORE_KEY = "learnMoreDirtyWriteError";
+const PLAIN_WORKSPACE_SCHEME = "plain-workspace";
 const conflictEditorHelp = ( localize(
     10886,
     "Use the actions in the editor tool bar to either undo your changes or overwrite the content of the file with your changes."
 ));
+function plainSaveFailureMessage(resource, outcome) {
+    if (outcome?.status === "targetPublished") {
+        return ( localize(
+            10906,
+            "Failed to save '{0}': The file may have changed. Reload it before saving again, or use Save As to preserve your edits.",
+            basename(resource)
+        ));
+    }
+    if (outcome?.status === "outcomeUnknown") {
+        return ( localize(
+            10907,
+            "Failed to save '{0}': The write result could not be verified. Reload the file before saving again, or use Save As to preserve your edits.",
+            basename(resource)
+        ));
+    }
+    return ( localize(
+        10908,
+        "Failed to save '{0}'. Reload the file before saving again, or use Save As to preserve your edits.",
+        basename(resource)
+    ));
+}
+function plainSaveFailureDetails(outcome) {
+    if (outcome?.status === "targetPublished") {
+        return ( localize(
+            10909,
+            "Write outcome: target published; evidence {0}; rename {1}; directory sync {2}; target {3}.",
+            outcome.publicationEvidence,
+            outcome.rename,
+            outcome.directorySync,
+            outcome.target
+        ));
+    }
+    if (outcome?.status === "outcomeUnknown") {
+        return ( localize(
+            10910,
+            "Write outcome: unknown; observation {0}; rename {1}; directory sync {2}; target {3}.",
+            outcome.observation,
+            outcome.rename,
+            outcome.directorySync,
+            outcome.target
+        ));
+    }
+    return ( localize(10911, "The write was rejected without a confirmed file update."));
+}
 let TextFileSaveErrorHandler = class TextFileSaveErrorHandler extends Disposable {
     static {
         this.ID = "workbench.contrib.textFileSaveErrorHandler";
@@ -106,7 +151,14 @@ let TextFileSaveErrorHandler = class TextFileSaveErrorHandler extends Disposable
         let message;
         const primaryActions = [];
         const secondaryActions = [];
-        if (fileOperationError.fileOperationResult === FileOperationResult.FILE_MODIFIED_SINCE) {
+        if (resource.scheme === PLAIN_WORKSPACE_SCHEME) {
+            const outcome = getPlainWorkspaceWriteOutcome(error);
+            message = plainSaveFailureMessage(resource, outcome);
+            primaryActions.push(( new PlainReloadModelAction(model)));
+            primaryActions.push(this.instantiationService.createInstance(SaveModelAsAction, model));
+            primaryActions.push(( new PlainWriteDetailsAction(plainSaveFailureDetails(outcome), this.notificationService)));
+        }
+        else if (fileOperationError.fileOperationResult === FileOperationResult.FILE_MODIFIED_SINCE) {
             if (this.activeConflictResolutionResource && isEqual(this.activeConflictResolutionResource, model.resource)) {
                 if (this.storageService.getBoolean(LEARN_MORE_DIRTY_WRITE_IGNORE_KEY, StorageScope.APPLICATION)) {
                     return;
@@ -331,6 +383,27 @@ class RetrySaveModelAction extends Action {
         }
     }
 }
+class PlainReloadModelAction extends Action {
+    constructor(model) {
+        super("workbench.files.action.plainReload", ( localize(10912, "Reload")));
+        this.model = model;
+    }
+    async run() {
+        if (!this.model.isDisposed()) {
+            await this.model.revert();
+        }
+    }
+}
+class PlainWriteDetailsAction extends Action {
+    constructor(details, notificationService) {
+        super("workbench.files.action.plainWriteDetails", ( localize(10913, "Details")));
+        this.details = details;
+        this.notificationService = notificationService;
+    }
+    async run() {
+        this.notificationService.info(this.details);
+    }
+}
 class RevertModelAction extends Action {
     constructor(model) {
         super("workbench.files.action.revertModel", ( localize(10903, "Revert")));
@@ -431,6 +504,9 @@ async function acceptOrRevertLocalChangesCommand(accessor, resource, accept) {
     if (!URI.isUri(resource)) {
         return;
     }
+    if (accept && resource.scheme === PLAIN_WORKSPACE_SCHEME) {
+        return;
+    }
     const editorPane = editorService.activeEditorPane;
     if (!editorPane) {
         return;
diff --git a/vscode/src/vs/workbench/contrib/files/browser/fileActions.js b/vscode/src/vs/workbench/contrib/files/browser/fileActions.js
index f4f533eb6e2e9cbcd50b4d4a4d90167b513462b3..d47a3d968f3f3d21bd04701f8a896313bc142b2b 100644
--- a/vscode/src/vs/workbench/contrib/files/browser/fileActions.js
+++ b/vscode/src/vs/workbench/contrib/files/browser/fileActions.js
@@ -10,6 +10,7 @@ import { Action } from '../../../../base/common/actions.js';
 import { dispose } from '../../../../base/common/lifecycle.js';
 import { VIEWLET_ID, VIEW_ID, UndoConfirmLevel } from '../common/files.js';
 import { IFileService } from '../../../../platform/files/common/files.service.js';
+import { classifyPlainWorkspaceDeleteResource, runPlainWorkspaceDeleteCoordinator } from '../../../../platform/files/common/plainWorkspaceDelete.js';
 import { EditorResourceAccessor, SideBySideEditor } from '../../../common/editor.js';
 import { ItemActivation } from '../../../../platform/quickinput/common/quickInput.js';
 import { IQuickInputService } from '../../../../platform/quickinput/common/quickInput.service.js';
@@ -99,6 +100,42 @@ async function deleteFiles(
         primaryButton = ( localize(10966, "&&Delete"));
     }
     const distinctElements = distinctParents(elements, e => e.resource);
+    const classifiedElements = distinctElements.map(element => ({
+        element,
+        classification: classifyPlainWorkspaceDeleteResource(element.resource)
+    }));
+    if (classifiedElements.some(value => value.classification === undefined)) {
+        throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: invalid deletion resource rejected"));
+    }
+    const plainElementCount = classifiedElements.reduce((count, value) => count + (value.classification?.kind === "plain" ? 1 : 0), 0);
+    if (plainElementCount > 0) {
+        if (plainElementCount !== classifiedElements.length) {
+            throw ( new Error("PLAIN_WORKSPACE_DELETE_CONTRACT: mixed-scheme deletion rejected"));
+        }
+        const plainElements = classifiedElements.map(({ element, classification }) => {
+            const resource = URI.from({
+                scheme: classification.scheme,
+                authority: classification.rootId,
+                path: `/${classification.relativePath}`
+            }, true);
+            resource.toString();
+            void resource.fsPath;
+            Object.freeze(resource);
+            return Object.freeze({
+                resource,
+                name: element.name,
+                isDirectory: element.isDirectory,
+                isSymbolicLink: element.isSymbolicLink
+            });
+        });
+        return runPlainWorkspaceDeleteCoordinator(Object.freeze({
+            elements: Object.freeze(plainElements),
+            explorerService,
+            dialogService,
+            workingCopyFileService,
+            filesConfigurationService
+        }));
+    }
     const dirtyWorkingCopies = ( new Set());
     for (const distinctElement of distinctElements) {
         for (const dirtyWorkingCopy of workingCopyFileService.getDirty(distinctElement.resource)) {
diff --git a/vscode/src/vs/workbench/services/textfile/common/textFileEditorModel.js b/vscode/src/vs/workbench/services/textfile/common/textFileEditorModel.js
index e743e309f1b308aebcb4559a12d9591bd0d49701..3d4bda71433542a808b6827671788e0573d4e653 100644
--- a/vscode/src/vs/workbench/services/textfile/common/textFileEditorModel.js
+++ b/vscode/src/vs/workbench/services/textfile/common/textFileEditorModel.js
@@ -34,6 +34,57 @@ import { IProgressService } from '../../../../platform/progress/common/progress.
 import { isCancellationError } from '../../../../base/common/errors.js';
 import { EditSources } from '../../../../editor/common/textModelEditSource.js';
 
+const PLAIN_WORKSPACE_SCHEME = "plain-workspace";
+const PLAIN_BUFFER_NO_BASELINE_ETAG = "plain-buffer-no-baseline";
+const PLAIN_WORKSPACE_VERSION_PATTERN = /^wv1:[0-9a-f]{64}$/;
+const PLAIN_READ_RECEIPT_SOURCE = Symbol("plainReadReceipt");
+const PLAIN_BUFFER_NO_BASELINE_SOURCE = Symbol("plainBufferNoBaseline");
+const PLAIN_WRITE_RECEIPT_SOURCE = Symbol("plainWriteReceipt");
+function plainReadBaselineSource(resource, content) {
+    if (resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+        return undefined;
+    }
+    const descriptor = Object.getOwnPropertyDescriptor(content, "plainReadReceipt");
+    if (!descriptor || !("value" in descriptor) || descriptor.value !== true) {
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_READ_RECEIPT: resolved file content is missing its receipt marker"));
+    }
+    return PLAIN_READ_RECEIPT_SOURCE;
+}
+function plainWriteBaselineSource(resource, stat) {
+    if (resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+        return undefined;
+    }
+    const descriptor = stat && typeof stat === "object" ? Object.getOwnPropertyDescriptor(stat, "plainWriteReceipt") : undefined;
+    if (!descriptor || !("value" in descriptor) || descriptor.value !== true) {
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_RECEIPT: successful save stat is missing its receipt marker"));
+    }
+    return PLAIN_WRITE_RECEIPT_SOURCE;
+}
+function acceptsPlainBaseline(resource, source, stat) {
+    if (resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+        return false;
+    }
+    if (source === PLAIN_READ_RECEIPT_SOURCE) {
+        if (stat.etag === ETAG_DISABLED || typeof stat.etag === "string" && PLAIN_WORKSPACE_VERSION_PATTERN.test(stat.etag)) {
+            return true;
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_READ_RECEIPT: read baseline must carry a wv1 token or ETAG_DISABLED"));
+    }
+    if (source === PLAIN_BUFFER_NO_BASELINE_SOURCE) {
+        if (stat.etag === PLAIN_BUFFER_NO_BASELINE_ETAG) {
+            return true;
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_BUFFER_BASELINE: buffer baseline must carry the no-baseline sentinel"));
+    }
+    if (source === PLAIN_WRITE_RECEIPT_SOURCE) {
+        if (typeof stat.etag === "string" && PLAIN_WORKSPACE_VERSION_PATTERN.test(stat.etag)) {
+            return true;
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_RECEIPT: write baseline must carry a wv1 token"));
+    }
+    return false;
+}
+
 var TextFileEditorModel_1;
 let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel {
     static {
@@ -112,6 +163,7 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
         this.inConflictMode = false;
         this.inOrphanMode = false;
         this.inErrorMode = false;
+        this.plainSaveRequiresReload = false;
         this.hasEncodingSetExplicitly = false;
         this.name = basename(this.labelService.getUriLabel(this.resource));
         this.resourceHasExtension = !!extUri.extname(this.resource);
@@ -255,22 +307,30 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
     }
     async resolveFromBuffer(buffer, options) {
         this.trace("resolveFromBuffer()");
+        const isPlainWorkspace = this.resource.scheme === PLAIN_WORKSPACE_SCHEME;
         let mtime;
         let ctime;
         let size;
         let etag;
+        let readonly = false;
+        let locked = false;
         try {
             const metadata = await this.fileService.stat(this.resource);
             mtime = metadata.mtime;
             ctime = metadata.ctime;
             size = metadata.size;
-            etag = metadata.etag;
+            etag = isPlainWorkspace ? PLAIN_BUFFER_NO_BASELINE_ETAG : metadata.etag;
+            if (isPlainWorkspace) {
+                readonly = Boolean(metadata.readonly);
+                locked = Boolean(metadata.locked);
+            }
             this.setOrphaned(false);
         } catch (error) {
             mtime = Date.now();
             ctime = Date.now();
             size = 0;
-            etag = ETAG_DISABLED;
+            etag = isPlainWorkspace ? PLAIN_BUFFER_NO_BASELINE_ETAG : ETAG_DISABLED;
+            readonly = isPlainWorkspace;
             this.setOrphaned(error.fileOperationResult === FileOperationResult.FILE_NOT_FOUND);
         }
         const preferredEncoding = await this.textFileService.encoding.getPreferredWriteEncoding(this.resource, this.preferredEncoding);
@@ -283,10 +343,10 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
             etag,
             value: buffer,
             encoding: preferredEncoding.encoding,
-            readonly: false,
-            locked: false,
+            readonly,
+            locked,
             executable: false
-        }, true, options);
+        }, true, options, isPlainWorkspace ? PLAIN_BUFFER_NO_BASELINE_SOURCE : undefined);
     }
     async resolveFromBackup(options) {
         const backup = await this.workingCopyBackupService.resolve(this);
@@ -353,7 +413,7 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
                 );
                 return;
             }
-            return this.resolveFromContent(content, false, options);
+            return this.resolveFromContent(content, false, options, plainReadBaselineSource(this.resource, content));
         } catch (error) {
             const result = error.fileOperationResult;
             this.setOrphaned(result === FileOperationResult.FILE_NOT_FOUND);
@@ -369,7 +429,7 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
             throw error;
         }
     }
-    resolveFromContent(content, dirty, options) {
+    resolveFromContent(content, dirty, options, plainBaselineSource) {
         this.trace("resolveFromContent() - enter");
         if (this.isDisposed()) {
             this.trace("resolveFromContent() - exit - because model is disposed");
@@ -389,7 +449,7 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
             isDirectory: false,
             isSymbolicLink: false,
             children: undefined
-        });
+        }, plainBaselineSource);
         const oldEncoding = this.contentEncoding;
         this.contentEncoding = content.encoding;
         if (this.preferredEncoding) {
@@ -513,6 +573,10 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
         if (!this.isResolved()) {
             return false;
         }
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME && this.plainSaveRequiresReload) {
+            this.trace("save() - refusing to reuse an unresolved Plain write baseline");
+            return false;
+        }
         if (this.isReadonly()) {
             this.trace("save() - ignoring request for readonly resource");
             return false;
@@ -532,6 +596,10 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
         if (typeof options.reason !== "number") {
             options.reason = SaveReason.EXPLICIT;
         }
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME && this.plainSaveRequiresReload) {
+            this.trace("doSave() - refusing to reuse an unresolved Plain write baseline");
+            return;
+        }
         const versionId = this.versionId;
         this.trace(`doSave(${versionId}) - enter with versionId ${versionId}`);
         if (this.ignoreSaveFromSaveParticipants) {
@@ -648,7 +716,7 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
         })(), () => saveCancellation.cancel());
     }
     handleSaveSuccess(stat, versionId, options) {
-        this.updateLastResolvedFileStat(stat);
+        this.updateLastResolvedFileStat(stat, plainWriteBaselineSource(this.resource, stat));
         if (versionId === this.versionId) {
             this.trace(
                 `handleSaveSuccess(${versionId}) - setting dirty to false because versionId did not change`
@@ -671,11 +739,18 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
             `[text file model] handleSaveError(${versionId}) - exit - resulted in a save error: ${( error.toString())}`,
             ( this.resource.toString())
         ]);
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME) {
+            this.plainSaveRequiresReload = true;
+            this.setDirty(true);
+            this.inErrorMode = true;
+        }
         if (options.ignoreErrorHandler) {
             throw error;
         }
-        this.setDirty(true);
-        this.inErrorMode = true;
+        if (this.resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+            this.setDirty(true);
+            this.inErrorMode = true;
+        }
         if (error.fileOperationResult === FileOperationResult.FILE_MODIFIED_SINCE) {
             this.inConflictMode = true;
         }
@@ -687,9 +762,13 @@ let TextFileEditorModel = class TextFileEditorModel extends BaseTextEditorModel
             this.bufferSavedVersionId = this.textEditorModel.getAlternativeVersionId();
         }
     }
-    updateLastResolvedFileStat(newFileStat) {
+    updateLastResolvedFileStat(newFileStat, plainBaselineSource) {
         const oldReadonly = this.isReadonly();
-        if (!this.lastResolvedFileStat) {
+        if (acceptsPlainBaseline(this.resource, plainBaselineSource, newFileStat)) {
+            this.lastResolvedFileStat = newFileStat;
+            this.plainSaveRequiresReload = plainBaselineSource === PLAIN_BUFFER_NO_BASELINE_SOURCE;
+        }
+        else if (!this.lastResolvedFileStat) {
             this.lastResolvedFileStat = newFileStat;
         }
         else if (this.lastResolvedFileStat.mtime <= newFileStat.mtime) {
diff --git a/vscode/src/vs/workbench/services/workingCopy/common/storedFileWorkingCopy.js b/vscode/src/vs/workbench/services/workingCopy/common/storedFileWorkingCopy.js
index 0a769e25cf6030e9aa17ae5ec845795f964e5017..0ca61d77c1a1dccfdc383e4c1faec37a5fe54953 100644
--- a/vscode/src/vs/workbench/services/workingCopy/common/storedFileWorkingCopy.js
+++ b/vscode/src/vs/workbench/services/workingCopy/common/storedFileWorkingCopy.js
@@ -3,7 +3,7 @@ import { __decorate, __param } from '../../../../../../../external/tslib/tslib.e
 import { localize } from '../../../../nls.js';
 import { Emitter, Event } from '../../../../base/common/event.js';
 import { CancellationToken, CancellationTokenSource } from '../../../../base/common/cancellation.js';
-import { ETAG_DISABLED, FileOperationResult, NotModifiedSinceFileOperationError } from '../../../../platform/files/common/files.js';
+import { ETAG_DISABLED, FileOperationResult, NotModifiedSinceFileOperationError, getPlainWorkspaceWriteOutcome } from '../../../../platform/files/common/files.js';
 import { IFileService } from '../../../../platform/files/common/files.service.js';
 import { SaveReason } from '../../../common/editor.js';
 import { IWorkingCopyService } from './workingCopyService.service.js';
@@ -30,6 +30,101 @@ import { IProgressService } from '../../../../platform/progress/common/progress.
 import { isCancellationError } from '../../../../base/common/errors.js';
 import Severity$1 from '../../../../base/common/severity.js';
 
+const PLAIN_WORKSPACE_SCHEME = "plain-workspace";
+const PLAIN_BUFFER_NO_BASELINE_ETAG = "plain-buffer-no-baseline";
+const PLAIN_WORKSPACE_VERSION_PATTERN = /^wv1:[0-9a-f]{64}$/;
+const PLAIN_READ_RECEIPT_SOURCE = Symbol("plainReadReceipt");
+const PLAIN_BUFFER_NO_BASELINE_SOURCE = Symbol("plainBufferNoBaseline");
+const PLAIN_WRITE_RECEIPT_SOURCE = Symbol("plainWriteReceipt");
+function plainReadBaselineSource(resource, content) {
+    if (resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+        return undefined;
+    }
+    const descriptor = Object.getOwnPropertyDescriptor(content, "plainReadReceipt");
+    if (!descriptor || !("value" in descriptor) || descriptor.value !== true) {
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_READ_RECEIPT: resolved file content is missing its receipt marker"));
+    }
+    return PLAIN_READ_RECEIPT_SOURCE;
+}
+function plainWriteBaselineSource(resource, stat) {
+    if (resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+        return undefined;
+    }
+    const descriptor = stat && typeof stat === "object" ? Object.getOwnPropertyDescriptor(stat, "plainWriteReceipt") : undefined;
+    if (!descriptor || !("value" in descriptor) || descriptor.value !== true) {
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_RECEIPT: successful save stat is missing its receipt marker"));
+    }
+    return PLAIN_WRITE_RECEIPT_SOURCE;
+}
+function acceptsPlainBaseline(resource, source, stat) {
+    if (resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+        return false;
+    }
+    if (source === PLAIN_READ_RECEIPT_SOURCE) {
+        if (stat.etag === ETAG_DISABLED || typeof stat.etag === "string" && PLAIN_WORKSPACE_VERSION_PATTERN.test(stat.etag)) {
+            return true;
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_READ_RECEIPT: read baseline must carry a wv1 token or ETAG_DISABLED"));
+    }
+    if (source === PLAIN_BUFFER_NO_BASELINE_SOURCE) {
+        if (stat.etag === PLAIN_BUFFER_NO_BASELINE_ETAG) {
+            return true;
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_BUFFER_BASELINE: buffer baseline must carry the no-baseline sentinel"));
+    }
+    if (source === PLAIN_WRITE_RECEIPT_SOURCE) {
+        if (typeof stat.etag === "string" && PLAIN_WORKSPACE_VERSION_PATTERN.test(stat.etag)) {
+            return true;
+        }
+        throw ( new Error("PLAIN_WORKSPACE_INVALID_WRITE_RECEIPT: write baseline must carry a wv1 token"));
+    }
+    return false;
+}
+function plainSaveFailureMessage(name, outcome) {
+    if (outcome?.status === "targetPublished") {
+        return ( localize(
+            18313,
+            "Failed to save '{0}': The file may have changed. Reload it before saving again, or use Save As to preserve your edits.",
+            name
+        ));
+    }
+    if (outcome?.status === "outcomeUnknown") {
+        return ( localize(
+            18314,
+            "Failed to save '{0}': The write result could not be verified. Reload the file before saving again, or use Save As to preserve your edits.",
+            name
+        ));
+    }
+    return ( localize(
+        18315,
+        "Failed to save '{0}'. Reload the file before saving again, or use Save As to preserve your edits.",
+        name
+    ));
+}
+function plainSaveFailureDetails(outcome) {
+    if (outcome?.status === "targetPublished") {
+        return ( localize(
+            18316,
+            "Write outcome: target published; evidence {0}; rename {1}; directory sync {2}; target {3}.",
+            outcome.publicationEvidence,
+            outcome.rename,
+            outcome.directorySync,
+            outcome.target
+        ));
+    }
+    if (outcome?.status === "outcomeUnknown") {
+        return ( localize(
+            18317,
+            "Write outcome: unknown; observation {0}; rename {1}; directory sync {2}; target {3}.",
+            outcome.observation,
+            outcome.rename,
+            outcome.directorySync,
+            outcome.target
+        ));
+    }
+    return ( localize(18318, "The write was rejected without a confirmed file update."));
+}
+
 var StoredFileWorkingCopy_1;
 var StoredFileWorkingCopyState;
 (function(StoredFileWorkingCopyState) {
@@ -107,6 +202,7 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
         this.ignoreSaveFromSaveParticipants = false;
         this.inConflictMode = false;
         this.inErrorMode = false;
+        this.plainSaveRequiresReload = false;
         this._register(workingCopyService.registerWorkingCopy(this));
         this.registerListeners();
     }
@@ -187,22 +283,30 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
     }
     async resolveFromBuffer(buffer) {
         this.trace("resolveFromBuffer()");
+        const isPlainWorkspace = this.resource.scheme === PLAIN_WORKSPACE_SCHEME;
         let mtime;
         let ctime;
         let size;
         let etag;
+        let readonly = false;
+        let locked = false;
         try {
             const metadata = await this.fileService.stat(this.resource);
             mtime = metadata.mtime;
             ctime = metadata.ctime;
             size = metadata.size;
-            etag = metadata.etag;
+            etag = isPlainWorkspace ? PLAIN_BUFFER_NO_BASELINE_ETAG : metadata.etag;
+            if (isPlainWorkspace) {
+                readonly = Boolean(metadata.readonly);
+                locked = Boolean(metadata.locked);
+            }
             this.setOrphaned(false);
         } catch (error) {
             mtime = Date.now();
             ctime = Date.now();
             size = 0;
-            etag = ETAG_DISABLED;
+            etag = isPlainWorkspace ? PLAIN_BUFFER_NO_BASELINE_ETAG : ETAG_DISABLED;
+            readonly = isPlainWorkspace;
             this.setOrphaned(error.fileOperationResult === FileOperationResult.FILE_NOT_FOUND);
         }
         return this.resolveFromContent({
@@ -213,10 +317,10 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
             size,
             etag,
             value: buffer,
-            readonly: false,
-            locked: false,
+            readonly,
+            locked,
             executable: false
-        }, true );
+        }, true, isPlainWorkspace ? PLAIN_BUFFER_NO_BASELINE_SOURCE : undefined);
     }
     async resolveFromBackup() {
         const backup = await this.workingCopyBackupService.resolve(this);
@@ -273,7 +377,7 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
                 );
                 return;
             }
-            await this.resolveFromContent(content, false );
+            await this.resolveFromContent(content, false, plainReadBaselineSource(this.resource, content));
         } catch (error) {
             const result = error.fileOperationResult;
             this.setOrphaned(result === FileOperationResult.FILE_NOT_FOUND);
@@ -289,7 +393,7 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
             throw error;
         }
     }
-    async resolveFromContent(content, dirty) {
+    async resolveFromContent(content, dirty, plainBaselineSource) {
         this.trace("resolveFromContent() - enter");
         if (this.isDisposed()) {
             this.trace("resolveFromContent() - exit - because working copy is disposed");
@@ -309,7 +413,7 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
             isDirectory: false,
             isSymbolicLink: false,
             children: undefined
-        });
+        }, plainBaselineSource);
         if (this.isResolved()) {
             await this.doUpdateModel(content.value);
         }
@@ -404,6 +508,10 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
         if (!this.isResolved()) {
             return false;
         }
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME && this.plainSaveRequiresReload) {
+            this.trace("save() - refusing to reuse an unresolved Plain write baseline");
+            return false;
+        }
         if (this.isReadonly()) {
             this.trace("save() - ignoring request for readonly resource");
             return false;
@@ -423,6 +531,10 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
         if (typeof options.reason !== "number") {
             options.reason = SaveReason.EXPLICIT;
         }
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME && this.plainSaveRequiresReload) {
+            this.trace("doSave() - refusing to reuse an unresolved Plain write baseline");
+            return;
+        }
         const versionId = this.versionId;
         this.trace(`doSave(${versionId}) - enter with versionId ${versionId}`);
         if (this.ignoreSaveFromSaveParticipants) {
@@ -567,7 +679,7 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
         })(), () => saveCancellation.cancel());
     }
     handleSaveSuccess(stat, versionId, options) {
-        this.updateLastResolvedFileStat(stat);
+        this.updateLastResolvedFileStat(stat, plainWriteBaselineSource(this.resource, stat));
         if (versionId === this.versionId) {
             this.trace(
                 `handleSaveSuccess(${versionId}) - setting dirty to false because versionId did not change`
@@ -591,11 +703,18 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
             ( this.resource.toString()),
             this.typeId
         ]);
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME) {
+            this.plainSaveRequiresReload = true;
+            this.setDirty(true);
+            this.inErrorMode = true;
+        }
         if (options.ignoreErrorHandler) {
             throw error;
         }
-        this.setDirty(true);
-        this.inErrorMode = true;
+        if (this.resource.scheme !== PLAIN_WORKSPACE_SCHEME) {
+            this.setDirty(true);
+            this.inErrorMode = true;
+        }
         if (error.fileOperationResult === FileOperationResult.FILE_MODIFIED_SINCE) {
             this.inConflictMode = true;
         }
@@ -606,7 +725,38 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
         const fileOperationError = error;
         const primaryActions = [];
         let message;
-        if (fileOperationError.fileOperationResult === FileOperationResult.FILE_MODIFIED_SINCE) {
+        if (this.resource.scheme === PLAIN_WORKSPACE_SCHEME) {
+            const outcome = getPlainWorkspaceWriteOutcome(error);
+            const details = plainSaveFailureDetails(outcome);
+            message = plainSaveFailureMessage(this.name, outcome);
+            primaryActions.push(toAction({
+                id: "fileWorkingCopy.plainReload",
+                label: ( localize(18319, "Reload")),
+                run: () => this.revert()
+            }));
+            primaryActions.push(toAction({
+                id: "fileWorkingCopy.plainSaveAs",
+                label: ( localize(18306, "Save As...")),
+                run: async () => {
+                    const editor = this.workingCopyEditorService.findEditor(this);
+                    if (editor) {
+                        const result = await this.editorService.save(editor, {
+                            saveAs: true,
+                            reason: SaveReason.EXPLICIT
+                        });
+                        if (!result.success) {
+                            this.doHandleSaveError(error, options);
+                        }
+                    }
+                }
+            }));
+            primaryActions.push(toAction({
+                id: "fileWorkingCopy.plainWriteDetails",
+                label: ( localize(18320, "Details")),
+                run: () => this.notificationService.info(details)
+            }));
+        }
+        else if (fileOperationError.fileOperationResult === FileOperationResult.FILE_MODIFIED_SINCE) {
             message = ( localize(
                 18298,
                 "Failed to save '{0}': The content of the file is newer. Do you want to overwrite the file with your changes?",
@@ -741,9 +891,13 @@ let StoredFileWorkingCopy = class StoredFileWorkingCopy extends ResourceWorkingC
         );
         this._register(Event.once(handle.onDidClose)(() => listener.dispose()));
     }
-    updateLastResolvedFileStat(newFileStat) {
+    updateLastResolvedFileStat(newFileStat, plainBaselineSource) {
         const oldReadonly = this.isReadonly();
-        if (!this.lastResolvedFileStat) {
+        if (acceptsPlainBaseline(this.resource, plainBaselineSource, newFileStat)) {
+            this.lastResolvedFileStat = newFileStat;
+            this.plainSaveRequiresReload = plainBaselineSource === PLAIN_BUFFER_NO_BASELINE_SOURCE;
+        }
+        else if (!this.lastResolvedFileStat) {
             this.lastResolvedFileStat = newFileStat;
         }
         else if (this.lastResolvedFileStat.mtime <= newFileStat.mtime) {
