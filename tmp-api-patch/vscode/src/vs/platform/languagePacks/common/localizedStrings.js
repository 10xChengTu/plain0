
import { localize } from '../../../nls.js';

const open = ( localize(2136, "open"));
const close = ( localize(2137, "close"));
const find = ( localize(2138, "find"));
var localizedStrings = {
    open: open,
    close: close,
    find: find
};

export { localizedStrings as default };
